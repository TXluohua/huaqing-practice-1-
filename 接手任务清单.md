# 接手任务清单（下一位接手人）

> **这份文档只回答一件事：你按什么顺序做什么、怎么算做完。**
> 需要细节时再点开文末的「文档地图」；这里不重复抄接口定义。
>
> **基线**：分支 `feature-txluohua-dev` @ **`26f8e7e`**（领先 `origin/main` 11 个提交，**尚未合入**）
> **当前状态**：后端三块新业务 + 问答主链路全部完成并实测通过；四个业务页面**骨架可用**，需要打磨。
> 更新日期：2026-09-20

---

## 0. 五分钟进入状态

### 0.1 跑起来

```bash
git clone git@github.com:TXluohua/huaqing-practice-1-.git && cd huaqing-practice-1-
git checkout feature-txluohua-dev

make install                                             # 后端依赖（venv）
.venv/bin/python scripts/prepare_model.py --check         # 看本地模型是否就绪
.venv/bin/python scripts/prepare_model.py --download --source modelscope --with-reranker
make ingest                                              # 建索引：5 文档 / 32 切片

cp .env.example .env                                     # 填 DEEPSEEK_API_KEY / DASHSCOPE_API_KEY
make dev                                                 # 后端 http://127.0.0.1:8000（/docs 可看全部 35 个接口）
make install-web && make dev-web                          # 前端 http://127.0.0.1:5173
```

没有模型/密钥也能跑通（`EMBEDDING_PROVIDER=hashing RERANK_PROVIDER=lexical`），
但**评测数字会明显下降**，别用降级值交差。

### 0.2 现在能演示什么

前端共 7 个页面，顶栏分两组（中间竖线分隔）：

| 组 | 路由 | 页面 |
| --- | --- | --- |
| 问答组 | `/chat`、`/history`、`/admin` | 问答（SSE 流式 + 引用卡片 + 置信度）、历史会话、管理（入库/重建/切片/高频问题） |
| 业务组 | `/plans`、`/parts`、`/procurement`、`/training` | 维护计划、备件商城（含购物车）、采购结算、考核认证 |

**记住一件事**：业务组这四页**与问答完全独立**，这是项目方明确要求的红线（见 §2.3）。

### 0.3 基线数字（自己跑一遍核对，别信我）

| 项 | 期望值 | 命令 |
| --- | --- | --- |
| 单测 | **139 passed** | `.venv/bin/python -m pytest tests/ -q` |
| 接口 | **35 个**（原 11 + 业务 24） | 打开 `http://127.0.0.1:8000/docs` |
| 健康检查 | 7 个组件全 `ok` | `curl -s localhost:8000/api/health` |
| 文本评测（45 条） | G1 **100%** / G2 覆盖率 **100%**（270 引用、**0 伪造**）/ G3 严格 **89.7%**（宽松 100%）/ 负样本拒答 **100%** / MRR **0.9750** | `.venv/bin/python scripts/eval.py` |
| 图片评测（5 条） | **5/5** | `.venv/bin/python scripts/eval_image.py` |
| 前端类型检查 | **0 错误** | `cd frontend && npx vue-tsc --noEmit` |

> ⚠️ 延迟（P95）随机器负载浮动极大：空闲机器约 **3.9s**，同机并行跑演示与其它任务时实测到 **24s**。
> **评测请在空闲机器上跑**，否则会误判成性能回归。

---

## 1. 立刻做（P0，半小时内做完）

### T1 把这个分支合进 `main`（10 分钟）

- **为什么**：11 个提交还没进 `main`，后面所有人的工作都基于 `main`。
- **怎么做**：打开 **https://github.com/TXluohua/huaqing-practice-1-/pull/new/feature-txluohua-dev**
  → 目标分支 `main` → Create pull request → Merge（用 merge commit，别 squash，提交历史里
  每条都对应一个可复跑的验证）。
- **验收**：`git fetch && git log --oneline -1 origin/main` 能看到 `docs: 页面独立性与空购物车口径…`。

### T2 轮换两个密钥（5 分钟，**安全事项，别跳过**）

- **为什么**：`DEEPSEEK_API_KEY` 和 `DASHSCOPE_API_KEY` 在开发过程的聊天记录里**以明文出现过**，
  必须视为已泄漏。
- **怎么做**：去 DeepSeek / 阿里云百炼控制台**作废旧 key、生成新 key**，写进本地 `.env`
  （`.env` 已在 `.gitignore` 里、权限 600）。
- **验收**：
  ```bash
  git grep -nE "sk-[A-Za-z0-9._-]{10,}" -- .   # 必须无输出
  git ls-files .env | wc -l                    # 必须是 0
  ```

### T3 跑一遍全量自测，拿到你自己的基线（15 分钟）

```bash
.venv/bin/python -m pytest tests/ -q                 # 期望 139 passed
.venv/bin/python scripts/eval.py                     # 期望 §0.3 的六个数字
.venv/bin/python scripts/eval_image.py               # 期望 5/5
.venv/bin/python scripts/check_golden.py             # 黄金集自检：引用章节真实存在
.venv/bin/python scripts/demo_walkthrough.py         # 问答侧 4 个演示场景走查
cd frontend && npx vue-tsc --noEmit                  # 期望 0 错误
```

数字对不上就先别改代码，把差异贴到回执里（§8）——很可能是密钥没配或机器在跑别的东西。

---

## 2. 你的主要工作：四个业务页面打磨（P0，1~2 天）

### 2.0 先搞清楚边界：后端与页面骨架都已经就位

- **后端**：24 个接口、6 张表、41 个业务单测，全部端到端验证过。**不需要你写后端**。
- **页面**：`PlanView.vue`、`PartsView.vue`、`ProcurementView.vue`、`TrainingView.vue` 已经能跑通全流程。
  **你的活是打磨**（交互细节、异常态、空态、文案、演示流畅度），不是从零搭。
- 细节材料：**`前端对接说明（三块新业务）.md`**（请求/响应示例、按钮状态机、错误码、演示脚本、自测清单）。

### 2.1 逐页验收标准（这几条是硬要求）

| 页面 | 必须保持的行为 | 怎么验 |
| --- | --- | --- |
| `/plans` 维护计划 | 生成表单（设备型号/运行小时/片数/`top_k`）→ 预览或落库；**`uncovered` 必须如实展示**（不可关闭的警示，逐条列「名称：原因」）；每条计划项**都能点开看依据**（文档/版本/章节/页码）；剩余量负数显示「已超期 N 单位」；`due_at` 为 `null` 显示「按需执行」 | 用 `Etcher-A / 5200h / 46000 片` 生成一次，应有 8 条计划项 + 4 条 uncovered |
| `/parts` 备件商城 | 目录库存按 `stock === 0` 判缺货；**禁止替代清单必须红色常显、不许折叠**；替代件必须显示台账 `basis` 原文；购物车金额用后端 `total_amount`（**前端不许自己累加**）；提交文案是「提交采购申请」，**不许出现「一键下单/在线支付」** | 搜 `O-ring`：库存 12、价格 185、替代件 FFKM（560）带依据 |
| `/procurement` 采购结算 | **按钮完全由 `allowed_actions` 渲染**（不许前端硬编码状态机）；`approve` 文案是「人工确认」；驳回与撤销分开（撤销的 `reason` 必须 `cancel: ` 前缀）；409 `INVALID_STATE` 要自动重取详情；台账金额用后端 `total_amount` | 走一遍 草稿→提交→人工确认→收货→登记结算；重复结算应提示 `ALREADY_SETTLED` |
| `/training` 考核认证 | 每题**依据可展开**；`generator=extractive-fallback` 时标「离线出题」但**不能报错**；422 给人话提示；**交卷后不自动发证**，只有「申请认证」按钮；签发方固定「内部授权」，**界面不许出现「原厂认证」** | 出 5 题 → 交卷 → 通过 → 点「申请认证」→ 认证记录 +1；到期提醒可见 |

### 2.2 已知的待改进点（**我核查过代码，确认存在**；改不改由你判断，但要写进回执）

| 页面 | 现状 | 建议 |
| --- | --- | --- |
| 计划页 | 列表**不按设备型号过滤**，是跨设备统一待办队列 | 若希望按表单设备筛选，改一行 query（`listPlans({device_model})`） |
| 计划页 | `persist=false` 的预览条目 `id === null`，无法标记完成/跳过（操作只能在下方落库列表做） | 保持现状即可，或给预览加「保存后再操作」的引导 |
| 计划页 | 每页 10 条，`total ≤ 10` 不渲染分页器 | 可选：改成 20 条或加每页选择 |
| 商城页 | 没有草稿时是「加入第一件备件才建单」（懒建） | **后端已支持建空车**（`POST /parts/orders` 的 `items` 可为空），可改成进页面就建空车，体验更顺 |
| 商城/采购页 | `applicant` / `purpose` / `device_model` **只在建单那一次写入**（后端没有改表头的接口） | 如需「建单后还能改申请人/用途」，找后端加一个 `PATCH /parts/orders/{id}`（契约只追加，不会破坏现有页面） |
| 商城页 | 台账没写价格时 `price_cny = 0`，界面显示「台账未提供」 | 保持（**不许显示成 0 元**，这是刻意的诚实性设计） |
| 采购页 | `submit` / `approve` / `receive` 点击即执行，没有二次确认弹窗 | `approve` 是系统唯一的确认点，**建议加二次确认** |
| 采购页 | 已结算的订单仍会显示 `settle` 按钮，再点得到 409 `ALREADY_SETTLED` | 可据结算记录在前端隐藏该按钮 |
| 采购页 | 结算方式（`method`）是自由文本，默认「月结」（契约里没有枚举） | 若业务有固定枚举，让后端在 schema 里补 `Literal`，前端跟着换下拉 |
| 考核页 | **漏选的判断题按「错误」提交**（`toSubmitAnswer('judgement','')` → `false`），交卷前有确认提示 | 若要改成「跳过/不计分」，需同时改 `frontend/src/api/training.ts` 与后端判分口径 —— **先和后端确认再动** |
| 考核页 | 认证记录一次拉 50 条、无分页；出题主题默认留空 | 可选：加分页；主题留空是刻意的（避免默认主题抽不到依据直接 422） |

### 2.3 四页独立性的红线（**不许破坏**）

项目方明确要求「不要将这几个业务和问答绑一起，保持功能页面独立」。已落地的保证：

1. **路由独立**：`/plans`、`/parts`、`/procurement`、`/training` 与 `/chat` 并列，业务页**不许**嵌进问答页；
2. **API 独立**：业务页只 import `@/api/plans`、`@/api/parts`、`@/api/training`（共享类型放 `@/api/contracts.ts`）；
3. **不许引用**：`@/api/chat`、`@/utils/sse`、`@/stores/chat`、`@/stores/session` —— 业务页不用会话、不用 `qa_id`、不走 SSE；
4. **后端边界由测试强制**：`tests/test_api/test_module_independence.py`（7 项，用 AST 检查 import + 检查路由路径不相交），改坏立刻红。

改完必须跑这三条验证，结果写进回执：

```bash
# ① 业务页与问答零耦合（必须无输出）
grep -rn "@/api/chat\|@/utils/sse\|@/stores/" \
  frontend/src/views/PlanView.vue frontend/src/views/PartsView.vue \
  frontend/src/views/ProcurementView.vue frontend/src/views/TrainingView.vue

# ② 后端独立性测试（必须 7 passed）
.venv/bin/python -m pytest tests/test_api/test_module_independence.py -q

# ③ 类型检查 + 构建（必须 0 错误）
cd frontend && npx vue-tsc --noEmit && npx vite build --logLevel warn
```

---

## 3. 之后做（P1/P2，按价值从高到低）

| # | 事项 | 现状（实测） | 价值 / 影响 |
| --- | --- | --- | --- |
| 1 | **图片知识侧入库**（FR-02 另一面） | `kb_image` 表 **0 行**；抽图不写 caption | 图纸/参数表截图目前**检索不到**（作为**提问图片**已完全可用，图片评测 5/5） |
| 2 | **检索层权限分级**（FR-11） | `user_role` 已透传到检索层但**未参与过滤** | 检索即可见全部语料；需**先定义权限分级表**再动手 |
| 3 | **会话删除 / 重命名** | 接口未实现（接口文档 P2 档已定义） | 历史页无法管理会话；后端两个薄接口即可 |
| 4 | **并发吞吐**（NFR-02） | n=20 → P95 190s、吞吐恒定 ≈0.11 req/s，**不能支撑 20 并发** | 主因是 CPU 上 Cross-Encoder 精排 ≈4.9s/问且串行；两条低成本路径：外部查询与精排并行（省 ~4s）、GPU 部署；也可把 `rerank_max_candidates` 12→8 |
| 5 | 工单检索改语义召回 | 现在是关键词重合，无词重合即返回空 | 跨源体验（FR-03） |
| 6 | docx / pptx / xlsx 解析 | 上传返回 415（**刻意为之**，避免抓不到页码的切片） | 接 MarkItDown 后放开 |
| 7 | 阈值重标 | 精排 0.5 / 术语缺失比 0.40 / 引用归属 0.45 **仅在 45+5 条样本上标定** | 真实语料规模变化后重标，只改 `setting.py`，不改代码 |
| 8 | 采购审批身份校验 | `approve` 不校验角色（靠前端区分申请人/审批人视图） | 与 #2 同一根因（缺权限分级表） |
| 9 | 收货不扣库存 | `receive` 只登记「已到货」 | 真实入库需接 ERP |

---

## 4. 不要做（红线，避免返工）

1. **不许为了报告好看而放宽验收指标** —— G2 引用覆盖率 100% 是**不可降级**线；
   数字掉了就查原因，别改阈值（阈值改动要留下标定样本与依据）。
2. **契约只追加**：`backend/agents/state.py`、`backend/setting.py`、`backend/schemas.py`、`backend/db.py`
   只加字段/表，**不改既有字段语义**（前端与评测都依赖它们）。
3. **SSE 契约不许改**：`meta → image? → token* → citations → done`；`error` 只在失败时出现。
4. **业务红线（代码层强制，别在前端绕开）**：
   - **不对供应商发起真实下单**（只出内部采购申请单，`approve` 是人工确认点）；
   - **不自动发证**（交卷的 `certification_id` 恒为 `null`，发证须显式调用，签发方「内部授权」）；
   - **无依据不产出**（计划项与考题都必须带 `evidence`；替代件必须有台账 `basis`；
     知识库查不到的进 `uncovered`，**不许前端补数据**）。
5. **不许把业务塞回问答页**（§2.3）；也**不许**让业务页去调用 `/api/chat`。
6. **不许提交密钥**、不许把 `data/sqlite/*` 与 `frontend/dist/` 提交进仓库（已在 `.gitignore`）。
7. 别在没跑 §1.3 全量自测的情况下说「改完了」。

---

## 5. 自测命令速查

```bash
# 后端
.venv/bin/python -m pytest tests/ -q                        # 单测：期望 139 passed
.venv/bin/python -m pytest tests/test_api -q                 # 只跑接口/业务层
.venv/bin/python scripts/eval.py                             # 文本评测（45 条，产出 data/eval/report_*.json）
.venv/bin/python scripts/eval.py --badcases                  # 失败样例追加到 data/eval/badcases.md
.venv/bin/python scripts/eval_image.py                       # 图片评测（5 条）
.venv/bin/python scripts/check_golden.py                     # 黄金集自检
.venv/bin/python scripts/demo_walkthrough.py                 # 问答侧演示走查（A 抢修 / B 培训 / C 参数+备件 / D 拒答）
.venv/bin/python scripts/load_test.py                        # 并发压测（NFR-02）
.venv/bin/python scripts/inspect_chunks.py                   # 看切片（调分块参数时用）

# 前端
cd frontend && npx vue-tsc --noEmit                          # 类型检查：期望 0 错误
cd frontend && npx vite build --logLevel warn                # 构建（产物各自独立 chunk）
cd frontend && npm run dev                                    # 前端开发服（代理到 8000）

# 接口冒烟
curl -s localhost:8000/api/health | python3 -m json.tool | head -20
curl -s "localhost:8000/api/parts/catalog?limit=2" | python3 -m json.tool | head -30
curl -s "localhost:8000/api/plans?limit=2" | python3 -m json.tool | head -30
curl -s "localhost:8000/api/training/certifications/expiring?days=30" | python3 -m json.tool
```

---

## 6. 环境与凭据

| 项 | 说明 |
| --- | --- |
| Python | 3.10+，用 `.venv`（`make install` 会建；本机 venv 用了 `--system-site-packages`） |
| 本地模型 | `bge-large-zh-v1.5`（1024 维）+ `bge-reranker-v2-m3`，约 **4.6GB**，放 `backend/data/models`（**不入库**） |
| 密钥 | `DEEPSEEK_API_KEY`（文本生成）、`DASHSCOPE_API_KEY`（图片识别 qwen-vl-max），放 `.env`；**接手第一件事就是轮换（T2）** |
| 业务库 | 默认 SQLite `backend/data/sqlite/business.sqlite`（12 张表）；`.env` 可切 MySQL（对接方的在 `127.0.0.1:3307/smka`） |
| 会话检查点 | `backend/data/sqlite/checkpoints.sqlite`（LangGraph 异步 SQLite，重启后会话保留） |
| 端口 | 后端 8000（`make dev`）、前端 5173（`make dev-web`）；Vite 把 `/api`、`/static` 代理到 8000 |

---

## 7. 文档地图（哪份文档解决什么问题）

| 你想知道 | 看这份 |
| --- | --- |
| **我该做什么、怎么算做完** | **本文档** |
| 四个业务页面怎么对接（请求/响应/按钮状态机/错误码/演示脚本/自测清单） | `前端对接说明（三块新业务）.md`（§0 讲与问答的独立性） |
| 35 个接口的权威定义（字段、错误码、状态机） | `接口文档.md`（业务接口在 §10） |
| 交付了什么、怎么部署、怎么验收、有哪些已知限制 | `交付说明.md` |
| 每项功能完成到哪、实测证据、缺陷与修复记录 | `功能完成度清单.md`（§7 RAG 修复回执、§8 三块业务回执） |
| 检索/生成链路的实现细节与指标真值 | `RAG实现说明.md` |
| 两人分工与协作规范（契约冻结、PR 流程） | `后端二人分工与协作规范.md` |
| 更细的收尾任务（含已完成项的历史记录） | `收尾任务清单（第三人）.md` §8（T16~T20 前端打磨清单） |
| 项目最初的完整设计（需求、角色、验收指标） | `半导体设备维护知识库智能问答系统_开发文档（初稿）(2).md` |

---

## 8. 交付回执模板（做完请按这个格式回话）

```markdown
## 接手回执（YYYY-MM-DD）

### 基线核对
- pytest: __ / 139
- 文本评测: G1 __ / G2 __ / G3 严格 __ / 负样本 __ / MRR __
- 图片评测: __ / 5
- vue-tsc: __ 错误

### T1 合 PR
- [ ] 已合入 main（merge commit：____）
### T2 密钥轮换
- [ ] 旧 key 已作废、新 key 已写入 .env；`git grep sk-` 无输出
### T3 全量自测
- [ ] 命令与结果：____（数字不符的项及原因：____）

### 页面打磨（§2）
- [ ] /plans：____
- [ ] /parts：____
- [ ] /procurement：____
- [ ] /training：____
- 独立性验证三条命令结果：____

### 已知待改进点的处置（§2.2 那张表）
- 改了：____（为什么）
- 没改：____（为什么）

### 遗留 / 阻塞
- ____
```
