# 交付说明（最终版）

> **项目**：半导体设备维护知识库智能问答系统
> **交付日期**：2026-09-20
> **代码基线**：`main` = `363e137`（PR #8）；交付分支 `feature-txluohua-dev` = `26f8e7e`（含 11 个提交，**待合并**）
> **文档体系**：**`接手任务清单.md` 是接手人的第一入口**（做什么、怎么算做完、红线）；
> 本文是**交付主文档**；`README.md` 面向"怎么跑"，`接口文档.md` 面向接口契约，
> `功能完成度清单.md` 面向逐项完成度与证据，`前端对接说明（三块新业务）.md` 面向四个业务页面的对接与打磨，
> `收尾任务清单（第三人）.md` 面向更细的收尾项，`RAG实现说明.md` 面向检索生成链路的实现细节，
> `后端二人分工与协作规范.md` 面向分工与 Git 规范。
> 历史文档 `开发进度与交付说明.md` 描述的是骨架阶段，已被本文取代。

---

## 1. 交付内容一览

| 类别 | 内容 | 位置 |
| --- | --- | --- |
| 后端 | FastAPI 接入层（**35 接口** + SSE）、服务层（问答 / 计划 / 备件 / 考核）、业务库（12 张表）、会话检查点 | `backend/` |
| 智能体 | LangGraph 单图 **9 节点 + 1 条件边**（ingest_image / rewrite / retrieve / rerank / build_context / augment / generate / verify / respond） | `backend/agents/` |
| 检索生成 | 解析切片、向量库唯一出口、BM25+向量+RRF+精排、引用校验与归属、拒答闸门 | `backend/rag/` |
| 工具与数据源 | 8 个工具（知识库检索 / 题库 / 备件台账 / 术语 / 图片识别 / 工单 MCP） | `backend/tools/`、`backend/config/` |
| 前端 | Vue3 + Vite + Element Plus。**问答组**（`/chat`、`/history`、`/admin`：SSE 流式、引用卡片、置信度、反馈）+ **业务组**（`/plans`、`/parts`、`/procurement`、`/training`：维护计划 / 备件商城+购物车 / 采购结算 / 考核认证，四个独立页面，与问答零耦合；已含设备筛选、空车入口、人工确认二次确认、已结算隐藏结算按钮、认证记录分页） | `frontend/src/views/`、`frontend/src/api/` |
| 部署 | Dockerfile ×2、nginx（已关 SSE 缓冲）、docker-compose（backend + frontend + mysql） | `deploy/`、`docker-compose.yml` |
| 数据 | 语料 5 篇、黄金集 45 条文本 + 5 条图片、备件台账、工单示例、术语表 | `data/`、`backend/config/` |
| 脚本 | 入库 / 建索引 / 看切片 / 评测（文本 + 图片）/ 黄金集自检 / 演示走查 / 并发压测 / 模型准备 | `scripts/` |
| 测试 | **139 passed**：链路、检索、引用校验、SSE、多模态、MCP、维护计划 / 备件商城与购物车 / 采购结算 / 考核认证、**业务与问答的独立性** | `tests/` |

---

## 2. 功能完成情况（对开发文档 §3.1）

| 编号 | 功能 | 状态 | 关键证据 |
| --- | --- | --- | --- |
| FR-01 | 知识问答（多轮）+ SSE 流式 | ✅ | 事件序列 `meta→token*→citations→done`；追问「你刚才说的第一步是什么？」能正确答出上文结论 |
| FR-02 | 多模态提问（图片 + 文字） | ✅ | 图片评测 **5/5**；识别失败按设计拒答并提示改用文字；识别结果作为证据入上下文 |
| FR-03 | 跨源检索（手册 + 工单 + 台账） | ✅ | MCP `ticket_search` 实测返回工单 TK-2026-0512 的根因/处置/用料；引用按 `source_type` 区分来源 |
| FR-04 | 答案溯源 | ✅ | 引用覆盖率 **100%**、真实性 **100%**（270 项、0 伪造）；引用含文档名/版本/章节/页码/切片 ID，图片类带原图 URL |
| FR-05 | 置信度标注与拒答 | ✅ | 三级标注 + 置信度公式；负样本拒答 **100%**（5/5）；拒答时置信度压到低档并给出可读原因 |
| FR-06 | 会话与历史 | ✅ | 会话列表 / 历史消息含引用；**服务重启后会话与引用完整保留** |
| FR-07 | 反馈记录 | ✅ | 四态反馈落库（`done` 事件带 `qa_id`，前端可直接反馈）；驳回案例进 `data/eval/badcases.md` |
| FR-08 | 知识入库与索引重建 | ✅ | 上传 → 异步 job（进度可查）；一键重建「5 篇 / 32 切片」**19.6s**（验收 ≤5min）；元数据完整率 100% |
| FR-09 | 培训与认证 | ✅ **本期补齐考核认证后端** | 问答侧 `mode=training` 分层讲解 + 高频问题记录；**新增**出题 / 判分 / 发证 6 个接口（每题带依据、不自动发证、证书标注内部授权） |
| FR-10 | 备件查询与订购引导 | ✅ **本期补齐商城与采购后端** | 问答侧工具路径（库存 / 库位 / 到货 / 替代件带依据）；**新增**商城 2 + 购物车 3 + 采购结算 9 个接口，状态机 `draft→submitted→approved→received` + 结算台账；**仍不对供应商发起真实下单** |
| FR-11 | 权限分级（检索即鉴权） | ⬜ 未实现 | `user_role` 已透传到检索层但未参与过滤；需先定义权限分级表（文档列为后续批次） |

新增三块业务（原开发文档 §1.3 列为「不做 / 后续批次」，本期按要求补齐后端）：

| 功能 | 端点数 | 服务层 | 数据表 | 零幻觉落法 |
| --- | --- | --- | --- | --- |
| 维护计划生成 | 4 | `backend/services/plan_service.py` | `maintenance_plan` | 周期值只从**检索到的原文**正则抽取；每条带 `evidence`；抽不到的进 `uncovered`（含原因），不生成计划项 |
| 备件商城与采购结算 | 14 | `backend/services/parts_service.py` | `part_order`、`part_settlement` | 替代件必须有台账 `basis`，否则 `400 SUBSTITUTE_BASIS_REQUIRED`；价格只做台账透传，不估算；购物车 = 草稿单，提交后明细冻结、空单不可提交 |
| 考核认证 | 6 | `backend/services/training_service.py` | `training_quiz`、`training_attempt`、`certification` | 出题前先检索、每题带依据；LLM 题目若引用证据外的数字则丢弃；一题不剩则 `422`；交卷 `certification_id` 恒为 `null` |

后端已端到端验证（详见 `功能完成度清单.md` §8），前端页面交接材料见 `前端对接说明（三块新业务）.md`。

---

## 3. 实测指标（真实 LLM + 真实 VLM）

| 编号 | 指标 | 目标 | 实测 | 判定 |
| --- | --- | --- | --- | --- |
| G1 | Top-5 检索命中率 | ≥ 80% | **100%** | 达标 |
| G2 | 引用覆盖率 | **100%（不可降级）** | **100%** | 达标 |
| G2 | 引用真实性 | ≥ 95% | **100%**（270 项、伪造 0） | 达标 |
| G3 | 要点命中率（严格口径 / 宽松口径） | ≥ 80% | **87.6%** / **100%**（185/185 要点；多次复跑 84%~90%） | 达标 |
| G3 | 无依据拒答率 | ≥ 90% | **100%**（5/5） | 达标 |
| G4 | 图片提取与作答 | ≥ 85% | **5/5 通过** | 达标 |
| G5 | 首 Token | ≤ 8s（已按 §8.4 放宽） | 与整体耗时同界（先过 verify 再出字） | 达标 |
| G5 | 文字 P95 / 带外部源 P95 | ≤ 8s / ≤ 15s | 空闲环境 **3.9s / 4.3s**；同机并行跑演示与子任务时 13.7s / 19.1s（最差 24.0s / 23.3s） | 达标（**受机器负载影响极大**，验收请在空闲机器上跑） |
| — | 误拒正样本 | 0 | **0 / 40** | 达标 |
| — | MRR / NDCG@5 | — | 0.9750 / 0.9785 | — |
| — | 单测 | — | **91 passed** | 达标 |

> **G3 的两种口径都要看**：宽松口径（要点出现在答案或所引原文中）为 100%，严格口径（只看答案正文）
> 为 89.7%（宽松口径 100%）。未配置 LLM 时走抽句式降级路径，严格口径只有 44.7% —— 因此**必须配 `DEEPSEEK_API_KEY` 才有交付意义**。

---

## 4. 环境与数据资产

### 4.1 随代码分发（已入库）

| 数据 | 位置 | 说明 |
| --- | --- | --- |
| 语料 | `data/raw/*.md` | 5 篇（刻蚀手册 / CVD 规范 / 备件目录 / 安全标准 / 清洗指南）；**入库只扫这个目录** |
| 黄金集 | `data/eval/golden_qa.jsonl`（45 条）、`golden_image.jsonl`（5 条） | 含 5 条负样本与 5 条图片样例 |
| badcase | `data/eval/badcases.md` | 驳回案例记录表 |
| 术语表 | `backend/config/glossary.yaml` | 术语归一 + 型号别名 |
| 备件台账 | `backend/config/parts_inventory.yaml` | 5 条示例（接真实 ERP 时整体替换） |
| 工单 | `backend/config/tickets.yaml` | 6 条示例（MCP 工单源） |
| 评测产物 | `data/eval/report_*.json` 等 | 报告/压测/演示记录（需 `git add -f`，默认被忽略） |

### 4.2 运行时生成（`.gitignore` 排除，每个环境都要准备）

| 数据 | 位置 | 生成方式 | 缺失后果 |
| --- | --- | --- | --- |
| 本地向量模型 | `backend/data/models/hub/…bge-large-zh-v1.5`（2.6G） | `scripts/prepare_model.py` | `EMBEDDING_PROVIDER=local` **直接报错** |
| 本地精排模型 | `backend/data/models/bge-reranker-v2-m3`（2.3G） | 同上 `--with-reranker` | 降级为词法精排（MRR 0.975 → ~0.94） |
| 向量索引 | `backend/data/chroma/` | `scripts/build_index.py` | 检索为空 → 全部拒答 |
| 会话检查点 | `backend/data/sqlite/checkpoints.sqlite` | 首次问答自动建 | 多轮记忆丢失 |
| 上传图片 / 抽图 | `backend/data/uploads/`、`images/` | 上传接口 / 入库时写入 | 引用原图 404 |
| 图片评测 fixture | `data/eval/fixtures/*.png`（5 张） | `scripts/make_image_fixtures.py` | 图片评测无法执行 |

### 4.3 外部服务与配置

| 项 | 必需性 | 未配置时 |
| --- | --- | --- |
| MySQL（6 张表，启动自动建） | 交付推荐 | 回落 SQLite（单机可用） |
| `DEEPSEEK_API_KEY` | **强烈建议** | 生成走抽句式降级，G3 严格口径 44.7% |
| `DASHSCOPE_API_KEY` | 图片功能需要 | 图片提问不可用（按设计拒答并提示改用文字） |
| `fastmcp` / `langchain-mcp-adapters` / `mcp` | **必需（已锁定版本）** | 工单 MCP 不可用（健康检查报 `mcp: false`，主链路不受影响） |

---

## 5. 部署与验收命令

```bash
# 1) 依赖
make install                    # venv + pip install -r requirements.txt
make install-web                # 前端依赖（首次）

# 2) 密钥
cp .env.example .env            # 填 DEEPSEEK_API_KEY / DASHSCOPE_API_KEY / DATABASE_URL

# 3) 模型（约 4.6G）与索引
.venv/bin/python scripts/prepare_model.py --check
.venv/bin/python scripts/prepare_model.py --download --source modelscope --with-reranker
.venv/bin/python scripts/build_index.py           # 5 篇 / 32 切片（约 20s）

# 4) 校验
make test                                        # 91 passed
.venv/bin/python scripts/check_golden.py         # 黄金集引用真实性自检
.venv/bin/python scripts/make_image_fixtures.py --check
curl -s localhost:8000/api/health                # 7 个组件应全 ok

# 5) 起服务与验收
make dev && make dev-web                         # 后端 8000 / 前端 5173
.venv/bin/python scripts/eval.py                 # 文本类 45 条
.venv/bin/python scripts/eval_image.py           # 图片类 5 条
.venv/bin/python scripts/demo_walkthrough.py     # 三个演示场景走查
```

> **一键起（容器）**：`docker compose up -d`；生产 nginx 配置见 `deploy/nginx.conf`
> （`proxy_buffering off` + `X-Accel-Buffering: no`，否则首 Token 退化）。

---

## 6. 架构与关键设计

```
接入层   Vue3（SSE via fetch+getReader / 引用卡片 / 置信度标签 / 反馈）
   │  REST + SSE（/api/*，11 个接口）
编排层   FastAPI(async) + LangGraph 单图 9 节点 + 条件边
   │      ingest_image → rewrite → retrieve → rerank → build_context
   │                            └─[证据是否足够?]─> augment ─┐
   │                                        └─────> generate → verify → respond
检索层   BM25(jieba) + 向量(bge-large-zh-v1.5) + RRF + Cross-Encoder(bge-reranker-v2-m3)
数据层   语料（手册）＋ 工单（MCP stdio）＋ 备件台账（YAML）＋ MySQL / 向量库 / SqliteSaver
```

| 机制 | 说明 |
| --- | --- |
| **建库与查询同实例** | 索引清单记录 provider/维度/签名，不匹配直接报错；重建走 `store.rebuild_store()`（先清空再建，不被签名挡住） |
| **零幻觉四道关** | ① 引用由代码分配 ② 引用真实性校验（伪造编号一律拒答） ③ 引用覆盖率 ④ 证据不足/分数不足即拒答 |
| **引用归属** | LLM 漏引用的句子由 `enforce_citations()` 按文本重合度归属到上下文块，**归属不上的句子省略** → 覆盖率 100% 是代码保证的 |
| **图片即证据** | 识别结果作为 `source_type=image` 的证据块入上下文（参数表/铭牌类问题的答案就在图里） |
| **多轮追问补全** | 含指代词或过短的追问会把「最近一个自足提问」补进检索式；连续追问回溯跳过中间指代句 |
| **降级不中断** | 模型 / MCP / 数据库任一不可用都不阻止启动，`GET /api/health` 如实报 `degraded` 及原因 |
| **fail-closed** | `status` 初值 `NOT_COVERED`；有材料却无引用的答案判未覆盖 |
| **token 缓冲** | token 在 `verify` 定稿后才发（拒答会改写答案，避免"先给步骤再拒答"） |

---

## 6.1 三块业务与问答的独立性（项目方要求）

维护计划 / 备件商城与采购 / 考核认证这三块业务**不与问答绑定**，页面与链路都是独立的：

| 层 | 落地方式 |
| --- | --- |
| 接口 | `/api/plans`、`/api/parts`、`/api/training` 三个独立前缀，统一 JSON（非 SSE），不需要会话或 `qa_id` |
| 后端代码 | 问答链路不 import 三块业务，三块业务也不 import 问答链路；由 `tests/test_api/test_module_independence.py` 用 AST 检查 import 强制 |
| 前端页面 | 独立路由 `/plans`、`/parts`、`/procurement`、`/training`，顶栏分「问答组 / 业务组」两组；业务页不引用 `@/api/chat`、`@/utils/sse`、`@/stores/*`；构建产物各自独立 chunk，不含 `event-stream` / `chat` 痕迹 |
| 降级隔离 | 清空 `DEEPSEEK_API_KEY` / `DASHSCOPE_API_KEY` 启动（`llm`/`vlm` 降级）时，三块业务仍全部可用（商城纯台账；计划 8 条带依据；考核走 `extractive-fallback` 出题判分） |

> 说明：问答页里的「备件问答」与 `mode=training` 分层讲解属于**问答能力**；
> 下单、采购结算、出题发证这些**业务操作**只在独立页面里发生。

---

## 7. 已知限制与遗留项

| # | 项 | 影响 | 建议 |
| --- | --- | --- | --- |
| 1 | **并发不提升吞吐**（n=20 → 190s，≈0.11 req/s） | 不能支撑 NFR-02 的 20 并发 | 外部查询与精排并行（省 ~4s）＋ GPU 部署；或将 `rerank_max_candidates` 12→8 |
| 2 | 首 Token 与整体耗时同界（4~19s 视负载） | 体感偏慢 | 已按开发文档 §8.4 放宽到 ≤8s；真流式需先扩 SSE 契约 |
| 3 | 检索层不做权限过滤（FR-11） | 检索即可见全部语料 | 先定义权限分级表，再在 `build_filters` 落地 |
| 4 | 阈值仅在 50 条样本上标定 | 语料规模变化后可能误判 | 用真实语料重标 `setting.py` 三个阈值（不改代码） |
| 5 | 图片知识侧不入库（`kb_image` 0 行） | 图纸/参数表截图**不能被检索到**（作为提问图片已可用） | 接 VLM caption 后写表 |
| 6 | 工单检索是关键词重合 | 无词重合即返回空 | 需要时改向量召回 |
| 7 | docx/pptx/xlsx 不支持 | 上传返回 415 | **刻意为之**（避免抓不到页码的切片），接 MarkItDown 后支持 |
| 8 | 会话删除 / 重命名未实现 | 历史页无法管理会话 | 接口文档 P2 档已定义，后端加两个薄接口 |
| 9 | 三块新业务的前端仍有少量待打磨项 | 四个独立页面已可用并完成首轮打磨（设备筛选、空车入口、人工确认二次确认、已结算不再给假按钮、认证记录分页）；剩余项（申请人建单后不可改、漏选判断题按错误提交、结算方式无枚举等）已逐条列在 `接手任务清单.md` §2.2b | 交接手人按 §2.2b 逐条判断，需要后端配合的在对应条目里注明了要加什么 |
| 10 | 采购审批无身份校验 | 谁能点「人工确认」由前端区分，后端不校验角色 | 与 FR-11 同一根因（缺权限分级表）；补齐后在 `approve` 加角色校验 |
| 11 | 收货不动库存 | `receive` 只登记「已到货」，不扣减 `stock` | 真实入库需接 ERP；当前库存是台账读数，仅用于展示与提醒 |

---

## 8. 运维要点

1. **密钥**：只放 `.env`（已被 `.gitignore` 忽略，日志有 `SecretMaskingFilter` 脱敏）。
   ⚠️ **本次交付过程中两个密钥曾在聊天记录中出现，交付前请轮换 DeepSeek 与 DashScope 的 key**。
2. **模型目录**：`HF_HOME` 指向 `backend/data/models`，容器/换机只需同步这一个目录；
   不要依赖用户家目录缓存（换用户会找不到模型）。
3. **重建索引**：换 embedding 模型后必须重建（`scripts/build_index.py`），否则查询会被签名校验拦住。
4. **日志**：默认 `log_level=INFO`（与 `debug` 解耦）。`DEBUG=true` 会让 LangGraph 把完整检查点打进日志。
5. **数据备份**：业务库（MySQL 6 表）＋ `backend/data/sqlite/checkpoints.sqlite`（会话）＋ `backend/data/chroma`（索引，可重建）。
6. **健康检查**：`GET /api/health` 是唯一权威的运行状态入口（7 组件 + 预热耗时 + 向量库/精排提供方）。

---

## 9. 交付验收清单

- [x] 端到端问答（SSE 流式、多轮追问、引用完整）可演示
- [x] 图片提问（报警截图 / 参数表 / 备件标签）可演示；识别失败按设计拒答
- [x] 跨源检索（手册 + 工单 + 台账）可达
- [x] 拒答场景可演示（问知识库没有的型号 → 明确未覆盖 + 材料清单）
- [x] 知识入库与一键重建索引可用（≤5 分钟）
- [x] 会话与历史（含重启持久化）、反馈落库
- [x] 培训分层讲解、高频问题查询
- [x] 91 项单测通过；文本/图片两份评测报告产出
- [x] 部署产物齐备（Dockerfile ×2 / nginx / docker-compose）
- [ ] **两个密钥轮换**（交付前必做）
- [ ] 交付分支合入 `main`（PR 待合并）
- [ ] 在交付环境重跑两份评测并归档（`git add -f`）

---

## 10. 变更记录

| 日期 | 说明 |
| --- | --- |
| 2026-09-20 | 首版：汇总最终交付内容、FR 完成情况、实测指标、数据资产、部署与验收命令、架构与关键机制、已知限制与遗留项、运维要点与交付验收清单 |
| 2026-09-20（追加） | 按项目方要求补齐三块业务后端（维护计划生成 / 备件商城与采购结算 / 考核认证，共 21 个接口、6 张表、34 个单测）：更新交付一览（接口 32、单测 125）、FR-09 / FR-10 状态、新增业务表与遗留项 9~11 |
