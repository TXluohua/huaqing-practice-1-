# 后端二人分工与协作规范

> **项目**：半导体设备维护知识库智能问答系统
> **范围**：**仅后端**。前端由第三人负责，只通过 HTTP / SSE 契约对接，见 §7
> **基线**：分支 `feature-txluohua-dev`，提交 `0a20a36`（与 `origin/main` 一致）
> **依据**：《半导体设备维护知识库智能问答系统_开发文档（初稿）(2).md》§4 / §6 / §7、`接口文档.md`、`开发进度与交付说明.md`
> **读者**：后端两人（下文称 **甲** / **乙**），以及需要了解接缝的第三人

---

## 0. 开工前先对齐事实

### 0.1 仓库真实状态（写本文档时逐文件核对）

| 项 | 旧文档说法 | 仓库实际（`0a20a36`） |
| --- | --- | --- |
| LangGraph 骨架 | 《开发进度与交付说明》§2.1 | ✅ 一致：`state.py`(251 行) / `graph.py`(152) / `agent_factory.py`(211) / `memory.py`(204) / `nodes/__init__.py`(110) / `tests/test_agents/test_graph.py` / `scripts/ask.py` 均已实现 |
| HTTP 接入层 | 同文档 §8 称 `routers/`、`schemas.py`、`setting.py` **仍是 0 字节** | ❌ **该说法已过时**：`routers/__init__.py`(243) / `routers/chat.py`(267) / `routers/admin.py`(124) / `schemas.py`(249) / `setting.py`(128) **已实现并冻结**（提交 `0a20a36`） |
| service 层与入口 | —— | ⬜ **仍为 0 字节**：`services/chat_service.py`、`services/kb_service.py`、`main.py`、`dependency.py`、`db.py` |
| RAG / 节点 / 工具 | —— | ⬜ **一行未动**：`rag/*`、`nodes/{retrieval,generation,multimodal}.py`、`prompts/*.md`、`tools/kb_tools.py`、`config/glossary.yaml` |
| 虚拟环境 | 交付说明称已建 `.venv` | ⚠️ **仓库内不存在 `.venv`**（被 `.gitignore` 排除），需各自 `make install` 重建 |
| Git | 交付说明称「未对远程做任何操作」 | ⚠️ 远程 `origin = git@github.com:TXluohua/huaqing-practice-1-.git` 已启用：前端分支经 **PR #1 合入 `main`**（`f0a7f34`）。仓库**没有 `develop`**，本项目统一走「feature 分支 → PR → main」（见 §8.1） |

**结论**：`接口文档.md`「平台侧现在就能开工」这句话已经**开工过一半**（HTTP 层完成），平台侧剩余工作是 **service 层 + 入口 + 落库 + 上传 + MCP + 部署**；RAG 侧**完全从零开始**。本分工按此现状切。

### 0.2 今天就要做的三件事

1. **把本地 `main` 与远程对齐**（`git fetch origin --prune && git merge --ff-only origin/main`）——前端已经进 `main` 了；此后谁都不许直推 `main`，一律走 PR。
2. **拍板一个契约缺口**：`schemas.py` 的 `ChatRequest` 已有 `mode: Literal["qa","training"]`，但 `state.py` 的 `AgentState` **没有对应字段**（`接口文档.md` §8 缺口①）。甲要把 `mode` 传进图，字段名必须先定 —— 属契约级（P0），见 §6。
3. **各自跑通自测入口**：甲 `make install` + 起 `main.py` 看 `/docs`；乙 `make install` + `make test` + `make ask`。见 §5.3。

---

## 1. 范围与前提

| 项 | 约定 |
| --- | --- |
| 纳入本作业流 | `backend/` 全部、`scripts/`、`tests/`、`data/eval/`、`deploy/Dockerfile.backend`、`deploy/nginx.conf`、`docker-compose.yml`、`.env.example`、`Makefile` |
| 不纳入 | `frontend/` 全部、`deploy/Dockerfile.frontend` —— 归第三人 |
| 交付底线（任何情况不砍） | 强制引用 + 引用校验 + 无依据拒答（开发文档 §7.4 末行）；`verify` 节点不得绕过；`status` 初值 `NOT_COVERED`（fail-closed）不得改为 `OK` |
| 工程铁律（§6.4） | 依赖单向 `routers → services → agents/tools/rag → config`，禁止反向 import；全链路异步；参数与提示词不硬编码；接口先行 |

---

## 2. 角色定义

| | **甲（平台侧 / Platform）** | **乙（检索生成侧 / RAG & Quality）** |
| --- | --- | --- |
| 一句话职责 | 把图**接进 HTTP**：入口、service、SSE、会话落库、上传、MCP、部署 | 把**图里面**做准：切片、索引、检索、精排、上下文、生成、校验、评测 |
| 对外身份 | **前端第三人的唯一对接人**（提供 `/docs`、契约答疑、联调排障） | 不直接对前端 |
| 交付判据 | 接口可调通；首 Token ≤ 2s；多轮追问生效；重启后历史仍在；一键起服务 | Top-5 命中 ≥ 80%；引用覆盖率 100%；拒答 ≥ 90%；图片提取 ≥ 85% |
| 主战场文件 | `main.py`、`dependency.py`、`db.py`、`services/*`、`utils/*`、`mcp_*.py`、`nodes/multimodal.py` | `rag/*`、`nodes/retrieval.py`、`nodes/generation.py`、`prompts/*`、`tools/kb_tools.py`、`config/`、`scripts/`、`data/eval/` |
| 参考工时（开发文档 §7.5） | 多模态 1.0 + 服务化约 1.0 + 部署/交付约 0.5 ≈ **2.5 人天** | RAG 链路 4.5 + 评测 1.0 ≈ **5.5 人天** |

> **建议对应**（**以二人当面确认为准**）：仓库现有两个提交身份 `tutu <2178883423@qq.com>` 与 `TXluohua <3012228397@qq.com>`。HTTP 层（`0a20a36`）由 `tutu` 提交，**建议 甲 = tutu**（顺势做完 service），**乙 = TXluohua**（从零起 RAG 线）。若实际相反，只需互换两列负责人，文件清单与规则全部不变。

---

## 3. 文件级所有权：一人一文件，禁止交叉编辑

核心原则（`开发进度与交付说明.md` §9）：

> **双方的接口是 `state.py`，不是彼此的代码。**

### 3.1 冻结契约区（双方只读；改动一律走 §6 流程）

| 文件 | 为什么冻结 | 谁最常消费 |
| --- | --- | --- |
| `backend/agents/state.py` | 契约级：`AgentState` / `Evidence` / `Citation` / `ImageExtraction` | 双方 |
| `backend/agents/graph.py` | 主拓扑；含条件边 `TODO` | 乙改造（**唯一允许的改动者**），甲 approve |
| `backend/agents/agent_factory.py`、`backend/memory.py` | 甲调 `astream()` / 会话映射；乙的节点在其上运行 | 双方 |
| `backend/routers/**`、`backend/schemas.py` | HTTP + 请求响应模型已实现，是**前端契约的事实来源** | 甲执行、第三人消费 |
| `backend/setting.py` | 参数集中地：**只追加字段，不改已有字段名与类型** | 双方 |

### 3.2 甲的独占文件

| 文件 | 现状 | 交付内容 |
| --- | --- | --- |
| `backend/main.py` | 0 B | FastAPI app + `lifespan` 预热（agent / MCP / 模型）+ `register_error_handlers()` + `register_routers()` |
| `backend/dependency.py` | 0 B | `settings` / `agent` / `service` 单例依赖 |
| `backend/db.py` | 0 B | 表模型 + 会话：会话 / 消息 / 问答记录 / 反馈 / 异步任务 |
| `backend/services/chat_service.py` | 0 B | 实现 §4.2 冻结签名；**SSE 事件映射在这里** |
| `backend/services/kb_service.py` | 0 B | 实现 §4.2 冻结签名；入库 / 重建 / 任务 / 切片 |
| `backend/utils/image.py`、`backend/utils/text.py` | 0 B | 压缩/格式/体积校验；分句/脱敏/日志 |
| `backend/mcp_client.py`、`backend/mcp_servers/ticket_server.py` | 0 B | 工单检索 MCP（stdio）+ **加载失败降级为空工具，不中断主链路** |
| `backend/agents/nodes/multimodal.py` | 0 B | `ingest_image` + `augment`（受限 agent） |
| `backend/agents/prompts/vision.md` | 0 B | 图片识别提示词（**只转写不判断**） |
| `backend/tools/__init__.py` | 0 B | 本地工具 + MCP 工具聚合、白名单组装、降级兜底（见 §3.4） |
| `deploy/Dockerfile.backend`、`deploy/nginx.conf`、`docker-compose.yml`、`.env.example` | 0 B | 部署；**nginx 必须对 SSE 关 `proxy_buffering`** |
| `tests/test_api/test_sse.py`、`tests/test_multimodal/test_vision.py`、`tests/test_mcp/test_ticket_server.py` | 0 B | 含「流式中途出错发 `error` 事件」「识别失败必须拒答」「工具报错主链路仍可用」用例 |
| `tests/conftest.py` | 不存在 | 建议新建一次，把项目根塞进 `sys.path`，免去每个测试文件重复样板（见 §8.5） |
| `Makefile` | 已存在 | 追加 `dev` / `eval` / `up` 目标（`test` / `ask` 已有） |

### 3.3 乙的独占文件

| 文件 | 现状 | 交付内容 |
| --- | --- | --- |
| `backend/rag/ingest.py` | 0 B | 解析 + 抽图 + 清洗 + 切片（写入 `page`/`version` 等元数据，完整率 100%） |
| `backend/rag/store.py` | 0 B | embedding + 向量库，**唯一出口**（换向量库只改此文件） |
| `backend/rag/retriever.py` | 0 B | BM25(jieba) + 向量 + RRF + 精排 + 上下文构建 |
| `backend/rag/citation.py` | 0 B | 引用生成与校验（编号 `[n]` **由代码分配，禁止模型编造**） |
| `backend/agents/nodes/retrieval.py` | 0 B | `rewrite` / `retrieve` / `rerank` / `build_context` |
| `backend/agents/nodes/generation.py` | 0 B | `generate` + `verify`（引用校验、置信度三级、拒答判定）—— **质量底线，禁止绕过** |
| `backend/agents/prompts/system.md`、`generate.md`、`rewrite.md` | 0 B | 零幻觉总则 / 生成与输出格式 / 改写 |
| `backend/tools/kb_tools.py` | 0 B | 本地工具实现（签名见 §4.3） |
| `backend/config/glossary.yaml` | 0 B | 术语归一表（刻蚀机/Etcher、真空度/chamber pressure） |
| `backend/agents/graph.py` | 已实现 | **一次性改造**：按已标 `TODO` 用 `add_conditional_edges` 接入「证据是否足够 → augment」分支；PR 需甲 approve |
| `scripts/ingest.py`、`build_index.py`、`inspect_chunks.py`、`eval.py` | 0 B | 一键入库 / 重建索引 / 看切片 / 跑黄金集出指标 |
| `data/eval/golden_qa.jsonl`、`golden_image.jsonl`、`badcases.md` | 0 B | 黄金集 v0（20 条含 3 条图片 + 2 条负样本）→ D8 扩到 50 条 |
| `tests/test_rag/{test_ingest,test_retriever,test_citation}.py`、`tests/test_agents/test_verify.py` | 0 B | 切片元数据完整性、检索、引用校验、拒答用例 |

### 3.4 共享冲突热点（4 处，规则写死）

| 文件 | 归属 | 硬规则 |
| --- | --- | --- |
| `requirements.txt` | 共享 | **按依赖分组段内追加**；严禁整文件排序/重排（排序会让对方每次 rebase 都冲突） |
| `backend/setting.py` | 共享 | 追加字段 + 默认值；禁止改已有字段名/类型；密钥只进 `.env` |
| `.env.example` | 甲维护 | 新增键由乙提需求、甲落；与 `setting.py` 字段同名同义 |
| `backend/data/models/` | 乙（RAG 侧） | **本地 HF 模型目录**（HF_HOME 指向此处，约 4.6 GB，已被 `.gitignore` 排除）：向量化与精排都跑本地模型，**不调用云端 API**。准备/自检命令：`scripts/prepare_model.py --check / --copy-from-cache / --download --source modelscope`。模型换了必须重建索引（清单签名会直接报错） |
| `backend/agents/nodes/__init__.py` | 共享（**约定都不用改**） | 各节点在**自己模块底部**调 `register_node(NodeName.X, fn)`（`开发进度与交付说明.md` §7 已给写法），**不修改注册表文件本身** |
| `backend/tools/__init__.py` | 甲 | 聚合与降级归平台侧；乙只在 `kb_tools.py` 定义函数供甲 import |
| `Makefile` | 甲 | 只追加 target，不改已有 target 语义 |
| `接口文档.md` | 甲 | 甲是前端对接人，因此由甲维护；乙改自己相关章节需在 PR 描述里说明 |

### 3.5 第三人的文件

`frontend/**`、`deploy/Dockerfile.frontend` —— 不在本分工作业流内，不得由甲乙顺手改动；需要契约变化时走 §7。

---

## 4. 接缝清单：只有这四处需要"对齐"

### 4.1 缝 1 —— `AgentState`（`backend/agents/state.py`）

节点只返回自己产出的字段（增量更新）：

| 节点 | 产出字段 | 归属 |
| --- | --- | --- |
| `ingest_image` | `image_result` | 甲 |
| `rewrite` | `queries` | 乙 |
| `retrieve` | `candidates` | 乙 |
| `rerank` | `ranked` / `top_score` | 乙 |
| `build_context` | `context` / `context_blocks` | 乙 |
| `augment` | `evidence_sufficient` / `augment_rounds` / `tool_calls` | 甲 |
| `generate` | `answer` / `answer_structured` | 乙 |
| `verify` | `citations` / `confidence` / `confidence_label` / `status` / `uncertain` | 乙 |
| `respond` | `response` | 甲（SSE 收尾） |
| 横切 | `trace_id` / `timings`(reducer 合并) / `errors`(reducer 追加) | 双方只追加 |

规定：**字段只追加，不改名、不改类型、不删**。`status` 初值 `NOT_COVERED` 是 fail-closed 设计（忘记赋值即拒答，而非放行），任何人不得改回 `OK`。

> ⚠️ 上表 `evidence_sufficient` 记在 `augment` 名下，但条件边「证据是否足够?」在 `augment` **之前**就要判定（见 §6.3 第 4 行）—— 该字段的**产出时序**需开工第一天定死。

### 4.2 缝 2 —— service 契约（**已冻结**在 `routers/__init__.py` 与两个路由模块的 docstring 中）

甲要实现的全部方法（均 `async`，实现契约以**调用点**为准）：

| 模块 | 签名 | 返回 / 语义 |
| --- | --- | --- |
| `chat_service` | `stream_answer(payload: ChatRequest, *, user_id, user_role, trace_id)` | `AsyncIterator[str]`，产出**已格式化好的 SSE 文本块**；事件顺序 `meta → image? → token* → citations → done`；流已开始后出错 → 发 `error` 事件（不再改 HTTP 状态码） |
| `chat_service` | `list_sessions(*, limit, offset, keyword)` | `tuple[list[SessionItem], int]` |
| `chat_service` | `list_messages(session_id, *, limit, offset, with_citations)` | `tuple[SessionRef, list[MessageItem], int] \| None`，`None` → 路由抛 404 `SESSION_NOT_FOUND`；`SessionRef.thread_id` 被读取 |
| `chat_service` | `add_feedback(*, qa_id, type, comment, corrected_answer, trace_id)` | `FeedbackRecord \| None`（需 `feedback_id` / `created_at`），`None` → 404 `QA_NOT_FOUND` |
| `kb_service` | `save_image_upload(file, *, session_id, trace_id)` | 返回值须能 `ImageUploadResponse.model_validate`（含 `image_id`/`url`/`width`/`height`/`mime`/`size_bytes`） |
| `kb_service` | `create_document(file, *, doc_title, version, category, uploader, trace_id)` | `DocumentRef(doc_id, job_id, status)`，立即返回 202 |
| `kb_service` | `rebuild_index(*, scope, force, trace_id)` | `str`（`job_id`），返回 202 |
| `kb_service` | `get_job(job_id)` | `JobStatusResponse \| None`，`None` → 404 `JOB_NOT_FOUND` |
| `kb_service` | `list_chunks(*, doc_id, q, page, limit, offset, trace_id)` | `tuple[list[ChunkItem], int]` |

两条已内建的保护，双方都不要破坏：

- **未实现即 503**：`_load_service()` 会校验上表方法存在，缺失时抛 `ApiError(503, "SERVICE_NOT_READY")` —— 路由层不会因为 service 没写完而起不来，**前端看到的是明确错误码而不是启动失败**。
- **可替换**：`app.dependency_overrides[get_chat_service] = lambda: FakeChatService()` 是官方留的替身口子（`routers/__init__.py` 注释已写明），联调期用它。

> 实现细节提醒：`chat.py` 的 docstring 里 `add_feedback` 漏写了 `trace_id` 参数，实际调用**带** `trace_id`；`save_image_upload` 写在 `chat.py` 的调用点而 `admin.py` 的契约 docstring 未列它。以调用点为准，两处 docstring 由甲顺手订正。

### 4.3 缝 3 —— 工具签名（乙产出、甲消费；**待冻结**）

建议按开发文档 §4.4.1 落成以下签名（全部 `async`，类 MCP schema；**冻结前甲不得在 `multimodal.py` / `augment` 里写死调用**）：

| 工具 | 建议签名 | 实现方 | 主消费方 |
| --- | --- | --- | --- |
| `kb_search` | `(query: str, filters: dict \| None, k: int)` | 乙（本地函数） | 乙 `retrieve` 节点固定调用 |
| `kb_search_expand` | `(query: str, relax: bool)` | 乙（`@tool`） | 甲 `augment`（≤2 轮） |
| `kb_get_chunk` | `(id: str, neighbors: int)` | 乙（`@tool`） | 甲 `augment` / 甲溯源 |
| `kb_stats` | `()` | 乙（`@tool`） | 甲 `stream_answer` 拒答时的建议 |
| `glossary_lookup` | `(term: str)` | 乙（本地函数） | 乙 `rewrite` |
| `vision_extract` | `(image_id: str)` | 甲（本地函数，调阿里云） | 甲 `ingest_image` |
| `ticket_search` | `(symptom: str)` | 甲（**MCP stdio**） | 甲 `augment` |
| `parts_query` | `(part: str, device_model: str \| None)` | 甲（`@tool`，接口按 MCP schema 设计） | 甲 `augment` / 备件类提问 |

规则：**docstring 是工具的唯一说明书**（何时使用、参数含义、返回结构必须写清）；工具总数 ≤ 10；工具报错必须 `try/except` 兜底，不得中断主链路。

### 4.4 缝 4 —— SSE 事件契约（甲实现，第三人消费）

以 `接口文档.md` §6 为准（`meta` / `image` / `token` / `citations` / `done` / `error`），**该表与 `state.py` 逐字段对齐，因此 `astream()` 的产出可直接映射，无需中间转换层**。

规定：
- 三个必测场景：正常带引用、拒答（`NOT_COVERED`，不给操作步骤）、图片识别失败（不允许猜测）。
- 响应头 `X-Accel-Buffering: no` 已在 `chat.py` 实现；`deploy/nginx.conf` 必须同步关 `proxy_buffering`（否则首 Token 退化，NFR-01 ≤2s 不达标）。

---

## 5. 并行开工：谁不用等谁

### 5.1 立即能开工（互不阻塞）

**甲（不需要乙的任何代码）**

1. `make install`（双方前置；仓库当前无 `.venv`）。
2. `main.py` + `dependency.py`：起服务、`/docs` 可访问 → **第三人当场就能看契约**。
3. `chat_service.stream_answer` **骨架版**：把 `astream()` 的 state 增量映射成 SSE 事件；节点未接入时自动输出 `done(status=NOT_COVERED)`、无 `token` —— 事件序列仍然合法，前端可先联调。
4. `utils/image.py` + `save_image_upload`：纯文件处理，等不到任何人。
5. `db.py` 建表 + 会话/反馈接口。

**乙（不需要甲的任何代码）**

1. `rag/ingest.py` + `scripts/ingest.py` + `store.py` → 用 `scripts/inspect_chunks.py` 看切片质量。
2. `retrieval.py` 四个节点 → 用 `make ask Q="..."` 直连图自测。
3. `data/eval/golden_qa.jsonl` v0 + `scripts/eval.py` → 先有指标，才有迭代方向。

> 关键：**乙的自测入口是 `make test` + `scripts/ask.py`，不需要 HTTP 层；甲的自测入口是 `curl` + `/docs`，不需要任何节点。**

### 5.2 联调期的替身与降级

| 场景 | 手段 |
| --- | --- |
| 甲要测路由，乙尚未产出节点 | `agent_factory.astream()` 骨架直连 `START → END`，返回 `NOT_COVERED` —— **这是预期结果，不是故障** |
| 甲要测路由，自己的 service 没写完 | `app.dependency_overrides[get_chat_service]` 注入 Fake；或让路由自然返回 503 `SERVICE_NOT_READY` |
| 乙要验证节点，甲尚未起服务 | `make ask` / `make ask-stream` 直连图 |
| MCP 加载失败 | 降级为空工具列表，主链路继续（R7） |

### 5.3 各自的自测命令（收工前必跑）

| | 命令 | 期望 |
| --- | --- | --- |
| 双方 | `make test` | 全绿（骨架期 7 passed） |
| 乙 | `make ask Q="刻蚀机真空度异常怎么排查？"` / `make ask-stream Q="参数下限是多少"` | 节点接入后：`status=OK`、答案带 `[n]`、引用数 > 0 |
| 乙 | `.venv/bin/python scripts/eval.py`（乙实现） | 输出 G1–G5 指标，写入 `data/eval/badcases.md` |
| 甲 | `.venv/bin/python -m uvicorn backend.main:app --reload --port 8000` + `curl localhost:8000/api/health` | `status=ok` 或 `degraded`，且 `components.graph.ok=true` |
| 甲 | `curl -N -X POST localhost:8000/api/chat/stream -H 'Content-Type: application/json' -d '{"question":"参数下限是多少"}'` | 收到 `meta` … `done` 事件序列 |

---

## 6. 契约变更流程（先文档 → 再测试 → 后实现）

### 6.1 三级定级

| 级别 | 涉及 | 流程 |
| --- | --- | --- |
| **P0 契约级** | `state.py` 字段、`graph.py` 拓扑、`schemas.py`、`routers/**`、SSE 事件、service 方法签名 | 改 `接口文档.md`/`开发进度与交付说明.md` → 改测试 → 改实现 → **PR 需双方 approve** → 群里明示"契约已变" |
| **P1 参数级** | `setting.py` 追加字段、`requirements.txt` 追加依赖、`.env.example` 追加键 | 追加即可，PR 描述里写明，对方无需 approve |
| **P2 私有级** | 节点内部实现、提示词措辞（不改输出结构）、工具内部算法 | 自由改；提示词改动**必须**跑评测 |

### 6.2 冻结窗口

- **D5 起进入联调期**：P0 变更需双方 + 第三人知会（若涉及前端可见行为）。
- **演示/交付前 1 天**：P0 全面冻结，只接受 bugfix。

### 6.3 已知待决的契约项

> **状态更新（2026-09-15，RAG 侧已实现）**：下表前四行已按「乙先落、甲对接」的方式定案，
> 详见 `RAG实现说明.md` §6/§8。`state.py` 的追加字段（`device_model` / `category` /
> `answer_mode` / `user_role`，均带默认值）**属只追加**，未改动任何既有字段。

| 项 | 现状 | 结论 / 待办 |
| --- | --- | --- |
| `mode=training` 进不了图 | `schemas.py: ChatRequest.mode` 已实现 | ✅ 已追加 `AgentState.answer_mode`（默认 `qa`），提示词已分 `qa`/`training` 两套模板；**待甲**在 `stream_answer` 里把 `mode` 传进 `make_initial_state` |
| `X-User-Role` 权限过滤落点 | 请求头已透传到 `stream_answer` | ✅ `state.py` 已追加 `user_role`；检索层 `retrieve` 节点已预留（当前仅记录，待权限分级表 FR-11 落地后在 `filters` 扩展） |
| 备件询价单草稿落点 | `接口文档.md` §8 缺口⑤ 暂定走 chat 工具调用返回 | 待甲：`kb_tools.py` 末尾已留 `parts_query` 签名占位（实现归甲，勿另起一份） |
| `evidence_sufficient` 的产出时序 | 旧文档把它记在 `augment` 名下，但条件边在 `augment` **之前**判定 | ✅ 已定案并实现：`build_context`（乙）给初值 → 条件边据此分流 → `augment`（甲）补检索后回写（≤2 轮）。判定在 `nodes/retrieval.py::should_augment()` |
| **SSE `token` 与 `verify` 改写答案的冲突**（新增，P0） | `verify` 在拒答时会把 `answer` 改写成拒答话术，而 `token` 是增量流；接口文档未定义此情形 | 待双方 + 第三人拍板：**先缓冲到 `verify` 再一次性发 token**（推荐，语义最干净），或先发 token 再用 `done` 纠正 |

---

## 7. 前端第三人的边界（后端怎么做才不返工）

| 项 | 约定 |
| --- | --- |
| 交互面 | **只有 HTTP + SSE**：`/api/*` 与 FastAPI 自动生成的 `/docs`（OpenAPI）。第三人不得依赖后端内部结构 |
| 唯一对接人 | **甲**。契约答疑、联调排障、mock 约定都找甲；第三人不应向乙提接口需求 |
| 契约变更方向 | 先改 `接口文档.md` → 再改 `schemas.py` + 测试 → 最后通知第三人。**禁止"顺手改个字段"** |
| 未实现接口的语义 | 一律 503 `SERVICE_NOT_READY` + 统一错误体（`接口文档.md` §5），**不返回假数据**，前端可据此做占位 |
| 流式 | 前端用 `fetch` + `response.body.getReader()`，**禁用 `EventSource`**；甲保证 `X-Accel-Buffering: no` 与 nginx `proxy_buffering off` |
| 图片 | 两段式：先 `POST /api/upload/image` 拿 `image_id`，再带 `image_ids` 提问；**识别不在上传接口做**（`chat.py` docstring 已澄清） |
| 第三人需要后端提供 | ① `/docs` 可访问（甲 `main.py`）；② 5 个 P0 接口可用或明确 503；③ 一份 SSE 事件清单（`接口文档.md` §6 已具备） |

---

## 8. Git 协作规范

### 8.1 分支模型

`main` ← `feature/*`（**PR 直接进 main**，与团队实际做法一致）

> **与开发文档 §6.4 的差异（已按现状定案）**：开发文档写的是 `main ← develop ← feature/*`，
> 但前端分支是直接以 PR #1 合入 `main` 的（提交 `f0a7f34`），仓库并不存在 `develop`。
> 为避免多一套没人用的分支，**本项目统一为「一人一 feature 分支 → PR → main」**。

```bash
# 一次性：把本地 main 与远程对齐
git fetch origin --prune
git switch main && git merge --ff-only origin/main

# 日常：从最新的 main 切自己的分支
git switch main && git merge --ff-only origin/main
git switch -c feature/platform-sse        # 甲
git switch -c feature/rag-pipeline        # 乙
```

| 规则 | 内容 |
| --- | --- |
| 命名 | `feature/platform-<topic>`（甲）/ `feature/rag-<topic>`（乙）/ `fix/<topic>` |
| 直推禁令 | 任何人不得直接 push `main`；一律走 PR |
| 起步姿势 | 开分支前先 `fetch` + `merge --ff-only origin/main`，避免 PR 里夹带别人的旧提交 |
| PR 粒度 | 每人每天 ≤ 1 个 PR，单 PR ≤ 400 行；节点/接口按块拆分 |
| 评审 | 对方 1 个 approve 即可合并；**P0 契约文件（`state.py` / `graph.py` / `schemas.py` / `routers/**` / `接口文档.md`）需双方 approve** |
| 合并方式 | PR **合入 main**：契约级改动用 **merge commit**（保留提交历史，便于回溯谁改了什么），纯文档/配置可用 **squash merge** |
| 分支更新 | 分支已推送过就用 `git merge main`；**未推送过的本地分支可以用 `git rebase main`**（历史更干净，无需强推） |
| 提交信息 | `feat` / `fix` / `docs` / `test` / `chore` + `(scope): 说明`，scope 用 `platform` / `rag` / `contract`，例：`feat(rag): 接入 BM25+向量 RRF 融合` |
| 禁止事项 | 不擅自升级依赖版本；不引入技术栈外组件；不绕过 `verify` 节点；**不写死返回值让测试变绿** |

### 8.2 PR 描述模板（四行即可）

```
改了什么：（一句话）
动了契约吗：否 / 是 → 接口文档 §X 已更新，已通知 <对方>
怎么验证的：（贴命令与输出摘要）
遗留问题：（无 / 说明）
```

### 8.3 冲突热点的处理

| 文件 | 硬规则 | 冲突了怎么办 |
| --- | --- | --- |
| `requirements.txt` | 按依赖分组**段内追加**，禁止排序/重排 | 保留两边的追加行，跑一次 `pip install -r requirements.txt` 幂等验证 |
| `backend/agents/state.py` | **只追加字段**，不改名不改类型 | 保留两处追加；若同一字段名，当面判定 |
| `backend/setting.py` | 追加字段 + 默认值 | 同上 |
| `backend/agents/nodes/__init__.py` | 双方约定都不改 | 若被改，回退到基线版本 |
| `Makefile` | 只追加 target | 保留两边 target |

先合并的一方为准，另一方 `rebase` 后**必须重跑 `make test`**。

### 8.4 提交节奏

- 每天至少 1 次推送到自己的 feature 分支（防止本地堆积成巨型冲突）。
- 每天收工前把**当天可运行的部分**提 PR 合入 `main`，保持 `main` 始终「提问 → 带引用答案 → 评测记录」可跑（开发文档 §7.3 每日最小闭环）。
- 契约变更当天必须合并，不得留在本地过夜。

### 8.5 测试约定

- 现有 `tests/test_agents/test_graph.py` 在每个测试文件顶部自行把项目根插入 `sys.path`（第 24–26 行）。**建议甲新建 `tests/conftest.py` 做一次**，此后乙无需重复样板（一次基础设施改动，属甲独占）。
- 新增能力必须附黄金集用例；多模态相关变更必须附「识别失败」用例（§6.4）。
- 提示词（`generate.md`）每次改动**必须**跑 `scripts/eval.py`，禁止凭感觉改（开发文档 §7.2 D4）。

---

## 9. 八天并行排期（D1–D8）

| 天 | 主题（开发文档 §7.1） | 甲（平台侧） | 乙（RAG 侧） | 当日集成检查点 |
| --- | --- | --- | --- | --- |
| D1 | 环境与数据 | `make install`；`main.py` + `dependency.py` 起服务；`/docs` 可访问；`stream_answer` 骨架 | 解析 / 清洗 / 切片 / 抽图；`store.py`；黄金集 v0（20 条含 3 图片 2 负样本） | `make test` 绿 + `GET /api/health` 返回 200 |
| D2 | 索引与检索 | `db.py` 建表；`utils/image.py` + `/api/upload/image` 两段式；会话落库 | BM25(jieba) + 向量 + RRF；`build_index.py`；`inspect_chunks.py` | 上传返回 `image_id`；Top-5 命中 ≥ 70%；建索引 ≤ 5min |
| D3 | 检索增强 | `/chat/sessions`、`/chat/sessions/{id}/messages`、`/feedback` 打通；`X-User-Role` 透传到检索层 | 查询改写 + 术语归一 + 元数据过滤 + Cross-Encoder 精排 | 历史接口返回含引用的消息；Top-5 ≥ 80% |
| D4 | **生成与引用（核心日）** | `multimodal.py`（`ingest_image` + `augment`）+ `vision.md`；MCP 降级兜底 | `generate` + `verify` + 引用校验 + 置信度 + 拒答；三个提示词 | **`/api/chat/stream` 出真实 `token` + `citations` 事件**；引用覆盖率 100%，负样本拒答 ≥ 80% |
| D5 | 服务化 | `lifespan` 预热（agent/MCP/模型）；`deploy/nginx.conf` SSE 头；`docker-compose.yml` | `citation.py` 收尾；首 Token 时延与 token 预算调优 | 首 Token ≤ 2s；多轮追问生效；重启后历史仍在；**联调开始，契约冻结** |
| D6 | 前端日 | 作为唯一对接人排障；`/api/admin/*` 可用 | 精排与上下文 `~6000` token 预算调优；图片知识侧入库 | 浏览器端到端可演示（第三人主导，甲乙支持） |
| D7 | 工具与管理页 | `ticket_search` MCP（stdio）+ `parts_query` + 任务进度接口 | 受限 agent 硬边界（≤2 轮 / ≤3 次调用 / 15s 超时 / 3 工具白名单）+ `kb_stats` | 故意让工具报错，主链路仍可用；备件查询与数据源一致 |
| D8 | 评测与交付 | `Dockerfile.backend`；README 一键起；`make up` | 黄金集扩到 50 条；跑分 + badcase 归因；差距说明 | G1–G5 达标或明确说明差距；一键建库 + 一键评测；3 个演示场景通过 |

> 工时说明：开发文档 §7.5 的 **8 人天是含前端的项目总量**。两人并行后，后端侧（RAG 4.5 + 多模态 1.0 + 服务化 ~1.0 + 评测 1.0 ≈ 7.5 人天）在 8 天窗口内有余量 —— **瓶颈不在人力，在契约对齐与评测迭代次数**。因此 §6 的契约流程与 §8.4 的每日合入纪律优先于"多写代码"。

---

## 10. 每日同步与验收

### 10.1 收工三步（每人每天）

1. 跑自测（§5.3），把命令与输出摘要贴进 PR/群。
2. 提交并提 PR 合入 `main`（当日可运行部分）。
3. 更新自己负责的文档：甲改 `接口文档.md`、`README.md`；乙改 `data/eval/badcases.md`、评测说明。

### 10.2 阻塞升级规则

| 情况 | 动作 |
| --- | --- |
| 被对方阻塞 > 半天 | 当面对齐一次；先看 §5.1 —— **绝大多数"被阻塞"其实是没找到自己的独立入口** |
| 被第三方（模型/依赖/硬件）阻塞 | 按开发文档 §7.4 取舍顺序降级，并记录到 `badcases.md` |
| 指标不达标 | **先保引用与拒答**（§7.4 底线），再谈命中率 |

### 10.3 验收清单（附录 B）

---

## 11. 协作风险与对策

| 风险 | 触发信号 | 对策 |
| --- | --- | --- |
| 单方偷改 `state.py` | 对方节点 `KeyError` / 字段名不一致 | P0 流程 + 契约文件双方 approve（§6.1） |
| 两人同时改 `graph.py` | 合并冲突、条件边被覆盖 | `graph.py` 只允许乙改（一次性接入条件边），PR 需甲 approve |
| `requirements.txt` 被排序 | 每次 rebase 都冲突 | 段内追加，禁止排序 |
| 前端倒逼改契约 | PR 里出现未经文档的 `schemas.py` 改动 | 拒绝合并；先改 `接口文档.md` 再改实现（§7） |
| 提示词改了不跑评测 | 引用覆盖率/拒答率下滑但无人察觉 | 改 `generate.md` 必跑 `scripts/eval.py`（§8.5） |
| `status` 初值被改回 `OK` | 无依据答案被放行 | fail-closed 是设计决定，PR 直接拒绝 |
| 两人互等 | 一方说"等对方接口" | 回到 §5.1/§5.2：骨架期双方各有独立入口 |
| 本地无 `.venv` | 新人 clone 后跑不起来 | 各人 `make install`；README 已写明 |
| 契约在联调期变动 | 前端返工 | D5 起契约冻结（§6.2） |

---

## 附录 A：`CODEOWNERS` 模板（放入 `.github/CODEOWNERS`）

把 `<甲>` / `<乙>` 换成 GitHub handle（远程为 `TXluohua/huaqing-practice-1-`）：

```
# 默认：双方
*                       @<甲> @<乙>

# 平台侧（甲）
/backend/main.py        @<甲>
/backend/dependency.py  @<甲>
/backend/db.py          @<甲>
/backend/services/      @<甲>
/backend/routers/       @<甲>
/backend/utils/         @<甲>
/backend/schemas.py     @<甲>
/backend/setting.py     @<甲>
/backend/mcp_client.py  @<甲>
/backend/mcp_servers/   @<甲>
/deploy/                @<甲>
/docker-compose.yml     @<甲>
/接口文档.md            @<甲>

# RAG 侧（乙）
/backend/rag/           @<乙>
/backend/agents/nodes/  @<乙>
/backend/agents/prompts/ @<乙>
/backend/tools/         @<乙>
/backend/config/        @<乙>
/scripts/               @<乙>
/data/eval/             @<乙>

# 契约级：必须双方
/backend/agents/state.py        @<甲> @<乙>
/backend/agents/graph.py        @<甲> @<乙>
/backend/agents/agent_factory.py @<甲> @<乙>
/backend/memory.py              @<甲> @<乙>

# 前端：第三人
/frontend/              @<第三人>
```

> 注：`/backend/tools/` 在 CODEOWNERS 里写乙，但 `tools/__init__.py` 的聚合改动归甲（§3.4）—— 差异由 PR 评审兜住。

---

## 附录 B：验收清单

**甲（平台侧）**

- [ ] `main.py` 起服务，`/docs` 可访问，`/api/health` 返回 `components.graph.ok=true`
- [ ] `chat_service` 五个方法全部实现，SSE 事件顺序与 `接口文档.md` §6 一致
- [ ] `kb_service` 五个方法全部实现；未实现时返回 503 而非启动失败
- [ ] `/api/upload/image` 两段式可用；体积/格式/数量阈值取自 `setting.py`
- [ ] 会话与反馈落库；重启后历史仍在
- [ ] MCP 加载失败降级为空工具，主链路不受影响
- [ ] `deploy/nginx.conf` 关闭 SSE 缓冲；`X-Accel-Buffering: no` 生效
- [ ] `docker-compose.yml` 一键起后端 + MySQL
- [ ] `tests/test_api/test_sse.py`、`test_multimodal/test_vision.py`、`test_mcp/test_ticket_server.py` 有对应用例

**乙（RAG 侧）**

- [ ] 切片元数据完整率 100%（`page` + `version` 齐全）；抽检 20 块合格 ≥ 18
- [ ] Top-5 命中 ≥ 80%；精排后 Top-8 含正确块 ≥ 90%
- [ ] 引用覆盖率 100%；抽检 20 条无编造；负样本拒答 ≥ 80%
- [ ] 图片提取 ≥ 85%；`ingest_image` 失败必须拒答（不猜）
- [ ] `graph.py` 条件边接入 `evidence_sufficient → augment`，`augment` 硬边界生效
- [ ] 黄金集扩到 50 条；`scripts/eval.py` 一键跑分；badcase 有归因
- [ ] 提示词每次变更都有对应评测记录

**共同**

- [ ] `make test` 全绿；新增能力均有测试用例
- [ ] 无反向 import（`routers → services → agents/tools/rag → config`）
- [ ] 无硬编码阈值/密钥；密钥只在 `.env`
- [ ] `verify` 节点未被绕过；`status` 初值仍为 `NOT_COVERED`
- [ ] 契约变更均先在文档与测试落地

---

**变更记录**

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.1 | —— | 首版：以 `0a20a36` 为基线，界定后端二人范围、文件级所有权、四道接缝、契约变更流程、Git 规范与 D1–D8 并行排期 |
