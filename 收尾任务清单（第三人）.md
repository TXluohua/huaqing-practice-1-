# 收尾任务清单（负责人：第三人 / 前端 `lei`）

> **基线**：`main` = `94b6c60`（PR #6）；RAG 侧修复分支 = `feature-txluohua-dev` = `f39ed4c`（**尚未合入 main，建议先合**）
> **本文档的定位**：把「后续要求 + 最后收尾」合成一张表，**每一项的负责人都是第三人**。
> 涉及别人文件（RAG 侧 / 平台侧）的项，标注了「需谁配合」——由你推动、协调对方落地，或者直接找对方要 PR。
> **纪律**：`main ← feature/*`（PR 直进 main）；契约只追加；P0 契约文件（`state.py`/`graph.py`/`schemas.py`/`routers/**`/`接口文档.md`）需双方 approve。
> **底线（任何情况不砍）**：强制引用 + 引用校验 + 无依据拒答；`status` 初值 `NOT_COVERED`。

---

## 0. 当前状态（实测基线，可直接对照）

| 项 | 数值 | 来源 |
| --- | --- | --- |
| 单测 | **88 passed** | `.venv/bin/python -m pytest tests/ -q` |
| 黄金集 | **20 条**（18 正 + 2 负）+ 3 条图片（**图片实物缺失**） | `data/eval/golden_qa.jsonl`、`golden_image.jsonl` |
| 五项验收指标 | 全达标：Top-5 100%、引用覆盖率 100%、引用真实性 100%、要点命中 100%、负样本拒答 100% | `scripts/eval.py` |
| MRR / NDCG@5 | 1.0000 / 0.9932 | 同上 |
| 稳态延迟 | 纯文档 ≈ **5s**（其中精排 ≈4.9s）；备件类 ≈ **14s**（augment 再 +8.8s） | 逐节点 `timings` |
| 接口 | 11 个全部实现 | `接口文档.md` |
| 环境 | `.env` 已有 `DEEPSEEK_API_KEY` / `DASHSCOPE_API_KEY`；MySQL `127.0.0.1:3307/smka`；本地 HF 模型就绪 | 甲侧复核 |

---

## 1. 必做（D8 交付阻塞，按这个顺序做）

### T1 先把 RAG 修复合进 main（10 分钟）

- **为什么**：`f39ed4c` 修了三个阻塞缺陷（Ollama 超时 / 重建死锁 / LLM 路径覆盖率门槛），后续所有评测都依赖它。
- **做法**：打开 **https://github.com/TXluohua/huaqing-practice-1-/pull/new/feature-txluohua-dev** → 目标 `main` → Create pull request → Merge（merge commit）。
- **验收**：`main` 上 `git log --oneline -1` 能看到 `fix(rag): 修复甲侧实测的 3 条阻塞缺陷`。

### T2 配好密钥后重跑评测，拿到 G3 / G4 真值（30 分钟）

- **为什么**：G3「要点命中 ≥80%」现在是 **44.7%**，那是**没有 LLM 时的抽句式降级值**；现在 `.env` 已有 `DEEPSEEK_API_KEY`，必须重测。G4 图片提取同理（`DASHSCOPE_API_KEY` 已配）。
- **做法**：
  ```bash
  .venv/bin/python scripts/eval.py                 # 产出 data/eval/report_<时间>.json
  .venv/bin/python scripts/eval.py --badcases      # 失败样例追加到 data/eval/badcases.md
  ```
- **要看的**：`answer_point_hit_rate`（严格口径也打印）、`citation_coverage` 必须仍是 100%、`fake_citation_count` 必须 0。
- **验收**：报告里 `generator` 不再是 `extractive-fallback`；G3 ≥ 80%（不达标就把差异写进报告，不要改指标）。
- **需谁配合**：RAG 侧（我）。若 LLM 路径出现新问题（例如某类问题被误拒），把 `report_*.json` + 问题原文给我，我按缺陷处理。

### T3 补 3 张图片 fixture，跑通图片类评测（40 分钟）

- **为什么**：`data/eval/golden_image.jsonl` 里 3 条样例引用的 `fixtures/alarm_E2041.png` 等**实物不存在** → G4 无法评测。
- **做法**：按清单造 3 张图（可截图/画图，内容要与 `expect_extract` 一致）：
  | 样例 | 文件 | 图中要有 |
  | --- | --- | --- |
  | img001 | `data/eval/fixtures/alarm_E2041.png` | 报警码 `E-2041` + 腔体压力异常字样 |
  | img002 | `data/eval/fixtures/cvd_param_table.png` | 参数表：沉积温度 780 ℃ / 压力 300 Pa / 流量 350 sccm |
  | img003 | `data/eval/fixtures/handwritten_note.png` | 模糊手写纸条（**故意识别不出**，用于验证"不允许猜测"） |
- **验收**：`POST /api/upload/image` 能上传；带图提问时 **img003 必须拒答**并提示「图片未能识别，请用文字描述现象」。

### T4 前端消费 `done.qa_id`，放开反馈按钮（20 分钟）

- **为什么**：后端已在 SSE `done` 事件里带 `qa_id`（实测 `done.qa_id=32` → `POST /api/feedback` → 200），但前端还没用 → **用户对自己刚拿到的答案无法提交反馈**，必须刷新历史页。
- **做法（已定位到行，改一处即可）**：
  1. `frontend/src/stores/chat.ts:300` 的 `onDone` 回调里，补一行
     `message.qaId = data.qa_id ?? null`（该回调目前只写了 status / confidence / label / uncertain / latencyMs）；
  2. `frontend/src/stores/chat.ts:57` 的注释「契约缺口：done 事件不带 qa_id」→ 改为已修复；
  3. 不用改 `FeedbackBar.vue`：它的置灰条件就是 `qaId === null`（`FeedbackBar.vue:38`），
     写入 `qaId` 后按钮自动解锁。
- **验收**：浏览器里问一句 → 不刷新即可点反馈 → `POST /api/feedback` 返回 200；刷新历史页仍能看到该反馈状态。
- **需谁配合**：无（后端已就绪）。

### T5 黄金集扩到 50 条 + 阈值重标（2~3 小时）

- **为什么**：开发文档 D8 要求 50 条；且**精排阈值 0.5 与术语阈值 0.40 只在现有 20 条上标定过**，20 条上的 100% 属于"缩样达成"。
- **构成要求**：设备维护 40% / 工艺 25% / 标准 15% / 负样本 10% / 图片 10%。
- **做法**：在 `data/eval/golden_qa.jsonl` 追加 30 条（每行一个 JSON，字段沿用现有格式；`must_cite` 必须是 `文档名#章节号` 且**真实存在于 `data/raw/*.md`**）；备件类条目记得加 `"allow_external_sources": true`。
- **重标阈值**：
  ```bash
  .venv/bin/python scripts/build_index.py
  .venv/bin/python scripts/eval.py
  ```
  看报告里正/负样本的分数与术语缺失比，调整 `backend/setting.py` 的
  `sufficient_score_threshold`（CE 默认 0.5）/ `sufficient_score_threshold_lexical`（0.25）/
  `anchor_kb_missing_ratio`（0.40）——**只追加注释说明，不要改字段名**。
- **验收**：50 条跑完，五项指标达标；报告与阈值调整写进 `RAG实现说明.md`。
- **需谁配合**：RAG 侧（我）。阈值重标是 RAG 侧文件，**建议我改、你跑**：你只要把 `report_*.json` 给我，我改完你复跑。

### T6 正式评测报告 + badcase 归因（1 小时）

- **做法**：
  ```bash
  .venv/bin/python scripts/eval.py --badcases
  git add -f data/eval/report_<最新>.json     # 报告默认被 .gitignore 忽略，留档要 -f
  ```
- **badcase 归因**：`data/eval/badcases.md` 表头是 `日期 | 样例 id | 问题 | 现象 | 归因 | 处理状态`；
  逐条填「现象」（指标哪项不达标）与「归因」（检索没召回 / 引用缺失 / 被误拒 / 阈值问题），
  并汇总一张小表：各类归因各多少条。
- **验收**：报告入库 + badcases 至少覆盖全部失败样例，每条有归因结论。

### T7 三个演示场景走查（1 小时）

- **场景 A 抢修**：报一个报警码（例如 `E-2041`）→ 拿到带引用的处置步骤 → 点引用看原文/页码。
- **场景 B 新人学习**：`mode=training` 提问 → 拿到「基础理解 / 操作要点」分层讲解 + 引用。
- **场景 C 参数查询 + 备件**：问参数（例如沉积温度窗口）→ 拿到数值 + 引用；问备件（例如「腔体门 O-ring 还有库存吗」）→ 拿到库存/库位/到货/**替代件及兼容性依据**。
- **每场景要记录**：截图或 SSE 事件序列、耗时、状态（`OK` / `NOT_COVERED`）、引用数。
- **验收**：三个场景全部走通；拒答场景（例如问不存在的型号 `XH-900`）也演示一次，展示 fail-closed。

---

## 2. 应该做（体验与稳定性，做完 1 再做）

### T8 前端加"备件查询"入口（1 小时）

- **为什么**：后端 `parts_query` 已接进链路，但**三个视图都没有入口**，用户不知道可以问备件。
- **做法（不需要后端改动）**：在 `ChatInput.vue` 加一组「快捷提问」按钮，点击后把问题模板填进输入框并聚焦，例如：
  - 「腔体门 O-ring 还有库存吗？有没有替代件、多久到货？」
  - 「<部件名> 缺货时可以用什么材料替代？有哪些限制？」
  答案会带 `source_type=kb_doc/ticket` 与台账来源的引用，前端 `CitationCard.vue` 已支持按来源分支渲染。
- **验收**：点按钮 → 发送 → 气泡里出现库存/到货/替代件依据（含 `basis`）。

### T9 前端"会话删除 / 重命名"（1~2 小时，需后端先加接口）

- **现状**：`接口文档.md` §2 P2 档已定义 `DELETE /api/chat/sessions/{id}`、`PATCH /api/chat/sessions/{id}`，**未实现**。
- **做法**：先请平台侧补这两个接口（很薄），前端在 `HistoryView.vue` 加删除/重命名的操作与二次确认；未上线前按钮置灰并给 tooltip。
- **验收**：历史页可删除与重命名，刷新后生效。
- **需谁配合**：平台侧（甲）。

### T10 20 并发压测（NFR-02，1 小时）

- **做法**：写一个脚本（`scripts/` 下，或放 `tests/`），用 `httpx.AsyncClient` / `asyncio.gather` 并发 20 个 `/api/chat/stream`，记录成功率、首 Token、总耗时、错误率；把结果写进交付报告。
- **关注点**：并发下 **CE 精排（CPU ≈4.9s/问）会成为瓶颈**——并发 20 时排队明显。若结果不可接受，见 T11。
- **验收**：有压测脚本 + 一份结果（成功/失败数、P95、错误率），结论写清楚"能否支撑 20 并发"。

### T11 优化外部源查询与精排的并行（可选但性价比高，需平台侧配合）

- **现状（实测）**：纯文档 ≈5s（精排 ≈4.9s 是大头）；备件类 ≈14s（`augment` 再 +8.8s，其中外部查询与二次精排 3.7s）。
- **做法（二选一或都做）**：
  1. 平台侧把 `augment` 里的**外部查询与首次精排并行**（`asyncio.gather`），预计省 ~4s；
  2. 有 GPU 时把精排从 4.9s 压到亚秒级，同时可调大 `rerank_max_candidates`（当前 12）换召回。
- **验收**：备件类 P95 ≤ 15s、纯文档 P95 ≤ 8s（`scripts/eval.py` 已按路径分列）。
- **需谁配合**：平台侧（甲）。

---

## 3. 可做（增量功能，文档 §7.4 排 4~6 位，时间不够可砍）

| # | 事项 | 现状 | 说明 |
| --- | --- | --- | --- |
| T12 | 图片知识侧入库（VLM caption + 写 `kb_image`） | `rag/ingest.py` 抽图只落盘，`kb_image` 表 0 行 | 让图纸/参数表截图能被关键词检索；RAG 侧文件，需我配合 |
| T13 | 检索层权限过滤（FR-11） | `build_filters(user_role=…)` 收下参数但不用于过滤 | 需先定义权限分级表；RAG 侧 |
| T14 | 工单检索的语义召回 | 当前是关键词重合打分（确定、可解释） | 无词重合即返回空；需要时改向量召回 |
| T15 | docx / pptx / xlsx 解析 | 明确返回 415（**有意为之**，避免抓不到页码） | 接 MarkItDown 后支持 |

---

## 4. 文档收尾（最后一起做）

- [ ] `README.md`：与最终能力对齐（快速开始 / 一键建库 / 一键评测 / 一键起服务 / 已知限制）
- [ ] `接口文档.md`：把已实现的 11 个接口与 SSE 契约再过一遍（`done` 已含 `qa_id`）
- [ ] `功能完成度清单.md`：把 T2~T7 的结果填进「验证缺口」与「指标对照」两节
- [ ] `开发进度报告.md` / `RAG实现说明.md`：补最终指标与遗留问题
- [ ] 所有报告与截图归档到 `data/eval/`（报告要用 `git add -f`）

---

## 5. 自测命令速查

```bash
# 环境与模型
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/python scripts/prepare_model.py --check        # 本地模型自检
.venv/bin/python scripts/build_index.py                  # 一键重建（默认清空重建；追加用 --keep）

# 测试与评测
.venv/bin/python -m pytest tests/ -q                     # 当前 88 passed
.venv/bin/python scripts/eval.py                         # 五项指标 + 报告
.venv/bin/python scripts/eval.py --limit 5               # 快速回归

# 起服务
make dev                                                 # 后端 8000（/docs 有 OpenAPI）
make dev-web                                             # 前端 5173（代理 /api 与 /static）
# 端口被占时：make dev PORT=8010

# 健康检查（7 个组件）
curl -s localhost:8000/api/health
```

**指标看板的正确读法**（避免误判）：

| 指标 | 目标 | 注意 |
| --- | --- | --- |
| Top-5 命中率 | ≥80% | 备件类问题看 `allow_external_sources`（台账/工单算有效证据） |
| 引用覆盖率 | **100%（不可降级）** | 门槛是「发出去的每条结论都有依据」；LLM 漏引用的句子会被程序化归属或省略 |
| 引用真实性 | ≥95% | `fake_citation_count` 必须为 0 |
| 要点命中率（G3） | ≥80% | **必须配 `DEEPSEEK_API_KEY` 才有意义**；抽句式降级值 44.7% 不算数 |
| 负样本拒答率 | ≥90% | 负样本拒答是**特性**，不是故障 |
| 文字 P95 / 带外部源 P95 | ≤8s / ≤15s | 分路径看，别混在一起 |
| 首 Token | ≤8s（已按决定放宽） | 本实现「先过 verify 再出字」，首 Token ≈ 整条链路耗时 |

---

## 6. 哪些事不要做（避免返工）

1. **不要为了指标好看改测试或改阈值**：不达标就把差异写进报告（`git` 历史里能看出来，得不偿失）。
2. **不要直推 `main`**：一律 feature 分支 → PR。
3. **不要改契约文件的既有字段名/类型**：`state.py` / `setting.py` / `schemas.py` 只追加。
4. **不要碰 `backend/rag/*`、`nodes/retrieval.py`、`nodes/generation.py`、`scripts/*` 的实现**（RAG 侧文件）：需要改就找我，我把改动给你。
5. **不要把 `backend/data/`（模型 4.6GB + 索引）提交进仓库**：已被 `.gitignore` 忽略，保持这样。
6. **报告文件默认被忽略**：要入库用 `git add -f data/eval/report_*.json`。

---

## 7. 交付清单（D8 当天要能拿出来）

- [ ] `main` 含 RAG 三个缺陷修复（T1）
- [ ] 50 条黄金集 + 重标阈值的说明（T5）
- [ ] 一份正式评测报告（含 G1~G5 对照与差距说明）（T2、T6）
- [ ] `badcases.md` 有归因汇总（T6）
- [ ] 3 张图片 fixture + 图片类评测结论（T3）
- [ ] 三个演示场景走查记录（T7）
- [ ] 20 并发压测结论（T10）
- [ ] 前端：反馈闭环可用、备件入口可用（T4、T8）
- [ ] 文档四件套更新完毕（第 4 节）

---

**变更记录**

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1 | 2026-09-18 | 首版：汇总《功能完成度清单》§2.1/§2.3、《平台侧待办与验收清单》遗留项、RAG 侧延迟实测与评测口径修正，**全部标记由第三人负责**，含执行顺序、验收标准、自测命令与"不要做"清单 |
