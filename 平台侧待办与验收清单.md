# 平台侧待办与验收清单（给甲 / tutu）

> **写给**：平台侧负责人（甲 / `ttudde`）
> **基线**：`main` = `44ea473`（PR #3）；本文档所在分支 = `feature-txluohua-dev` = `7745a20`
> **依据**：逐项**实测**（真起服务、真调接口、真跑评测），不是读代码推断。每条都给了证据与验收标准。
> **配套**：`RAG实现说明.md`（RAG 侧交付）、`后端二人分工与协作规范.md`（分工与契约流程）、开发文档 §3.1（FR 清单）

---

## 0. 先说结论

**已经能跑通的（实测通过，不需要再动）**：

| 能力 | 实测结果 |
| --- | --- |
| 端到端问答 + SSE | `meta → token → citations → done` 事件序列完整；`POST /api/chat/stream` 正常 |
| 答案溯源 | 每次 8 条引用，`doc/version/section/page/chunk_id/snippet` 全部有值 |
| 置信度与拒答 | 三级标注；问知识库没有的型号 → `NOT_COVERED` + 置信度 0.59 低档 + 可读原因 |
| 会话与历史 | 会话列表 / 历史消息含引用；**服务重启后 8 个会话完整保留** |
| 反馈 | `done` 事件已带 `qa_id`，用新问答的 qa_id 提交反馈 → `200 recorded`（闭环已通） |
| 入库与重建 | 上传 → 202 + job 进度 5%→100%「入库完成：7 个切片」；一键重建「5 篇/32 切片」19.6s（≤5 分钟达标） |
| 图片上传 | 两段式第一步可用；格式校验正确（txt → 415 `UNSUPPORTED_IMAGE_TYPE`） |
| 培训模式 | `mode=training` 分层讲解（`**基础理解**` / `**操作要点**`），覆盖率 100% |
| 评测 | 黄金集五项指标全达标：Top-5 100%、引用覆盖率 100%（不可降级）、引用真实性 100%（132 项 0 伪造）、要点命中 100%、负样本拒答 100%；MRR 1.0000 / NDCG@5 0.9932；稳态 P95 4.59s；单测 **56 passed** |

**缺口集中在 6 处**（下表按优先级）；另有 2 处配置类改进。**全部属于平台侧文件**，因此需要你来做；我这边（RAG）已完成的不要重复劳动。

---

## 1. 待办总表

> **状态列由甲侧于 2026-09-17 更新**（依据：真起服务、真调接口、真跑测试，见 §6 回执）。
> 原「现状证据」保留不删，便于对照。

| # | 事项 | 优先级 | 原现状证据 | 甲侧状态（2026-09-17） |
| --- | --- | --- | --- | --- |
| 1 | **FR-03 跨源检索：工单 MCP 源** | P0 | `mcp_client.py` / `mcp_servers/ticket_server.py` / `tools/__init__.py` 均 0 字节；`routers/chat.py:128` 硬编码 `mcp.ok=False` | ✅ **已完成**：三文件已实现；health 真实探测返回 `ok=true, tools=["ticket_search"]`；问答已能引出 `source_type=ticket` 的工单引用 |
| 2 | **FR-10 备件查询未接线** | P0 | `kb_tools.py parts_query` 已实现但**全仓零调用** | ✅ **已完成**：已纳入 augment 白名单并在确定性分支调用，「腔体门 O-ring 还有库存吗」的答案含库存/库位/到货/替代件依据 |
| 3 | **FR-02 识别失败的提示话术** | P1 | 无 key 时走通用拒答话术 | ✅ **已完成**：`respond` 节点把「图片未能识别，请用文字描述现象」写入 `uncertain`（→ SSE `done`），实测已出现在前端可见字段；识别失败时不发 `image` 事件 |
| 4 | **FR-09 高频问题记录** | P1 | 全仓 grep 无实现 | ✅ **已完成**：新增 `question_stat` 表 + `GET /api/admin/questions/top`；同问题不同标点会归并计数 |
| 5 | **部署产物** | P1（交付阻塞） | 四个文件均 0 字节 | ✅ **已完成**：`Dockerfile.backend` / `Dockerfile.frontend` / `nginx.conf` / `docker-compose.yml`；nginx 已设 `proxy_buffering off` + `proxy_cache off` + 长超时 |
| 6 | **三个测试文件** | P1 | 三个文件均 0 字节 | ✅ **已完成**：`test_api/test_sse.py`、`test_multimodal/test_vision.py`、`test_mcp/test_ticket_server.py`，新增 28 例，全套 **84 passed** |
| 7 | 日志级别与脱敏 | P2 | `debug=True` → LangGraph 把完整检查点打进日志 | ✅ **已完成**：`log_level` 与 `debug` 解耦（默认 INFO）；新增 `SecretMaskingFilter` 脱敏密钥与图片路径；第三方 logger 压到 WARNING |
| 8 | 本地密钥与降级说明 | P2 | 无 `.env` → 生成走抽句式降级 | ⏳ **待环境**（非代码问题）：需配 `DEEPSEEK_API_KEY` 后重跑 `scripts/eval.py`。已在 `.env.example` 与 README 写明区分降级值 |
| 9 | **前端未消费 `done.qa_id`**（跨端，归第三人） | P1 | `chat.ts:57` 注释仍写「done 不带 qa_id」 | ⏳ **待第三人**：后端已实测带回 `qa_id`（新增测试覆盖），仅需前端 `onDone` 取值 |

---

## 2. 逐项说明

### 2.1 【P0】FR-03 工单 MCP 源 —— 现在是"单源"而不是"跨源"

- **现状**：`backend/mcp_client.py`、`backend/mcp_servers/ticket_server.py`、`backend/tools/__init__.py` 三处都是 0 字节空文件；健康检查如实自报
  `components["mcp"] = {"ok": False, "detail": "尚未接入（D7：ticket_search MCP server）"}`（`backend/routers/chat.py:128`）。
  也就是说规格里的「统一检索手册文档与**历史工单**」目前只做到了手册这一源。
- **要做**：
  1. `mcp_servers/ticket_server.py`：用 `fastmcp` 起 stdio server，至少提供 `ticket_search(symptom)`（历史工单案例检索）；
  2. `mcp_client.py`：`MultiServerMCPClient` 建连 + `await client.get_tools()`，**加载失败必须 `try/except` 降级为空工具列表**，绝不中断主链路（风险 R7）；
  3. `tools/__init__.py`：聚合「本地工具（`kb_tools`）+ MCP 工具」，并处理同名/权限；与 `setting.augment_tool_whitelist` 取交集后供 `augment` 使用；
  4. `routers/chat.py:128` 的硬编码 `mcp.ok=False` 换成真实探测。
- **不要漏的点**：`Citation.source_type` 已支持 `"ticket"`；前端引用卡片也按 `source_type` 分支渲染（`CitationCard.vue`）。工单来源要正确填 `source_type="ticket"`，否则前端会显示成文档。
- **验收**：
  - `python -m pytest tests/test_mcp/test_ticket_server.py -q` 通过；
  - 手工：故意让 MCP server 启动失败 → 主链路仍能回答问题（降级为空工具）；
  - `/api/health` 的 `mcp` 项返回真实状态（`tools` 列表 / `degraded` 标志）。

### 2.2 【P0】FR-10 备件查询：工具是好的，但没人调用它

- **现状**：`backend/tools/kb_tools.py:514 parts_query` + `:442 _load_parts_inventory` 已实现，实测**质量很好**：

  ```
  parts_query("腔体门 O-ring") →
    {"found": true, "code": "SP-ETA-0101", "stock": 12, "location": "备件库 A-03",
     "lead_time_days": 3,
     "substitutes": [{"code": "SP-ETA-0101F", "basis": "备件目录 V1.4 §1.2：允许 FFKM 全氟醚橡胶替代 FKM…"}]}
  parts_query("不存在的备件XYZ") →
    {"found": false, "notes": ["备件目录中未找到该备件：不做无依据的替代推断，需原厂确认"]}
  ```

  但**全仓没有任何地方调用它**（`grep -rn parts_query backend --include=*.py`，除定义处无命中）。所以从问答链路触发不了，也没有独立接口 —— FR-10 目前是**死代码**。
- **要做（二选一，建议都做）**：
  1. 接进 `augment`：把 `parts_query` 纳入白名单，并在确定性分支里对「备件类提问」（含 库存/替代/到货/交期/缺货 等词，或诊断结论指向换件）调用它，结果作为证据块进上下文；
  2. 按 `接口文档.md` §2 P2 档开 `POST /api/parts/query`（前端 `AdminView`/问答页都能挂）。
  > 说明：`backend/agents/nodes/multimodal.py` 里的 `augment` 当前是**确定性有界流程**（无文本模型时不让模型选工具），所以第 1 条不需要 LLM 也能落地。
- **验收**：抽查 10 次与 `backend/config/parts_inventory.yaml` 一致率 100%；替代件建议必须附 `basis`；无权威依据时明确「需原厂确认」（工具已满足，接线后要在回答里体现）。

### 2.3 【P1】FR-02 识别失败要给"能照做"的提示

- **现状**：`multimodal.py:139` 已有判定
  `if merged.image_type == "unknown" or merged.confidence < settings.vision_min_confidence:` —— 但无 `DASHSCOPE_API_KEY` 时整条识别不执行，最终走到 `verify` 的**通用拒答**：
  「知识库未覆盖该问题，不给出具体操作步骤…」
- **规格要求**（开发文档 §3.2 FR-02 失败处理）：`confidence < 0.5` 或 `image_type = unknown` 时，明确提示
  **「图片未能识别，请用文字描述现象」**，**不允许猜测**。
- **要做**：把这条提示做成对用户可见的输出（`answer` 里或 `uncertain` 里），并保证：
  - 识别失败时**不发** `image` 事件（或发 `image_type=unknown` + `confidence` 低值，按接口文档 §6 的图片失败示例）；
  - 识别成功时 `image` 事件**先于** `token`（接口文档 §6 顺序）。
- **配套缺口**：`data/eval/golden_image.jsonl` 里 3 条样例引用的图片文件不存在（需要 `data/eval/fixtures/alarm_E2041.png` 等 3 张），所以多模态评测现在跑不了 —— 图片和用例要对齐同一份清单。

### 2.4 【P1】FR-09 高频问题记录

规格本期交付 = 分层讲解（已完成）+ **高频问题记录**（未实现）。
建议：在 `db.py` 里按「归一后的问题」聚合计数（或加一张统计表），提供查询接口或管理页数据。
验收：能拿到「被问得最多的 N 个问题」，用于反推培训内容与知识补充。

### 2.5 【P1】部署产物（当前无法容器化交付）

- `deploy/Dockerfile.backend`、`deploy/Dockerfile.frontend`、`deploy/nginx.conf`、`docker-compose.yml` 全是 0 字节。
- **硬要求**：nginx 对 SSE 必须
  `proxy_buffering off;` + `proxy_cache off;` + 响应头 `X-Accel-Buffering: no`
  （后端已在 `routers/chat.py` 发这个头），否则首 Token 会退化（接口文档 §8 缺口⑧）。
- **别漏**：镜像里要预置本地向量/精排模型（`backend/data/models`，约 4.6 GB，已被 `.gitignore` 排除）。
  构建阶段执行：
  ```bash
  .venv/bin/python scripts/prepare_model.py --download --source modelscope --with-reranker
  ```
  或把该目录作为卷挂进去。模型缺失时 `EMBEDDING_PROVIDER=local` 会**直接报错**（刻意不静默降级）。
- 另外 `main.py` 的 `lifespan` 会预热模型（实测 ~10s），容器 healthcheck 的 `start_period` 要留够。

### 2.6 【P1】三个空测试文件

| 文件 | 应覆盖 |
| --- | --- |
| `tests/test_api/test_sse.py` | SSE 事件完整性（`meta/image/token/citations/done`）、**流已开始后出错发 `error` 事件且改 HTTP 状态码无效**、`done` 带 `qa_id` |
| `tests/test_multimodal/test_vision.py` | **识别失败必须拒答（不允许猜测）**、`confidence < 0.5` 走提示分支、`image` 事件顺序 |
| `tests/test_mcp/test_ticket_server.py` | MCP server 可独立调用、工具报错时主链路降级 |

（RAG 侧的我已补齐：`tests/test_rag/*` 三个 + `tests/test_agents/test_verify.py`，当前全套 56 passed。）

### 2.7 【P2】日志级别与脱敏

`main.py:40` 是 `logging.basicConfig(level=DEBUG if settings.debug else INFO)`，而 `setting.debug` 默认为 `True`。
我实测时看到 LangGraph 把**整个检查点状态**（含大段二进制 repr）打进日志，`/tmp/srv.log` 单次问答就上万行。
建议：默认 `INFO`，并把 `DEBUG` 显式留在 `.env`；同时按 NFR-03 做脱敏（密钥、图片路径、识别结果）。

### 2.8 【P2】密钥与"降级值"的区分

无 `.env` → `DEEPSEEK_API_KEY` / `DASHSCOPE_API_KEY` 都没有 → `llm`/`vlm` 组件为 ❌，生成走**抽句式降级路径**
（`answer_structured.generator = "extractive-fallback"`，`errors` 里也会写明）。
配好 key 之后**必须重跑评测**：现在的「要点命中 100%」里，严格口径（只看答案正文）是 **44.7%**，那是降级值，不是 LLM 的水平。

### 2.9 【P1 / 跨端】前端还没消费 `done.qa_id`

后端这一环是通的（实测：新问答 `done.qa_id=32` → `POST /api/feedback` → `200 recorded`），
我本轮也把 `schemas.DoneEvent.qa_id` 补齐了（此前只在 payload 里给、模型没声明）。
**但前端还没用**：`frontend/src/stores/chat.ts:57` 仍写着「契约缺口：done 事件不带 qa_id」，
`onDone` 回调也没有取这个字段 —— 所以「刚拿到的答案」的反馈按钮仍会是置灰状态，
必须刷新历史页才拿到 `qa_id`。

请转告第三人（前端/`lei`）改一处即可：在 SSE `done` 处理里把 `data.qa_id` 写入当前消息的
`qaId`，反馈按钮即可解锁。**后端不需要再动**。

---

## 3. 你可以直接跑的自测命令

```bash
# 环境与模型（首次）
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/python scripts/prepare_model.py --check        # 本地向量/精排是否就绪（含自检）

# 语料与索引
.venv/bin/python scripts/build_index.py                  # 一键重建：5 篇 / 32 切片（实测 19.6s）
.venv/bin/python scripts/inspect_chunks.py --stats        # 看索引统计

# 测试与评测（RAG 侧 + 链路骨架）
.venv/bin/python -m pytest tests/ -q                      # 当前 56 passed
.venv/bin/python scripts/eval.py                          # 黄金集五项指标 + JSON 报告

# 起服务
make dev        # 后端 http://127.0.0.1:8000（/docs 有 OpenAPI）
make dev-web    # 前端 http://127.0.0.1:5173（代理 /api 与 /static）
```

**验收口径提醒（已修订）**：`开发文档 §8.4` —— 首 Token 目标由 `≤2s` 放宽为 **`≤8s`**，因为本实现刻意
「先过 `verify` 再出字」（首 Token ≈ 整条链路耗时，实测 4.33–6.92s）。要恢复 ≤2s 的两条路写在 §8.4。

---

## 4. 协作约定（避免返工）

1. **契约只追加**：`state.py` / `setting.py` / `schemas.py` 都已有先例（我追加了 4 个输入字段 + RAG 配置；你追加了 augment/多模态配置）。**不要改已有字段名与类型**。
2. **P0 契约文件需双方 approve**：`state.py`、`graph.py`、`schemas.py`、`routers/**`、`接口文档.md`。
   注意：`graph.py` 的条件边是我上次改的（`build_context → [证据是否足够?] → augment | generate`），
   你新增 `augment` 节点后**会自动进入这条分支**，不需要再改图。
3. **分支模型**：`feature/* → PR → main`（团队现状，开发文档 §6.4 的 `develop` 未启用）。
   建议先把 `feature-txluohua-dev`（含我这两个提交）合进 `main`，再切你的分支，免得后续 rebase 冲突。
4. **`tools/__init__.py` 一直留给你**（按分工：聚合 + MCP 降级属平台侧），我没有动它 —— FR-03 要在这里落地。
5. 我的文件范围：`backend/rag/*`、`nodes/retrieval.py`、`nodes/generation.py`、`prompts/{system,generate,rewrite}.md`、`tools/kb_tools.py`、`config/glossary.yaml`、`scripts/*`、`tests/test_rag/*`、`tests/test_agents/test_verify.py`、`data/raw`、`data/eval`。要改这些请先说一声。

---

## 5. 我这边剩下的（不用你管）

- **黄金集 20 → 50 条**（D8 目标）：扩集后需要重新标定精排阈值（`sufficient_score_threshold` /
  `anchor_kb_missing_ratio` 是在现有 20 条上校准的，正样本 0.33 / 负样本 0.43）。
- **一个已知边界**：纯代词追问 + 话题切换到知识库完全没有的东西（例如「那它要多少钱？」）时，
  系统会用上文材料作答并给出引用（不会编造），但不会主动说「价格未覆盖」。
  确定性链路无法消解这种指代；配 `DEEPSEEK_API_KEY` 走 LLM 路径后由提示词覆盖。

---

*本文档基于 `7745a20` 实测撰写；「甲侧状态」一栏由甲于 2026-09-17 追加，见 §6。*

---

## 6. 甲侧回执（2026-09-17）

### 6.1 逐项完成情况

| # | 交付物 | 关键文件 |
| --- | --- | --- |
| 1 | 工单 MCP 源 | `backend/mcp_servers/ticket_server.py`（fastmcp stdio server）、`backend/mcp_client.py`（MultiServerMCPClient + 降级）、`backend/tools/__init__.py`（本地+MCP 聚合）、`backend/config/tickets.yaml`（6 条示例工单） |
| 2 | 备件接线 | `backend/agents/nodes/multimodal.py`（augment 增加外部源分支）、`setting.augment_tool_whitelist` 增加 `parts_query`/`ticket_search` |
| 3 | 识别失败提示 | `backend/agents/nodes/response.py`（`build_image_notice` → `uncertain`） |
| 4 | 高频问题 | `backend/db.py`（`question_stat` 表）、`services/chat_service.py`（计数 + `top_questions`）、`routers/admin.py`（`GET /api/admin/questions/top`） |
| 5 | 部署产物 | `deploy/Dockerfile.backend`、`deploy/Dockerfile.frontend`、`deploy/nginx.conf`、`docker-compose.yml` |
| 6 | 三个测试文件 | `tests/test_api/test_sse.py`（12 例）、`tests/test_multimodal/test_vision.py`（9 例）、`tests/test_mcp/test_ticket_server.py`（7 例） |
| 7 | 日志与脱敏 | `backend/utils/text.py`（`setup_logging` + `SecretMaskingFilter`）、`backend/main.py` |
| 附 | 图片 fixture | `data/eval/fixtures/` 三张**合成图**（alarm_E2041 / cvd_param_table / handwritten_note），让 `golden_image.jsonl` 可执行；**建议用现场真实截图替换** |

### 6.2 改动了乙侧两个文件（请复核）

| 文件 | 改动 | 原因 |
| --- | --- | --- |
| `backend/rag/retriever.py` | `evidence_to_citations` 的 `source_type` 由「只认 image，其余 kb_doc」改为通用透传（`kb_doc`/`ticket`/`image`，默认值不变） | 本清单 §2.1 明确要求工单引用填 `ticket`，否则前端按文档渲染 |
| `backend/agents/nodes/retrieval.py` | `should_augment` 增加一条：备件/工单类问题在硬边界内强制走 `augment` | 否则文档证据充分时不会进 augment，FR-03/FR-10 在正常提问下永远触发不到 |

两处均为**新增判断**，原有语义与默认值未变。

### 6.3 依赖版本注意（踩过的坑）

MCP 三个包必须**成套锁定**，否则 ImportError：

```
fastmcp==2.14.7  +  langchain-mcp-adapters==0.3.2  +  mcp==1.30.0   ✅ 实测可用
fastmcp 4.x 需要 mcp>=2，而 langchain-mcp-adapters 0.3.x 需要 mcp<2 → 冲突
```

已写入 `requirements.txt` 并加注释说明。

### 6.4 自测命令与结果

```bash
.venv/bin/python -m pytest tests/ -q          # 84 passed（原 56 + 新增 28）
make dev                                       # 起服务
curl localhost:8000/api/health                 # mcp 为真实探测
```

> 本机无 4.6GB 本地模型，验证时用 `EMBEDDING_PROVIDER=hashing RERANK_PROVIDER=lexical` 兜底；
> 未配 `DEEPSEEK_API_KEY` 时生成仍走抽句式降级（第 8 项）。
