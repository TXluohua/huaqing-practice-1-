# 环境变量示例（复制为 .env 后填写；.env 不入版本库）
# 所有变量名与 backend/setting.py 的字段名一致（大小写不敏感）

# ---------------------------------------------------------------- 应用
DEBUG=true
API_PREFIX=/api
# 前端开发服务器地址（CORS 白名单，逗号分隔或 JSON 数组）
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

# ---------------------------------------------------------------- 模型
# 文本模型（DeepSeek，OpenAI 兼容接口）
# 未配置时生成节点降级为抽句式答案（要点命中率明显下降，务必配置）
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# 图片识别（阿里云 DashScope qwen-vl-max）；未配置时多模态提问不可用
DASHSCOPE_API_KEY=
VLM_MODEL=qwen-vl-max

# ------------------------------------------------------------ 向量化与精排
# local（默认，本地 HF 模型，需先跑 scripts/prepare_model.py）
# auto（local -> ollama -> hashing 降级链，不含云端）
# ollama / hashing（零依赖兜底，检索质量弱）/ dashscope（云端，需显式开启）
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5
EMBEDDING_OFFLINE=true

# auto / cross-encoder / lexical
RERANK_PROVIDER=auto
RERANK_MODEL=BAAI/bge-reranker-v2-m3

# ---------------------------------------------------------------- 数据库
# 本机已就绪的 MySQL：127.0.0.1:3307（端口非默认 3306），库名 smka，utf8mb4；
# 账号密码见各自 .env（不入版本库）。建表由 db.py 的 init_db() 启动时自动完成。
# DATABASE_URL=mysql+aiomysql://user:password@127.0.0.1:3307/smka?charset=utf8mb4
# 零依赖备用（无需任何服务）：
# DATABASE_URL=sqlite+aiosqlite:///./backend/data/sqlite/business.sqlite
DB_ECHO=false

# ---------------------------------------------------------------- 目录
# UPLOAD_DIR=./backend/data/uploads
# CHROMA_DIR=./backend/data/chroma
# MODEL_DIR=./backend/data/models

# ---------------------------------------------------------------- 检索参数
# 调参不改代码（NFR-04）；改动影响检索质量时必须重跑评测
# CHUNK_SIZE=800
# RERANK_TOP_N=8
# RETRIEVE_TOP_K=40
# RERANK_MAX_CANDIDATES=12
