# 讲解提纲（演示与答辩用）

> **这份文档解决三件事**：开场怎么说、每屏讲什么、被问到怎么答。
>
> | 你要找的 | 去哪份文档 |
> | --- | --- |
> | 点哪个按钮、会看到什么（操作步骤） | `使用手册.md` |
> | 接口字段与错误码 | `接口文档.md` |
> | 交付范围、指标、已知限制 | `交付说明.md` |
> | 逐项完成度与实测证据 | `功能完成度清单.md` |
> | 接手做什么 | `接手任务清单.md` |
>
> **基线**：`main` = `8775408`｜单测 **146 passed**｜接口 **35 个**（业务 24 个）｜页面 **7 个**
> 讲解前请先跑一遍 §9「演示前 3 分钟自检」。

---

## 0. 开场：30 秒电梯陈述（建议背下来）

> "这是一套面向**半导体设备维护**的知识库问答系统。它要解决的不是「能不能答」，
> 而是**一线工程师敢不敢信**：每条结论都能点开看到出处 —— 哪本手册、哪个版本、第几页第几节；
> 知识库里没有依据的问题，它**直接说不知道**，而不是编一个看起来很像的答案。
>
> 在这之上我们做了三块**独立业务**：维护计划生成、备件商城与采购结算、考核认证。
> 其中计划项的周期和考卷题目，**同样只能来自手册原文**，抽不到依据就进「未覆盖」清单，绝不编。
>
> 现在 **146 个单测全过**；45 条评测集上引用覆盖率 **100%**、伪造引用 **0**、
> 无依据问题拒答 **100%**。"

**如果只允许说三句话**：查得准 · 引得实 · 不敢答的敢说不知道。

---

## 1. 讲解主线：五屏讲完（约 8 分钟）

每一屏都按「**一个观点 + 一个动作 + 一个数字**」讲，不要同时讲两个观点。

| # | 这一屏的观点 | 动作 | 期望看到 | 卡住了怎么接 |
| --- | --- | --- | --- | --- |
| 1 | **答案可溯源**：不是"模型说的"，而是"手册第几页说的" | 在 `/chat` 问「腔体门 O-ring 多久更换一次？」，点开引用卡片 | 答案 + 引用（文档名/版本/章节/页码）+ 置信度标签；点引用能看到原文切片 | 引用卡片打不开就说"引用里带着 `chunk_id`，可回到切片列表定位"（`/admin` 页） |
| 2 | **不会的地方敢说不会**（这是本系统的性格） | 问一个知识库里没有的：「明年维保预算大概多少？」 | 拒答：`知识库未覆盖该问题`，不给操作步骤 | 若答了 → 立刻换问「XH-900 腔体清洗周期是多少」（另一条负样本），并说明这一条已被负样本集覆盖 |
| 3 | **计划不是猜的**：周期只从手册原文抽 | 进 `/plans`，用默认值点「生成并保存」 | 计划项列表；每条有「依据」可点开；顶部有**「未覆盖项」告警**（手册没写周期的被如实列出） | 未覆盖项为空就说"这套语料里都有依据"，改点某条的「依据」看它来自手册哪一节 |
| 4 | **有流程、有边界、有人确认** | `/parts` 搜 `O-ring` → 详情看**禁止替代清单** → 加购物车 → `/procurement` **先演示空车提交被拦** → 提交 → 「人工确认」→「确认收货」→「登记结算」 | 替代件带兼容性依据；空车提交报 `EMPTY_ORDER`；人工确认弹二次确认框（写明**不会向供应商真实下单**）；结算台账出现记录 | 空车演示失败就跳过，改用"明细已冻结"（改数量按钮置灰）来讲边界 |
| 5 | **考核认证也是可溯源的，而且不自动发证** | `/training` 按「Etcher-A / 真空系统」出 5 题 → 答完交卷 | 每题可展开「依据」；判分结果逐题给对错/标准答案/依据；**通过后只有「申请认证」按钮**（不自动发证） | 若题量少于 5 道，正好讲"依据不足的题被剔除，宁可不命题"（这是加分点，不是故障） |

**收尾加一句（加分项）**：业务组四个页面与问答链路**完全解耦** —— 不共享会话、不走 SSE，
问答侧的模型/向量库/MCP 任一降级都不影响业务页；这条边界有测试强制（见 §3 第 7 条）。

---

## 2. 时间分配（三档，按场合挑）

| 时长 | 内容 | 备注 |
| --- | --- | --- |
| **5 分钟** | §0 开场 → 屏 1（引用溯源）→ 屏 2（拒答）→ 屏 5（考核认证）→ §8 收尾 | 业务页一句话带过：「另外还有计划/商城/采购三块，都是独立页面」 |
| **10 分钟** | §0 → 屏 1~5 → §8 收尾 | 默认档，每屏 90 秒左右 |
| **20 分钟** | §0 → 屏 1~5 → §1 收尾独立性 → **§3 技术底牌挑 3 条深讲** → §8 收尾 + 留 5 分钟问答 | 深讲推荐：零幻觉四道关、空覆盖缺陷、解耦测试 |

---

## 3. 技术底牌：被追问时的应答卡

> 用法：**先给一句话回答，等对方追问再展开**。不要一上来就讲实现。

| # | 可能被问 | 一句话回答 | 追问时展开 | 证据 |
| --- | --- | --- | --- | --- |
| 1 | AI 会胡编，你们怎么防？ | **四道代码闸门，不靠提示词自觉** | ①生成前强制引用；②校验：出现不存在的引用编号、或一条引用都没有 → 直接判未覆盖（fail-closed）；③**引用归属**：生成后逐句核对，能归属的自动补引用、归不上的整句不输出；④拒答闸门：没有任何引用、出现伪造编号、精排分低于阈值、**覆盖率低于要求**（默认 100%，置信度未到高挡时按 80% 兜底）、或问题里的关键标识在知识库里根本不存在 → 拒答 | `backend/rag/citation.py`、`backend/agents/nodes/generation.py` |
| 2 | 引用编号会不会是模型编的？ | **编号由代码分配，模型编的一律现形** | 引用编号在 `build_context` 阶段按上下文块顺序生成；校验时逐个比对，出现越界编号就是「伪造引用」，该答案直接判不通过，不做静默修补（有专门测试） | `tests/test_rag/test_citation.py::test_fake_citation_is_detected` |
| 3 | 这些指标是不是你们自己定的？ | **指标是任务书给的，我们只做两件事：如实测、不达标就说** | G2 引用覆盖率 100% 是**不可降级线**，我们没动过阈值；本轮上交前审计还发现一个**"空覆盖"陷阱**：有一条回答的结论句数为 0，覆盖率被当成 100% 记账 —— 指标看着达标其实没验，我们把它修了并加了回归测试。这种"自己拆自己的指标"比讲漂亮数字更值钱 | `功能完成度清单.md` §7.3 |
| 4 | 为什么用本地模型而不是调 API？ | **数据不出厂 + 可控成本** | 向量模型 `bge-large-zh-v1.5`（1024 维）与精排 `bge-reranker-v2-m3` 都跑在本地（约 4.6GB，`HF_HOME=backend/data/models`）；只有文本生成与图片识别用云端（可用 `.env` 一键切换/关闭，关掉后系统**降级但不挂**） | `README.md`、`backend/setting.py` |
| 5 | 业务和问答为什么不放一起？ | **项目方明确要求解耦，且我们把它做成了测试** | 业务四个页面各有独立路由与独立 API 模块，**不引用**问答的 SSE/会话/store；后端有 7 项独立性测试用 AST 检查 import、并断言业务路径与 `/api/chat` 不相交 —— 写歪一个 import 就红 | `tests/test_api/test_module_independence.py` |
| 6 | 20 个并发能撑住吗？ | **撑不住，我们实测并如实记录** | n=1 → 8.5s、n=5 → 40s、n=20 → 190s，吞吐恒定 ≈0.11 req/s。主因是 CPU 上精排（Cross-Encoder）≈4.9s/问且串行。两条低成本优化路径已写进文档：外部源查询与精排并行（省 ~4s）、GPU 部署 | `data/eval/loadtest_*.json`、`交付说明.md` §7 |
| 7 | 权限控制做了吗？ | **没做，缺前置条件** | `user_role` 已经透传到检索层但**没有参与过滤**，因为缺一张"权限分级表"（谁能看哪类文档）。这不是遗忘，是文档里明确列为待办的第 1 项；采购审批的角色校验是同一个根因 | `功能完成情况`、`接手任务清单.md` §3 |
| 8 | 图片能识别，但截图能存进知识库吗？ | **作为"提问图片"完全可用，作为"知识条目"还没入库** | 图片评测 5/5 通过（含一条按设计拒答的模糊纸条）；但抽图不写 caption，`kb_image` 表 0 行 —— 也就是**图纸/参数表截图目前检索不到**，这是已记录的遗留项 | `README.md` 已知限制 |
| 9 | 数据是真的吗？ | **语料是示例语料，但链路与约束是真的** | 5 篇手册/规范/备件目录共 32 个切片，都是自建的示例文档；工单、备件台账也是示例数据。**"依据必须来自原文"这套机制不因数据是示例而打折** —— 换真实语料只需替换 `data/raw` 与台账，代码不用改 | `data/raw/`、`backend/config/parts_inventory.yaml` |
| 10 | 为什么不用现成框架（RAGFlow / Dify / LangChain 全家桶）？ | **要的是可控的失败方式** | 用了 LangGraph 做编排，但检索/校验/拒答这些**决定"敢不敢答"的部分全部自己写**：BM25+向量+RRF、交叉编码器精排、引用归属、锚点校验。框架能给"能答"的能力，给不了"该拒答时一定拒答"的确定性 | `backend/rag/` |
| 11 | 三个人的活怎么分？ | **按契约切，不按文件切** | 先冻结契约（状态、接口模型、数据表、错误码），再并行实现；三层依赖单向：`routers → services → agents/tools/rag → config`，契约只追加不修改。所以三块业务能同时开工、互不阻塞 | `后端二人分工与协作规范.md` |
| 12 | 交付之后谁维护？ | **有接手清单，第一入口就是它** | `接手任务清单.md`：先做什么、验收标准、红线、已知待改进点（逐条写了现状与建议）、还有一份回执模板 | `接手任务清单.md` |

---

## 4. 现场操作卡（讲解版：说什么 + 兜底）

> 详细的逐步操作看 `使用手册.md` §7；这里只补「讲解时该说什么」和「出问题怎么圆」。

| 步骤 | 你要边做边说的一句话 | 若出问题 |
| --- | --- | --- |
| 打开 `/chat` 提问 | "这里问的是语料里有的问题，重点不是答案，是**下面那排引用**" | 首字慢：说"本机 CPU 跑精排，首次约 4 秒；空闲机器 P95 是 3.9 秒" |
| 点开引用 | "这是**切片级**定位：文档、版本、页码、章节、切片 ID 都有" | 抽屉空白：退回列表，说"切片可以在管理页按 `chunk_id` 查到" |
| 提负样本问题 | "现在问一个手册里没有的 —— 它应该说**不知道**" | 见 §1 屏 2 的兜底 |
| `/plans` 生成 | "注意上面这条**未覆盖项**告警：手册没写周期的项目，它列出来让我们人工确认，**没有编一个周期糊过去**" | 生成较慢：先讲"周期是从检索到的原文里正则抽的，不是模型算的" |
| `/parts` → `/procurement` | "先看一个**错的操作**：空车提交 —— 它被拦住了，这是有意的保护" | 状态不对：改讲"已结算的单子不会再给'登记结算'按钮，避免点了必然报错的假按钮" |
| 人工确认 | "这是**唯一的确认点**，弹窗里写明了：确认后才会允许收货与结算，**系统不会向供应商真实下单**" | 弹窗没出现：说明前端版本，改点提交后的状态变化 |
| `/training` 出题交卷 | "每题下面都能展开**依据**；判分是确定性的代码，不是模型打分" | 出题 422：说"这个主题抽不到有依据的题，换个主题"（这本身就是要讲的行为） |
| 交卷后 | "**没有自动发证** —— 通过只给一个'申请认证'按钮，发证必须有人显式操作，签发方固定写「内部授权」" | 证书列表为空：点刷新，或直接讲到期提醒接口 |

---

## 5. 数字卡（可引用的数字 + 出处）

**讲的时候只挑 3~4 个数字**，其余被问到再翻。

| 数字 | 含义 | 出处 / 命令 |
| --- | --- | --- |
| **146 passed** | 单测全绿 | `.venv/bin/python -m pytest tests/ -q` |
| **35 个接口**（业务 24） | 接入层规模 | `http://127.0.0.1:8000/docs` |
| **7 个页面** | 问答组 3 + 业务组 4 | `使用手册.md` §0 |
| **100% / 0** | 引用覆盖率 / 伪造引用数（270 项引用） | `data/eval/report_20260920_184613.json` |
| **84%~90%** | G3 严格要点命中率（多次复跑区间，目标 ≥80%） | 同上（单次 87.6%；宽松口径 100%） |
| **100%（5/5）** | 无依据问题拒答率 | 同上 |
| **5/5** | 图片类评测通过 | `data/eval/report_image_20260920_184820.json` |
| **0.9750 / 0.9785** | MRR / NDCG@5 | `report_20260920_184613.json` |
| **0 / 40** | 被误拒的正样本 | 同上 |
| **3.9s / 4.3s**（空闲） | 文字 P95 / 带外部源 P95（并行跑任务时 13.7~24s） | README 实测指标表 |
| **8.5s / 40.1s / 189.9s** | 并发 n=1 / n=5 / n=20 的 P95（吞吐恒定 **≈0.11 req/s**，**不能支撑 20 并发**） | `data/eval/loadtest_20260918_193036.json`（n=1）、`…_193125.json`（n=5）、`…_192737.json`（n=20） |
| **9 节点 + 1 条件边** | LangGraph 主图规模 | `backend/agents/graph.py` |
| **4.6GB** | 本地向量 + 精排模型体积 | `scripts/prepare_model.py --check` |

> **数字纪律**：延迟与 G3 都要说**区间**并说明机器负载；不要说"每次都是 87.6%"。

---

## 6. Q&A 预演（15 问，含"不要说什么"）

| 问题 | 回答要点（3 句内） | 不要说什么 |
| --- | --- | --- |
| 这跟直接用 ChatGPT 有什么区别？ | 差别不在"能不能答"，在**答案能不能验收**：每条结论有切片级出处；没依据时它拒答；引用编号由代码分配，模型编不出来 | 不要说"我们的模型更强" |
| 准确率多少？ | 分指标说：检索命中 100%、引用覆盖率 100%、要点命中 **84%~90%**（区间）、负样本拒答 100% —— 都要在 45+5 条自建评测集上说，**不宣称通用准确率** | 不要说"准确率 95% 以上"这种笼统数字 |
| 45 条评测集够吗？ | 不够，是**自建基线**，覆盖 5 篇文档的问答/拒答/图片/安全类问题；真实语料上必须重跑并重标三个阈值 | 不要说"已经充分验证" |
| 会不会答错？ | 会。所以我们把"错得可控"作为设计目标：要么有依据，要么拒答；置信度三档标注，低置信度会提示人工确认 | 不要说"不会答错" |
| 拒答是不是太保守了？ | 是有意保守：设备维护场景里，一个"看起来对"的错误操作会导致设备损坏或人身风险。宁可拒答，也不给没有依据的步骤 | 不要为了显得聪明而夸口"几乎不拒答" |
| 换真实语料要多久？ | 替换 `data/raw` 与备件台账，重跑入库与评测；三个阈值（精排、术语、引用归属）需要在真实语料上重标，只改配置文件 | 不要说"直接就能用" |
| 为什么延迟 4 秒？ | 本机 CPU 跑交叉编码器精排 ≈4.9s/问，是主要成本；GPU 部署或减少候选数可压到亚秒级 | 不要把延迟归因到"网络慢" |
| 为什么不做流式首字更快？ | 已经是 SSE 流式（`meta → token* → citations → done`）；首字慢是**生成前**的检索+精排决定的，不是流式实现问题 | 不要承诺"改成流式就快了" |
| 采购能直接下单吗？ | 不能，也不该能：只生成**内部采购申请单**，"人工确认"是显式确认点；真实下单要接 ERP 采购模块 | 不要说"已对接供应商" |
| 认证是原厂认证吗？ | 不是，是**内部授权**认证，证书备注固定写明"不代表设备原厂认证"；系统负责考核与记录，不代替原厂 | 千万别说"原厂认证" |
| 计划周期准不准？ | 周期值只来自手册原文（正则抽取 + 依据可点开）；手册没写的进"未覆盖"，不猜；生产片数口径由于拿不到产线速率，**到期日故意留空** | 不要说"AI 预测了最佳保养时间" |
| 三个人怎么协作不打架？ | 先冻契约再并行：状态/接口模型/数据表/错误码一次定死并只追加；三层依赖单向；每个功能都有独立测试文件 | 不要讲成"大家各写各的" |
| 这套东西能上生产吗？ | 能作为**试点**上：单机可跑、容器化产物齐、降级不挂；上生产前需要补权限分级、并发容量与真实语料重标 | 不要说"生产级、可直接承载并发" |
| 你个人做了什么？ | 如实说：RAG 检索生成链路与零幻觉校验、三块业务后端（含购物车）、四个独立前端页面、评测与缺陷修复、交接文档 | 不要夸大别人的部分，也不要回避"前端骨架是我先搭的、打磨在后" |
| 最大的不足是什么？ | 并发容量（0.11 req/s）与权限分级 —— 都在文档里如实记录并给了优化路径 | 不要说"没有不足" |

---

## 7. 讲解时的禁忌（容易踩的坑）

1. **不要说"100% 准确"**：可以说"引用覆盖率 100%""负样本拒答 100%"，这是两个**具体指标**，不是准确率承诺。
2. **不要把 G3 说成 100%**：宽松口径是 100%，严格口径是 **84%~90%**，说的时候要指明是哪个口径。
3. **不要拿单次最好值当结论**：延迟和 G3 都要给区间 + 机器负载说明。
4. **不要说"已对接真实 ERP / 供应商 / 原厂"**：全链路是示例数据 + 内部流程。
5. **不要说"原厂认证"**：只有"内部授权"。
6. **不要承诺 20 并发**：实测支撑不了，配套的优化路径已经写明。
7. **不要把"拒答"讲成故障**：这是设计目标，讲的时候主动点出来。
8. **不要隐藏遗留项**：主动说"权限分级、图片知识侧入库、并发容量还没做"，比被问出来强。
9. **别在演示中途改数据**：计划/订单/证书都是真实写库的，演示前想清楚顺序（可以先跑 §9 看库里的现状）。
10. **别一次讲两个观点**：每屏一个观点 + 一个动作 + 一个数字，讲完停一下等反应。

---

## 8. 收尾（30 秒）

> "总结一下：这套系统的核心不是'能回答'，而是**每条答案都能验收、没依据就拒答**；
> 在此基础上补了三块与问答**完全解耦**的业务 —— 维护计划、备件采购、考核认证，同样坚持'依据可溯源'。
> 现在 146 个单测全过、指标有报告可查；已知不足（并发容量、权限分级、图片知识入库）都写在交付文档里，
> 接手人看 `接手任务清单.md` 就能继续。"

**遗留项（主动说，别等问）**：

| 项 | 现状 | 下一步 |
| --- | --- | --- |
| 并发容量 | ≈0.11 req/s，支撑不了 20 并发 | 外部源查询与精排并行、GPU 部署、`rerank_max_candidates` 12→8 |
| 权限分级（FR-11） | `user_role` 透传未过滤，缺权限分级表 | 先定表再落地过滤 |
| 图片知识侧入库 | 抽图不写 caption，`kb_image` 0 行 | 接 VLM caption 后写表 |
| docx/pptx/xlsx | 上传 415（刻意，避免抓不到页码） | 接 MarkItDown |
| 会话删除/重命名 | 未实现 | 接口文档 P2 档已定义 |

---

## 9. 演示前 3 分钟自检

```bash
# 1) 服务是否就绪（7 个组件全 ok 才开讲）
curl -s localhost:8000/api/health | python3 -m json.tool | head -20

# 2) 前端 7 个页面是否都能打开（200 即可）
for p in /chat /history /admin /plans /parts /procurement /training; do
  printf "%-14s %s\n" "$p" "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:5173$p)"
done

# 3) 业务页要有可演示的数据（计划至少几条；订单/证书为空正好从零演示）
curl -s "localhost:8000/api/plans?limit=3" | python3 -m json.tool | head -30

# 4) 问答链路通不通（演示屏 1 用）
curl -s -X POST localhost:8000/api/chat/stream -H 'Content-Type: application/json' \
  -d '{"question":"腔体门 O-ring 多久更换一次？"}' | head -5
```

**演示前想清楚三件事**：

1. **顺序**：问答 → 拒答 → 计划 → 商城/采购 → 考核（从"主线能力"讲到"业务扩展"）。
2. **数据**：计划页要有数据（没有就点一次「生成并保存」）；采购与证书建议**从空开始**演示，
   这样"状态流转"和"不自动发证"才讲得清楚。
3. **兜底**：把 `使用手册.md` 和本文档的标签页开着；任何一个接口 4xx/5xx，
   都用"这是有意的保护/降级"这类**已知设计**来接，并翻到对应文档念一遍原因。
