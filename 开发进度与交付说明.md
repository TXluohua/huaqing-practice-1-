# 开发进度与交付说明

> **项目**：半导体设备维护知识库智能问答系统
> **当前阶段**：LangGraph 链路骨架（**节点与边未接入**）
> **对应提交**：`2cb0d4b` 「ttudde第二次提交」（12 files changed, +1478）
> **设计依据**：《半导体设备维护知识库智能问答系统_开发文档（初稿）(2).md》

---

## 1. 本次交付范围

**目标**：把 LangGraph 的链路打通 —— 不含任何业务节点、不含任何业务边。

**结论**：链路已打通并验证通过。compile → invoke → ainvoke → stream → 检查点 → 会话隔离
整条链路现在就能跑，且已被 7 个测试覆盖；九个业务节点与条件边留空待接入。

### 1.1 为什么没有节点和边也能"打通"

LangGraph 允许「**零节点 + START → END**」的图编译并执行，状态原样透传。
实测：

| 图形态 | 编译结果 |
| --- | --- |
| 零节点、零边 | ❌ 报错 `Graph must have an entrypoint` |
| 零节点 + `add_edge(START, END)` | ✅ 编译成功，`invoke` 正常，状态透传 |

因此整条链路可以在没有节点与业务边的前提下先行跑通、先行被测试覆盖 —— 这是本次交付成立的技术前提。

### 1.2 调用链现状

```
FastAPI（未实现）
  └─ services/chat_service.py（未实现）
       └─ agents/agent_factory.py ──── ainvoke() / astream()      ✅ 已实现
            └─ agents/graph.py ─────── compile_graph()            ✅ 已实现
                 └─ StateGraph(AgentState)   START → END          ⬜ 零节点（骨架）
                      └─ memory.py ─────── 检查点 + 会话映射        ✅ 已实现
```

---

## 2. 交付清单

### 2.1 LangGraph 骨架（8 个文件 / 1272 行）

| 文件 | 行数 | 职责 |
| --- | ---: | --- |
| `backend/agents/state.py` | 251 | **状态契约**。`AgentState` 按开发文档 §4.3 九个节点分组；`Evidence` / `Citation` / `ImageExtraction`；`timings` 与 `errors` 带 reducer 做累积合并 |
| `backend/agents/graph.py` | 152 | **主图拓扑**。`build_graph()` 无节点时连 `START → END`，有节点时按 `NODE_SEQUENCE` 自动串联；`compile_graph()`、`node_names()`、`has_nodes()` |
| `backend/agents/agent_factory.py` | 211 | **异步单例**。`get_agent()` / `ainvoke()` / `astream()` / `close_agent()` / `graph_summary()`；显式 `asyncio.Lock`，符合 §6.4「异步初始化不使用缓存装饰器」 |
| `backend/memory.py` | 204 | **会话记忆**。检查点逐级降级（异步 sqlite → 同步 sqlite → 内存）；`session_id ↔ thread_id` 映射（§5.6 的 session_map） |
| `backend/agents/nodes/__init__.py` | 110 | **节点注册表**。九个 `NodeName` 常量 + `NODE_SEQUENCE` + `register_node()`。**当前为空** —— 这就是"没有节点" |
| `tests/test_agents/test_graph.py` | 198 | 7 个链路测试；pytest 与 `python3 直接运行` 双入口 |
| `scripts/ask.py` | 106 | **命令行自检**。`--stream` / `--json` / `--session`，打印 trace_id、thread_id、检查点后端、已接入节点 |
| `backend/agents/__init__.py` | 40 | 包说明 + 契约类型导出 |

### 2.2 工程与环境（4 个文件 / 206 行）

| 文件 | 行数 | 说明 |
| --- | ---: | --- |
| `README.md` | 114 | 快速开始（venv 命令、预期输出）、接入节点写法、尚未实现清单 |
| `Makefile` | 34 | `make install / test / ask / ask-stream / venv / clean` |
| `requirements.txt` | 32 | 6 个依赖锁定版本；未接入组件按 §5.3 列在注释中 |
| `.gitignore` | 26 | `.venv/`、`__pycache__/`、`.env`、`backend/data/` |

> 上述 12 个文件在原脚手架中**均为 0 字节空文件**，本次为首次填充。

### 2.3 虚拟环境

- `.venv/`（Python 3.10.12），核心依赖：langgraph 1.2.11、**langgraph-checkpoint-sqlite 3.1.1**、langchain-core 1.6.3、pydantic 2.13.5、aiosqlite 0.22.1、pytest 9.1.1
- `backend/data/sqlite/checkpoints.sqlite`（运行时产物，已被 .gitignore 排除）

---

## 3. 如何运行

```bash
make install        # 建虚拟环境 + 安装锁定版本依赖
make test           # 7 个链路测试
make ask Q="刻蚀机真空度异常怎么排查？"
make ask-stream Q="参数下限是多少"
```

或手动：

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt
source .venv/bin/activate        # 可选，激活后直接用 python / pytest
```

### 3.1 预期输出（骨架阶段）

```
检查点后端  : sqlite-async（落盘=True）
已接入节点  : 无（骨架阶段，START -> END 直连）
thread_id   : thread-venv-demo
状态        : NOT_COVERED  置信度=0.0
答案        : （空 —— 尚未接入 generate 节点）
引用数      : 0
```

`answer` 为空、`status` 为 `NOT_COVERED` 属于**预期结果**，不是故障。

---

## 4. 验证记录

以下均为**实际执行过**的命令与结果，非设计承诺：

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 链路测试 | `make test` | ✅ 7 passed（pytest 与直接运行两种方式均通过） |
| 阻塞式链路 | `make ask` | ✅ sqlite-async / 落盘=True / 12.6ms |
| 流式链路 | `make ask-stream` | ✅ 收到 1 个 chunk（stream_mode=values） |
| 跨进程持久化 | 进程 A 写入 → 进程 B 读取 | ✅ 进程 B 读到了进程 A 写入的 state |
| 虚拟环境激活 | `source .venv/bin/activate` 后跑 pytest | ✅ 7 passed |
| 清理 | `make clean` | ✅ 不误删 .venv |
| 依赖可复现 | `pip install -r requirements.txt` | ✅ 幂等，无缺失 |

---

## 5. 三个关键设计决定

| # | 决定 | 理由 |
| --- | --- | --- |
| 1 | 初始 `status` 取 **`NOT_COVERED`**（fail-closed），而非 `OK` | 依 §1.4「零幻觉优先」：`verify` 节点判定之前任何答案都不可信。**忘记赋值时默认拒答，而不是默认放行**。文档 §5.4 的取值集合未做改动 |
| 2 | `memory.py` 检查点**逐级降级**，后端不可用不中断主链路 | 对齐文档 R7「MCP 工具加载失败影响主链路」的同一原则；并用 `graph_summary()` 暴露实际后端供 `/api/health` 使用 |
| 3 | `node_names()` 按 `NODE_SEQUENCE` 顺序返回，而非字母序 | 直接反映链路走向，便于接线自检与排障 |

---

## 6. 状态契约速查

`AgentState` 字段按开发文档 §4.3 的节点分组，节点只需返回自己产出的字段（增量更新）：

| 节点 | 产出字段 |
| --- | --- |
| `ingest_image` | `image_result` |
| `rewrite` | `queries` |
| `retrieve` | `candidates` |
| `rerank` | `ranked` / `top_score` |
| `build_context` | `context` / `context_blocks` |
| `augment` | `evidence_sufficient` / `augment_rounds` / `tool_calls` |
| `generate` | `answer` / `answer_structured` |
| `verify` | `citations` / `confidence` / `confidence_label` / `status` / `uncertain` |
| `respond` | `response` |
| 横切 | `trace_id` / `timings`（reducer 合并）/ `errors`（reducer 追加） |

> `backend/agents/state.py` 属**契约级文件**（§6.3）：字段变更需同步所有节点。

---

## 7. 如何接入节点

节点写在 `backend/agents/nodes/` 下并注册即可自动进入链路：

```python
# backend/agents/nodes/retrieval.py
from ..state import AgentState

async def rewrite(state: AgentState) -> dict:
    """问题 → 2~3 路检索式"""
    return {"queries": [state["question"]]}
```

```python
# 注册（可在同模块底部，或 nodes/__init__.py 中）
from backend.agents.nodes import NodeName, register_node
register_node(NodeName.REWRITE, rewrite)
```

接入顺序由 `NODE_SEQUENCE` 决定。真实拓扑含一处条件分支
（「证据是否足够?」→ 是否走 `augment`），需在 `graph.build_graph()` 中改用
`add_conditional_edges` —— 该处**已标 TODO**，注释里给出了完整示例。

---

## 8. 尚未实现

本次**未触碰**以下内容，仓库中 **79 / 92 个跟踪文件仍为 0 字节**：

- **HTTP / SSE 接入层**：`main.py`、`routers/`、`services/`、`schemas.py`、`setting.py`、`db.py`、`dependency.py`
- **九个业务节点**：`nodes/retrieval.py`、`nodes/generation.py`、`nodes/multimodal.py`
- **提示词**：`prompts/` 下 4 个 `.md` 文件
- **RAG 链路**：`rag/` 全部（ingest / store / retriever / citation）、`tools/`、`config/glossary.yaml`
- **MCP**：`mcp_client.py`、`mcp_servers/ticket_server.py`
- **前端**：`frontend/` 全部
- **部署**：`docker-compose.yml`、`deploy/`、`.env.example`

另：**未对 Git 远程做任何操作** —— 未创建 `develop` 分支、未推送、未建 CODEOWNERS。

---

## 9. 协作提示（提要）

两人并行开发时，本次骨架的价值在于**消除相互等待**：

- 平台侧可对着 `agent_factory.astream()` 写 SSE，**无需等节点完成**；
  节点接入后同一端点自动输出真实答案，SSE 代码无需改动。
- RAG 侧用 `make test` + `scripts/ask.py` 即可独立调节点，无需 HTTP 层。
- 双方的接口是 `state.py`，**不是彼此的代码**。

需重点约定所有权的冲突热点文件：`state.py`（契约级，只追加不改名）、
`requirements.txt`（按段追加，严禁整文件排序）、`setting.py`、`schemas.py`。

> 完整的两人分工方案与 GitHub 协作规范（分支模型、PR 流程、CODEOWNERS）另行成文。

---

## 10. 变更记录

| 提交 | 内容 |
| --- | --- |
| `2cb0d4b` | 本次交付：LangGraph 链路骨架 + 虚拟环境 + 工程配置（12 files, +1478） |

---

**维护约定**：本文档描述的是**当前阶段**的交付状态。节点接入后，
第 1、3、4、8 节需同步更新。
