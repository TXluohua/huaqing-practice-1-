# 前端对接说明（三块新业务）

> 适用版本：后端 `feature-txluohua-dev` 分支（三块新业务后端已完成）
> 对接对象：**第三人（前端）**
> 接口权威定义见 `接口文档.md` §10；本文件只讲**前端怎么用**：请求体、响应体、
> 按钮该不该点、空数据与错误怎么显示、演示脚本怎么走。

---

## 0. 先读这一节：这三块**与问答完全独立**

> 项目方要求：**不要把这几个业务和问答绑一起，功能页面必须独立**。这条已经落地成三层保证，
> 不是文档约定：

| 层 | 保证 | 怎么验证 |
| --- | --- | --- |
| 接口层 | 三块业务各有自己的路径前缀 `/api/plans`、`/api/parts`、`/api/training`，**与 `/api/chat` 完全不相交**；不依赖会话、`qa_id`、SSE | `pytest tests/test_api/test_module_independence.py`（7 项，含「业务与问答路径不得相交」「业务响应必须是 JSON 而非 SSE」） |
| 代码层 | 问答链路（`chat_service`、LangGraph 图、`routers/chat.py`）**不 import** 三块业务；三块业务**不 import** 问答链路 | 同上（测试用 AST 解析 import，不是字符串匹配） |
| 页面层 | 四个独立路由 `/plans`、`/parts`、`/procurement`、`/training`，顶栏与问答组**用竖线分隔的两组入口** | 见 §0.2 |

**共享的只有「数据契约」与「知识库读取」**：引用项类型（`Citation`）、`json` 响应体、
以及从知识库检索原文作为依据的能力（计划周期、考题依据都必须来自手册原文）。
共享这两样不是「绑一起」——它们是三块业务与问答**各自的**零幻觉基础。

### 0.1 问答能力 ≠ 这三块业务

容易混的两处，请分清楚：

| 问答侧（保留在 `/chat`） | 独立业务（本文档） |
| --- | --- |
| 用户问「O-ring 有没有库存」→ 聊天回答并给引用 | 备件商城页浏览台账、加入购物车、走采购流程 |
| `mode=training` 的**分层讲解**（把答案讲给新人听） | 考核认证页：出题 → 答题 → 判分 → 发证 → 到期提醒 |

问答侧的这两条是「**问答**能力」，不承担业务操作（下单、发证、改计划）；
业务页也不会去调问答接口。两边各自演进，互不影响。

### 0.2 前端怎么保证独立（已实现的骨架）

- 路由：`frontend/src/router/index.ts` 里业务四条路由带 `meta.group = 'biz'`；
- 导航：`App.vue` 顶栏分成两组（问答组 / 业务组），中间一条竖线；
- API：`frontend/src/api/plans.ts`、`parts.ts`、`training.ts`，**只依赖 `@/api/http`**，
  共享类型从 `@/api/contracts.ts` 取（原来 `Citation` 定义在 `api/chat.ts`，已抽出来，
  避免业务模块被迫 import 问答模块）；
- 页面：`PlanView.vue`、`PartsView.vue`、`ProcurementView.vue`、`TrainingView.vue`，
  **任何业务页面都不 import `@/api/chat`、`@/utils/sse`、`@/stores/chat`、`@/stores/session`**。

> 提交前如果想要一条命令确认没有越界：
> ```bash
> cd frontend && grep -rn "@/api/chat\|@/utils/sse\|@/stores/" src/views/PlanView.vue src/views/PartsView.vue src/views/ProcurementView.vue src/views/TrainingView.vue
> # 无输出 = 业务页面与问答零耦合
> ```

---

## 0.5 三块功能是什么（一句话）

| 功能 | 一句话 | 页面建议 |
| --- | --- | --- |
| 维护计划生成 | 输入设备型号 + 运行数据，后端从手册里抽周期，算出「哪些维护项快到期了」 | 一个页面：上方表单 → 下方计划表 |
| 备件商城 + 采购结算 | 查库存/价格/替代件 → 建采购申请单 → 人工确认 → 收货 → 记账 | 两个 Tab：商城 / 我的申请单（含结算台账） |
| 考核认证 | 按设备出题 → 答题 → 判分 → 显式发证 → 到期提醒 | 两个 Tab：考核 / 认证记录 |

**三条必须在前端体现的业务边界**（不是可选项，写进 UI 文案）：

1. 采购**不对供应商发起真实下单**——按钮文案写「提交采购申请」，确认环节写「人工确认」，
   不要出现「一键下单/在线支付」字样；
2. **不自动发证**——判分通过后只提示「可申请认证」，发证必须用户再点一次；
3. 计划项/题目**必有依据**——每条都要能点开看「依据（手册章节）」，
   没有依据的条目后端不会返回（会进 `uncovered` / 直接不命题），前端不要自己补。

---

## 1. 通用约定

- 基地址：`/api`（Vite 已配代理，前端直接请求相对路径 `/api/...` 即可）
- 所有 GET 列表都支持 `limit` / `offset`，响应统一带 `total`
- 所有响应都带 `trace_id`（排障时让后端拿它捞日志）
- **错误体统一为**：

```json
{
  "code": "INVALID_STATE",
  "message": "当前状态不允许该操作：approved",
  "detail": { "order_id": 12, "status": "approved" },
  "trace_id": "01J..."
}
```

前端只需读 `code` 分流文案、读 `message` 直接展示（**后端 message 已是中文**，无需前端翻译）。

三块功能可能出现的错误码：

| 错误码 | HTTP | 前端该做什么 |
| --- | --- | --- |
| `SERVICE_NOT_READY` | 503 | 显示「该功能暂不可用」，不要白屏 |
| `PART_NOT_FOUND` | 400 | 提示该备件编码不存在，清掉这一行 |
| `SUBSTITUTE_BASIS_REQUIRED` | 400 | 提示「替代件必须有兼容性依据，需原厂确认」，禁用提交 |
| `INVALID_STATE` | 409 | 刷新订单详情（按钮状态过期了） |
| `REASON_REQUIRED` | 400 | 驳回/取消未填原因，聚焦到原因输入框 |
| `ALREADY_SETTLED` | 409 | 提示已结算，跳结算台账 |
| `ITEM_NOT_FOUND` | 404 | 购物车里没有该备件，刷新购物车 |
| `EMPTY_ORDER` | 400 | 购物车为空不能提交，提示「请先加入备件」 |
| `QUIZ_GENERATION_FAILED` | 422 | 提示「该主题抽不到有依据的题目，换个主题」 |
| `QUIZ_EMPTY` | 422 | 试卷无题目（异常数据），提示重新出题 |
| `ATTEMPT_NOT_PASSED` | 400 | 提示「认证必须基于一次通过的考核」，禁用发证按钮 |
| `ATTEMPT_NOT_FOUND` | 404 | 考核记录不存在（换了浏览器/库被清），重新考试 |
| `DB_UNAVAILABLE` | 503 | 数据库不可用，提示稍后再试 |
| `KB_UNAVAILABLE` | 503 | 知识库检索全失败（生成计划时），提示「知识库暂不可用」 |
| `PLAN_NOT_FOUND` | 404 | 计划项不存在（被删/过期数据），刷新列表 |
| `ORDER_NOT_FOUND` | 404 | 采购申请单不存在，返回列表页 |
| `QUIZ_NOT_FOUND` | 404 | 试卷不存在，重新出题 |

---

## 2. 维护计划生成

### 2.1 生成（POST `/api/plans/generate`）

请求：

```json
{
  "device_model": "Etcher-A",
  "device_code": "ETCH-01",
  "runtime_hours": 5200,
  "wafer_count": 46000,
  "hours_since_pm": 4200,
  "wafers_since_pm": 36000,
  "top_k": 8,
  "persist": true
}
```

> `persist: false` = **只预览不落库**，用于「先生成看看」的按钮。

响应（关键字段）：

```json
{
  "device_model": "Etcher-A",
  "device_code": "ETCH-01",
  "items": [
    {
      "id": 31,
      "item_name": "干泵排气过滤器更换",
      "item_type": "耗材更换",
      "cycle_basis": "hours",
      "cycle_value": 4000.0,
      "baseline_value": 0.0,
      "current_value": 4200.0,
      "remaining": -200.0,
      "due_at": "2026-09-20T03:45:20+00:00",
      "status": "due",
      "evidence": [
        { "id": 1, "source_type": "kb_doc", "doc": "刻蚀设备维护手册", "version": "V3.2",
          "section": "3.5 真空泵维护", "page": 12, "chunk_id": "c_5ee7bc788b6b" }
      ],
      "note": "依据原文：每 4000 运行小时更换干泵排气过滤器"
    }
  ],
  "uncovered": ["静电吸盘（ESC）表面再生"],
  "trace_id": "01J..."
}
```

前端渲染要点：

- `status` 取值：`planned`（未到期）/ `due`（**已到期或超期，红色**）/
  `done`（已完成）/ `skipped`（已跳过）；
- `remaining` 为距离到期还差的计量单位，`remaining <= 0` 一定对应 `status="due"`，
  文案用「已超期 N 单位」而不是负数；`remaining > 0` 就是「还剩 N 单位」；
- 每个条目右上角放「依据」图标 → 悬浮/抽屉展示 `evidence`（文档名 + 版本 + 章节 + 页码）；
- **`uncovered` 必须显示**：文案「以下项目手册未给出周期，需人工确认」+
  逐条列出。元素格式是 `名称：原因`（成品字符串，直接显示即可），原因有三类：
  「知识库只给出执行频次…」「知识库提到该维护项，但没有可换算的周期数值」「知识库未收录该维护项」。
  这是诚实性设计，不是 bug，别隐藏；
- 每条还带 `created_at` / `updated_at` / `completed_at`（UTC ISO 字符串），
  `completed_at` 只在完成或跳过后有值 —— 想显示「上次完成时间」直接用这个字段，不要从 `note` 里抠；
- `cycle_basis` 取值与 `due_at` 口径（**前端只展示，不要自己算**）：

  | `cycle_basis` | 含义 | `due_at` 怎么来的 |
  | --- | --- | --- |
  | `hours` | 运行小时 | `now + max(0, remaining) / 24 天`（按 **24 h 连续运行**折算）；已超期时为当天 |
  | `wafers` | 生产片数 | **恒为 `null`**（手册不给产线速率，后端不猜）→ 显示「按片数触发」，到期时间留空 |
  | `days` | 日历天 | `now + cycle_value 天` |

  > ⚠️ `due_at` 为 `null` 是**正常且刻意的**（片数口径没有速率、按需执行），
  > 不要在前端按平均值补一个日期。`remaining` 仍然有意义（还剩多少片 / 多少次）。

### 2.2 列表 / 完成 / 跳过

| 动作 | 请求 | 说明 |
| --- | --- | --- |
| 列表 | `GET /api/plans?device_model=Etcher-A&status=due&limit=20` | 后端按 `remaining` 升序（最紧急在前），前端不要再排；`status` 只接受 `planned/due/done/skipped`，传别的会 400 |
| 完成 | `POST /api/plans/{id}/complete` body `{"note":"已更换","completed_value":36000}` | 完成后自动滚到下一周期 |
| 跳过 | `POST /api/plans/{id}/skip` body `{"note":"设备停机检修，本次跳过"}` | `note` 可选；不填会记为「已跳过（未填写原因）」，建议在 UI 上要求填写 |

---

## 3. 备件商城与采购

### 3.1 商城目录

- `GET /api/parts/catalog?device_model=Etcher-A&keyword=O-ring&limit=20`
- `GET /api/parts/{code}`（详情；注意**这个路由声明在最后**，前端不用关心）

单个备件响应：

```json
{
  "code": "SP-ETA-0101",
  "part": "腔体门 O-ring",
  "spec": "Φ220×5.3 FFKM",
  "stock": 12,
  "unit": "个",
  "stock_status": "ok",
  "lead_time_days": 3,
  "price_cny": 185.0,
  "supplier": "示例供应商-密封件",
  "substitutes": [
    { "code": "SP-ETA-0101F", "part": "腔体门 O-ring（FFKM 全氟醚）",
      "stock": 4, "price_cny": 560.0, "lead_time_days": 7, "basis": "厂商兼容性说明 §2" }
  ],
  "forbidden": ["禁止替代：NBR 丁腈橡胶 O-ring —— 不耐等离子，见备件目录 V1.4 §1.3"]
}
```

> `GET /api/parts/{code}` 是唯一**不带 `trace_id`** 的接口（`PartItem` 契约里没有该字段），
> 前端不要读它。`forbidden` 的每一条都是「禁止替代：A —— 原因」这种**成品句子**，
> 直接展示即可，不要前端再拼格式。

渲染要点：

- `stock_status` 只有两个取值：`ok`（正常）/ `low`（低于 `parts_low_stock` 阈值，黄色）；
  **缺货判断用 `stock == 0`**（此时也是 `low`），显示红色 + `lead_time_days` 到货预期；
- `substitutes` 里的每一项**都可能点「加入购物车」**，请求里带 `is_substitute: true` 即可
  （依据由后端从台账取，见 §3.2 与 §3.3）；
- `forbidden` 是**禁止替代清单**，必须用红色警示展示（例如「禁止用 NBR 替代：不耐等离子」），
  这是安全信息，不能折叠隐藏。

### 3.2 购物车（可选，但**推荐用后端购物车**）

「购物车」在后端就是一张 **`draft` 状态的采购申请单**（不额外建表），所以你可以二选一：

| 方案 | 怎么做 | 取舍 |
| --- | --- | --- |
| A. 后端购物车（推荐） | 进商城页时可以**直接建一张空车**（`POST /api/parts/orders` 的 `items` 允许为空；后端 2026-09-20 起放开），也可以复用 `GET /parts/orders?status=draft` 找到的那张草稿单，加/改/删都打接口 | 刷新不丢、金额由后端算（不会被前端算错），还能看到审计痕迹 |
| B. 前端本地购物车 | 购物车放在 `pinia`/`localStorage`，结算时一次性 `POST /api/parts/orders` | 少两个接口调用，但刷新/换设备会丢，金额要自己算 |

接口（均在 `draft` 状态可用）：

```http
POST   /api/parts/orders/{order_id}/items          # 加入购物车
{ "code": "SP-ETA-0101F", "qty": 2, "is_substitute": true, "operator": "张三" }

PATCH  /api/parts/orders/{order_id}/items/{code}   # 改数量（1~999）
{ "qty": 6, "operator": "张三" }

DELETE /api/parts/orders/{order_id}/items/{code}   # 移出购物车
```

三个接口都返回**整张订单**（`PartOrderResponse`，含最新 `items` 与 `total_amount`），
前端直接用它覆盖本地状态即可，**不要自己累加金额**。

要点：

- 同编码重复加入 -> **数量累加**（上限 999），不会出现两行同样备件；
- 单价与库存由后端回台账取，请求里**不要传价格**（传了也会被忽略）；
- 替代件同样要带 `is_substitute: true` 并受依据约束（无依据 400 `SUBSTITUTE_BASIS_REQUIRED`）；
- 移出最后一条 -> 购物车为空（正常状态），但**此时不能提交**，提交会 400 `EMPTY_ORDER`；
- 空车是允许存在的（甚至可以一进页面就建空车），只有「提交」这一步要求非空；
- 提交（`submit`）之后明细**冻结**：再加/改/删一律 409 `INVALID_STATE` —— UI 上应在
  提交后把「改数量 / 移出」按钮全部置灰（`allowed_actions` 里此时也没有它们）；
- 每个动作都会往订单 `note` 追加一行审计，带上 `operator` 更好排查。

### 3.3 创建采购申请单（POST `/api/parts/orders`）

```json
{
  "items": [
    { "code": "SP-ETA-0101", "qty": 4 },
    { "code": "SP-ETA-0101F", "qty": 2, "is_substitute": true }
  ],
  "device_model": "Etcher-A",
  "purpose": "ETCH-01 月度保养（工单 WO-2311）",
  "applicant": "张三",
  "note": "O-ring 已到周期，同步备一套全氟醚件"
}
```

- `is_substitute: true` 时**后端会去台账取兼容性依据**，取不到直接 400
  `SUBSTITUTE_BASIS_REQUIRED`；前端只要把商城里那项带 `basis` 的替代件原样传 `code` 即可，
  **不要自己编依据**。

### 3.4 状态机与按钮（关键）

```
draft ──submit──► submitted ──approve──► approved ──receive──► received ──settle──►（结算台账）
  │                   │                     │
  └──cancel──►cancelled└──cancel──►cancelled └──reject──► rejected
                      └──reject──► rejected
```

**不要在前端硬编码这个状态机**——直接用响应里的 `allowed_actions` 渲染按钮：

```json
{ "id": 12, "order_no": "PO-20260920-001", "status": "submitted",
  "total_amount": 1860.0, "currency": "CNY",
  "allowed_actions": ["approve", "reject", "cancel"], "trace_id": "01J..." }
```

各状态的 `allowed_actions`（后端固化为唯一依据）：

| `status` | `allowed_actions` |
| --- | --- |
| `draft` | `["submit", "cancel"]` |
| `submitted` | `["approve", "reject", "cancel"]` |
| `approved` | `["receive"]` |
| `received` | `["settle"]` |
| `rejected` / `cancelled` | `[]`（终态，只读） |

| `allowed_actions` 项 | 按钮文案 | 调用 |
| --- | --- | --- |
| `submit` | 提交申请 | POST `/api/parts/orders/{id}/submit` |
| `approve` | **人工确认** | POST `…/approve` |
| `reject` | 驳回 | POST `…/reject`（`reason` 必填） |
| `cancel` | 撤销申请 | POST `…/reject`，**`reason` 用 `cancel: 原因` 前缀** |
| `receive` | 确认收货 | POST `…/receive` |
| `settle` | 登记结算 | POST `…/settle` |

> `reject` 与 `cancel` 复用同一个端点：后端按 `reason` 是否以 `cancel:` / `取消：`
> 开头来区分意图（避免为取消单独加接口）。前端做撤销时务必带上这个前缀，
> 否则会被当成驳回（`approved` 状态下驳回会成功，而取消不会）。

冲突处理：任何动作返回 409 `INVALID_STATE` → 自动重取订单详情并提示「状态已变更，请重试」。

### 3.5 结算

- `POST /api/parts/orders/{id}/settle` body：

```json
{ "amount": 1860.0, "method": "月结", "invoice_no": "INV-20260920-77", "operator": "李四" }
```

   `amount` 省略则用订单金额；**同一订单重复结算返回 409 `ALREADY_SETTLED`**（幂等保护）。
   > 后端允许 `approved`（先结算后收货）与 `received` 两种状态结算，但按钮只按
   > `allowed_actions` 渲染 → 界面上结算按钮在**收货之后**出现，这是刻意收紧的默认路径；
   > 若业务要求「收货前也能结算」，告诉后端把 `approved` 的 `allowed_actions` 加上 `settle` 即可。
- `GET /api/parts/settlements?limit=20` → `{ items, total, total_amount, trace_id }`，
  顶部卡片直接显示 `total_amount`（后台汇总好的，前端不用再累加）。
- 文案注意：结算**只登记台账，不做财务过账**，详情页写「已登记结算，财务以 ERP 为准」。

---

## 4. 考核认证

### 4.1 出题（POST `/api/training/quizzes`）

```json
{ "device_model": "Etcher-A", "topic": "真空系统", "level": "basic",
  "n_items": 5, "question_types": ["single", "multiple", "judgement", "short"] }
```

响应：

```json
{
  "id": 7, "device_model": "Etcher-A", "topic": "真空系统", "level": "basic",
  "n_items": 5, "generator": "llm",
  "items": [
    { "no": 1, "question": "腔体真空度异常时，第一步应检查什么？",
      "type": "single", "options": ["干泵运行状态", "冷却水温度", "射频功率", "机械手位置"],
      "answer": 0, "explanation": "手册 4.1 要求先确认干泵与前级阀状态",
      "evidence": [{ "doc": "Etcher-A 设备维护手册", "version": "V2.1",
                     "section": "4.1 真空度异常排查", "page": 18 }] }
  ],
  "trace_id": "01J..."
}
```

渲染要点：

- `generator` 是 `extractive-fallback` 时说明**未配置文本模型，走确定性抽句出题**，
  功能正常，前端不用提示异常（可选：在角落显示「离线出题」）；
- 每题都要能展开「依据」；
- 422 `QUIZ_GENERATION_FAILED` → 提示换主题/设备，不要循环重试。

### 4.2 答题与交卷

答题状态：`answers` 数组**按题目顺序**，格式随 `type` 变：

| `type` | 提交格式 | 判分 |
| --- | --- | --- |
| `single` | 选项下标（数字），如 `0` | 下标相等 |
| `multiple` | 下标数组，如 `[0, 2]` | 集合**完全一致**才算对（多选/少选都不给分） |
| `judgement` | 布尔，`true` / `false` | 布尔相等 |
| `short` | 字符串 | 要点覆盖：逐要点算字符二元组重合 ≥0.5 记命中，命中率 ≥0.6 算对 |

> 题型名是 `single` / `multiple` / `judgement` / `short`（**不是 `multi` / `judge`**），
> 传错后端直接 400（不会静默换成别的题型）。`question_types` 省略时默认
> `["single", "judgement"]`。

`POST /api/training/quizzes/{quiz_id}/submit`：

```json
{ "trainee": "张三", "answers": [0, [0,2], true, "先确认干泵与前级阀"], "duration_s": 226.5 }
```

响应：

```json
{
  "attempt_id": 19, "quiz_id": 7, "trainee": "张三",
  "score": 80.0, "passed": true, "pass_line": 60.0,
  "detail": [
    { "no": 1, "correct": true, "expected": 0, "got": 0,
      "explanation": "…", "evidence": [ { "doc": "…", "section": "…", "page": 18 } ] }
  ],
  "certification_id": null,
  "trace_id": "01J..."
}
```

> **`certification_id` 恒为 `null`**（后端不自动发证）。通过时前端展示
> 「已通过，可申请认证」按钮 → 调 4.3。

### 4.3 发证与到期提醒

```json
POST /api/training/certifications
{ "trainee": "张三", "device_model": "Etcher-A", "level": "L2",
  "attempt_id": 19, "issuer": "内部授权", "valid_days": 365 }
```

- `attempt_id` 必须是**一次通过的考核**，否则 400 `ATTEMPT_NOT_PASSED`；
- **系统不代表原厂签发**，`issuer` 默认「内部授权」（后端还会在 `note` 里固定标注
  「不代表设备原厂认证；到期前请复训并重新考核」），前端不要显示「原厂认证」；
- `GET /api/training/certifications?trainee=张三` 列表；响应含
  `status`（`valid` / `expiring` / `expired`）与 `days_to_expiry`；
- `GET /api/training/certifications/expiring?days=30` 到期提醒（含已过期），
  适合做成首页红点 / 看板卡片。

---

## 5. 演示脚本（建议按这个顺序录屏）

1. **计划**：`Etcher-A / 5200h / 46000 片` → 生成 → 展开一条的依据 → 点「完成」→ 刷新看周期滚动；
2. **商城 + 购物车**：搜 `O-ring` → 看到库存 12、价格 185、替代件 FFKM →
   加 4 个主件 + 2 个替代件（车里有 2 行、合计 1860）→ 把主件改成 6（合计变 3910）→ 移出主件（只剩 2800）；
3. **采购**：提交（先演示空车提交被拦 `EMPTY_ORDER`）→ 切到管理员身份「人工确认」→ 收货 →
   登记结算 → 台账汇总金额变化 → 回到订单页确认「改数量 / 移出」按钮已置灰；
4. **考核**：按「Etcher-A / 真空系统」出 5 题 → 答题交卷 → 80 分通过 → 申请 L2 认证 → 到期提醒里能看到。

---

## 6. 联调清单（前端自测用）

- [ ] 三个页面在 `SERVICE_NOT_READY`（未配密钥/未建索引）时都不白屏
- [ ] 计划页 `uncovered` 有内容时能正常显示
- [ ] 替代件不带依据提交 → 前端能读懂 `SUBSTITUTE_BASIS_REQUIRED` 并给出人话提示
- [ ] 订单按钮完全由 `allowed_actions` 驱动（把某单改成 `approved` 后按钮应立刻变化）
- [ ] 购物车：同编码重复加入是**数量累加**（不是两行）；合计金额与后端返回的 `total_amount` 一致
- [ ] 购物车：清空后点提交得到 `EMPTY_ORDER` 人话提示，而不是提交成功
- [ ] 购物车：提交成功后「改数量 / 移出」按钮全部消失或置灰（明细已冻结）
- [ ] 重复点「登记结算」第二次出现 `ALREADY_SETTLED` 提示而不是新增一条
- [ ] 交卷后**没有**自动发证；认证记录页在点击「申请认证」后才多一条
- [ ] 所有时间字段（`created_at` / `due_at` / `issued_at`）按接口原样显示即可，不要自行换算时区

---

## 7. 还有什么要后端配合的

- 需要新增字段 / 新增筛选条件 → 直接提，后端按 `schemas.py` 追加（**只增不改**，不会破坏现有前端）；
- 会话删除 / 重命名接口（原 T9）仍待补，与这三块无关；
- 前端联调时如遇 500，把响应里的 `trace_id` 发给后端，可在 `data/logs` 里定位到具体节点。
