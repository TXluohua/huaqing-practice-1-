# 功能完成度清单

> **性质**：全项目功能的**已完成 / 未完成**对照清单，逐项附实测证据
> **基线**：`main` 分支当前工作区（含《平台侧待办与验收清单》全部实施）
> **依据**：**逐项实测**（真起服务、真调接口、真跑评测），不是读代码推断
> **口径**：只列「能证明的状态」，推断出来的不写
> **配套**：开发文档 · `接口文档.md` · `平台侧待办与验收清单.md` · `RAG实现说明.md`

---

## 0. 先说结论

| 维度 | 完成度 | 说明 |
| --- | --- | --- |
| 工程结构（开发文档 §6.2 目录树） | **100%** | 92 个跟踪文件中仅 2 个为空（均为包标记 `__init__.py`，属正常） |
| 业务功能 FR-01 ~ FR-10 | **8 完成 / 2 部分** | FR-02 图片知识侧、FR-07 归因汇总未做 |
| 非功能 NFR-01 ~ NFR-07 | **5 达成 / 2 未达** | NFR-02 已测：**20 并发不能支撑**（P95 190s，见缺陷 §2.3b）；NFR-03 权限过滤规则未定义 |
| 验收指标 G1 ~ G5 | **5 达成** | RAG 侧修复后复测：G1 100% / G2 覆盖率 100% 且 0 伪造 / G3 严格要点 89.7% / G4 图片 5-5 / G5 达标；误拒正样本 0/40（见 §7 与 README 实测指标表） |
| 里程碑 D1 ~ D8 | **D8 已完成**（收尾评测 2026-09-18） | 黄金集 50 条、正式报告、badcase 归因、演示走查、并发压测全部产出 |
| 接口 | **32 / 32 实现** | 原 11 个 + 三块新业务 21 个；端到端断言全过（新接口见 §8） |
| 测试 | **125 passed** | RAG 侧修复 + 三块新业务落地后复跑确认 |

**一句话**：**代码层面已交付**；收尾评测暴露的 5 条 RAG 侧缺陷（#11~#15）**已全部修复并复测**（§7），
项目方追加的**三块新业务后端已补齐**（§8）；剩余工作集中在**这三块的前端页面**（交第三人）
与既有已知限制（图片知识侧入库、权限过滤、并发吞吐）。

---

## 1. 已完成（实测通过）

### 1.1 架构与链路

| 能力 | 实测证据 |
| --- | --- |
| 三层架构全通 | 接入层（Vue3）→ 编排层（FastAPI + LangGraph）→ 工具与数据层（BM25/向量/精排/MCP） |
| LangGraph 主图 | **9 个节点全部注册** + **条件边已激活**（`build_context → augment \| generate`），与开发文档 §4.3 一致 |
| 跨源检索 | 文档源 + **工单源（MCP）**：引用里同时出现 `source_type=ticket` 与 `kb_doc` |
| 启动预热 | 实测 1.9~2.2s（hashing）/ 约 10s（真实模型）；任一失败不阻塞启动，health 如实报 degraded |
| 降级不中断 | 模型 / MCP / 数据库任一不可用都不阻止启动；MCP server 起不来时降级为空工具，主链路照常 |

### 1.2 接口（11 个，全部实现）

| 方法 | 路径 | 实测要点 |
| --- | --- | --- |
| GET | `/api/health` | **7 个组件全绿 status=ok**（图 / 文本模型 / 图片模型 / 数据库 / 向量库 / MCP / 预热） |
| POST | `/api/upload/image` | 413(>5MB) / 415(非图片) / 400(过小、空文件)；4000×3000 自动压到 1600×1200；静态可访问 |
| POST | `/api/chat/stream` | 事件顺序 `meta → image? → token* → citations → done`；`done` 带 `qa_id`；未知会话回 `error` 事件 |
| GET | `/api/chat/sessions` | 分页 / 关键词搜索 / message_count / last_question；limit 越界 400 |
| GET | `/api/chat/sessions/{id}/messages` | user+assistant 成对返回，含引用与置信度；未知会话 404 |
| POST | `/api/feedback` | 四态全通；`correct` 缺正文 400；未知 qa_id 404；非法 type 400；**驳回实际写入 badcases.md** |
| POST | `/api/admin/documents` | 202 + job；入库成功 `{"chunks":7,"pages":7}`；同版本 409；docx 415（提示 MarkItDown） |
| POST | `/api/admin/index/rebuild` | 202 + job；`{"documents":5,"chunks":32}` |
| GET | `/api/admin/jobs/{job_id}` | 进度 0~1 + message + result；不存在 404 |
| GET | `/api/admin/chunks` | 32 片；page / version / doc 字段齐全 |
| GET | `/api/admin/questions/top` | 同问题不同标点**归并计数**（实测 4 次） |

### 1.3 平台侧节点与工具

| 能力 | 实测证据 |
| --- | --- |
| `ingest_image` 节点 | 无图片时**零开销**（返回 `{}`）；识别失败明确提示，不猜测 |
| 视觉识别（真实 VLM） | 报警图 → `image_type=alarm_screen`，提取到 `alarm_codes:["E-2041"]`、`device_model:Etcher-A`；`image` 事件先于 `token` |
| 识别失败话术 | 模糊手写图 → `uncertain` 出现「图片未能识别，请用文字描述现象」 |
| `augment` 节点 | 硬边界由代码强制（≤2 轮 / ≤3 次调用 / 15s / 白名单）；外部源优先分支 |
| 备件查询（FR-10） | 答案含库存 12 个 / 库位 A-03 / 到货 3 天 / FFKM 替代件依据；查不到时输出「需原厂确认」 |
| 工单检索（FR-03） | MCP stdio server 可独立调用；返回经解包后拿到结构化字段 |
| `respond` 节点 | 安全前置提示（LOTO）与图片未识别提示统一走 `uncertain`，不改已冻结的 SSE 契约 |
| 工具聚合 | 本地 7 个 + MCP 1 个；白名单 = 注册表 ∩ setting；越权调用抛 PermissionError |

### 1.4 数据与持久化

| 能力 | 实测证据 |
| --- | --- |
| 业务库 | 5 + 1 张表（session_map / qa_record / qa_feedback / kb_document / kb_image / question_stat） |
| 会话检查点 | `checkpoints.sqlite` 24.5MB，覆盖 **27 个 thread**；服务重启后会话完整保留 |
| 多轮追问 | 同会话第 2 问历史累积到 4 条；message_count 正确更新 |
| 高频问题统计 | `question_stat` 10 行；Top: 腔体温度异常怎么处理？(4) / 刻蚀机真空度报警怎么处理？(3) |

### 1.5 交付物

| 项 | 证据 |
| --- | --- |
| 部署产物 | `Dockerfile.backend` / `Dockerfile.frontend` / `nginx.conf` / `docker-compose.yml` 全部落地 |
| nginx SSE 配置 | `proxy_buffering off` + `proxy_cache off` + 300s 超时 + `X-Accel-Buffering` |
| 日志与脱敏 | `log_level` 与 `debug` 解耦（默认 INFO）；密钥与图片路径打码；第三方 logger 压到 WARNING |
| 测试 | **84 passed**（含 28 个新增：SSE 契约 / 多模态失败 / MCP 降级） |
| 图片 fixture | `data/eval/fixtures/` 三张（报警图 / 参数表 / 模糊手写图），让 `golden_image.jsonl` 可执行 |
| 文档 | 开发文档 · 接口文档 · 平台侧待办清单（含甲侧回执）· RAG 实现说明 · 协作规范 · 本清单 |

---

## 2. 未完成

### 2.1 代码缺口

| # | 事项 | 优先级 | 实测证据 | 验收标准 |
| --- | --- | --- | --- | --- |
| 1 | **图片知识侧入库**（FR-02 离线侧） | P1 | `rag/ingest.py` 抽图只落盘（best-effort），**无 VLM caption**；`kb_image` 表 **0 行**，全仓无写入点 | 图内文字进检索；图纸/参数表截图可被关键词命中 |
| 2 | **检索层权限过滤**（NFR-03） | P2 | `build_filters(user_role=...)` 收下参数**不用于过滤**，注释写「等权限分级表（FR-11）」 | 角色不同 → 检索结果不同；需先定义权限分级表 |
| 3 | **前端消费 `done.qa_id`** | P1 | 后端已带回并实测闭环；`chat.ts:57` 注释仍写「契约缺口」，`onDone` 未取值 | 新答案的反馈按钮立即可用（跨端，归前端） |
| 4 | **前端备件查询入口** | P2 | 后端能力已具备且可被问答触发；三个视图（Chat/History/Admin）无入口 | 用户可见的备件查询与询价单草稿（跨端） |
| 5 | **会话删除 / 重命名** | P2 | 接口文档 §2 P2 档已定义，未实现 | 历史页可管理会话 |
| 6 | **docx / pptx / xlsx 解析** | P2 | 明确返回 415 并提示需 MarkItDown（**有意为之**，避免产生抓不到页码的切片） | 接入 MarkItDown 后支持 |
| 7 | 工单检索的语义召回 | P2 | 当前为关键词重合打分（确定性、可解释）；无词重合即返回空 | 需要时改用向量召回 |

### 2.2 RAG 侧缺陷（**阻塞类**，实测发现）

| # | 缺陷 | 证据 | 影响 |
| --- | --- | --- | --- |
| 8 | **`OllamaEmbedder` 超时写死 10 秒** | `rag/store.py:269` `timeout_s: float = 10.0`，不读 setting；`embed()` 把 32 条一次性发出 | **开发文档 §5.3 的 bge-m3（Ollama）路线实际不可用** —— 本机 Ollama 已有 `bge-m3:latest`（1024 维，接口正常），但批量 embedding 必超时 `ReadTimeout` |
| 9 | **索引签名不匹配会挡住重建路径** | 报错提示「请重建索引（`scripts/build_index.py --rebuild`）」，但该脚本**没有 `--rebuild` 参数**；`ingest.py --rebuild` 也会失败（先 `get_store()` → `ensure_ready()` 抛异常，清理逻辑到不了） | **FR-08「一键重建索引」实际会卡死**，只能手工 `rm backend/data/chroma/*` 才能恢复 |
| 10 | **LLM 路径下引用覆盖率不达标 → 主链路拒答** | 2026-09-18 配好 `DEEPSEEK_API_KEY` 后实测：「刻蚀机腔体真空度异常怎么排查？」拒答（覆盖率 **94%**）、「CVD 沉积温度是多少？」拒答（**78%**）；同一话题换成「刻蚀设备维护手册里真空度异常的处置步骤」却是 OK（0.89） | `citation.py:444` 在高置信度下要求覆盖率 **100%**，而该门槛是照**抽句式**路径标定的——那条路径由代码给每句分配引用，天然 100%。真 LLM 只「尽量」逐句挂引用（`generate.md:21`），实测落在 78%~94%，于是被硬闸门拒答。**G2 的「覆盖率 100%」当前是靠拒答不达标的回答维持的**。三选一：回退抽句式 / 放宽 `require_coverage` / 程序化补引用 |

> 三条均为 RAG 侧文件（`backend/rag/*`、`backend/agents/*`），需与 RAG 负责人确认后修改。

### 2.3 验证缺口（D8 交付日）

> **本节 11~17 项已于 2026-09-18 全部填补，结果见下方 §2.3b**；保留原表供追溯当时的缺口定义。

| # | 事项 | 现状 | 说明 |
| --- | --- | --- | --- |
| 11 | **黄金集 20 → 50 条** | 仍 20 条（+3 图片） | 文档 D8 要求；构成：设备维护 40% / 工艺 25% / 标准 15% / 负样本 10% / 图片 10% |
| 12 | **正式评测报告** | `data/eval/report_*` 不存在 | `scripts/eval.py` 可用但未产出报告 |
| 13 | **badcase 归因** | `badcases.md` 仍是空表 | 只有「驳回自动追加」的写入，无汇总统计 |
| 14 | **NFR-02 并发 20** | 无压测、无用例 | 需压测脚本 |
| 15 | **G5 首 Token 实测** | 未测 | 文档 §8.4 已放宽到 ≤8s（因采用「先过 verify 再出字」策略） |
| 16 | **三个演示场景走查** | 未做 | 场景 A 抢修 / B 新人学习 / C 参数查询 |
| 17 | 图片类评测（G4） | 未评测 | `DASHSCOPE_API_KEY` 已配置；但 `golden_image.jsonl` 引用的 `fixtures/*.png` 实物不存在，须先补图片 |

### 2.2b RAG 侧缺陷（**2026-09-18 收尾评测新发现**，编号接 §2.2）

> 来源：黄金集从 20 条扩到 45 条（+5 图片）后重跑全量评测。**新增的 25 条是口语化提问**
> （「我们准备开腔换个部件…」「车间里电气起火或者有人触电了…」），原有 20 条写成书面语，
> 恰好绕开了下面这几个闸门 —— 所以这批缺陷是「扩样」本身暴露出来的，不是新引入的回归。
> 全部为 RAG 侧文件（`backend/rag/*`、`backend/agents/nodes/*`、`backend/setting.py`）。

| # | 缺陷 | 证据 | 影响 |
| --- | --- | --- | --- |
| 11 | **图片识别结果没有进入生成上下文** | `nodes/generation.py` 全文**没有任何** `image` / `image_result` 引用；`image_result` 的唯一消费者是 `nodes/retrieval.py:154-168`（只把它拼成一路检索式）。实测 img001/img004/img005：识别**完全正确**（`E-2041` / `E-3061` / `SP-ETA-0101`，confidence 0.98），但 LLM 收到的仍只有文字问题「这个报警怎么处理？」，无法消解「这个」指代，按 `prompts/generate.md:48` 的「未覆盖分支」拒答；随后引用归属丢弃 3 句无引用句 → 答案变空 →「未生成答案」→ `NOT_COVERED` | **FR-02「拍报警截图拿处置步骤」实际不可用**：识别对了、检索对了（top5=1），却答不出来。5 条图片样例中 3 条正样本全挂 |
| 12 | **LLM 自称「未覆盖」时 status 仍是 `OK`** | 问「刻蚀机维护作业的工时定额标准是多少？」→ 答案正文是「知识库未覆盖该问题，不给出具体操作步骤。」（即 `generate.md:53` 让模型自己输出的拒答话术），但 `status=OK`、`confidence=0.47(low)`、`verification.rejected=False`、`reject_reasons=[]` | `should_reject()` 只看检索/引用/锚点/覆盖率四道**确定性**闸门，没有「模型自称未覆盖」这一路。任何以 `status` 判定拒答的评测，都会把「模型已拒答」记成「未拒答」；前端按 status 渲染，会把一句拒答话术显示成正常答案 |
| 13 | **锚点校验把口语虚词当「关键术语」** | `anchor_kb_missing_ratio` 默认 0.40。实测 q038「我们准备开腔换个部件，作业许可这块有什么讲究？」缺失比 **56%**，缺失词是「我们、准备、换个、这块、讲究」；q041 缺失比 **62%**（「用到、配药、药水、注意、怎么办」）；q042 缺失比 **57%**（含连词「或者」）。四条均为 top5=1、要点全可命中，却被拒答 | **口语化提问被系统性误拒**。真实工程师不会按文档措辞提问，这条闸门越严，可用性越差。需改为只对**内容词/型号/数值**做缺失比（虚词、连词、疑问词不应计入分母） |
| 14 | **型号里的数字被当成工艺数值** | q037 问「CVD-200 片内均匀性按什么判合格？…」被拒，理由「问题中的具体数值 `['200 片']` 在检索到的材料中找不到依据」。问题里**并没有**「200 片」—— 是型号 `CVD-200` 被数值抽取器切成 `200` 并吞掉后一个汉字 `片` | 任何形如 `XXX-<数字>` 的型号（CVD-200、DP-600、RP-300、TMP-1600）都会污染数值锚点；型号越常见，误拒越多 |
| 15 | **低置信度把覆盖率门槛放宽到 80% 并放行** | q009（分子泵 TMP-1600）返回 `OK`，`citation_coverage=0.8`：`should_reject` 在 `confidence < confidence_high` 时把 `require_coverage` 从 1.0 放宽到 0.8，于是 20% 的结论句**没有引用就发出去了**。这是 45 条中唯一一条覆盖率 < 100% 的 OK 回答 | 把「引用覆盖率 100%（**不可降级**）」实际拉到 **98.99%**。**注意这条与 #10 的修复方向相反**：#10 是用「程序化补引用 + 丢弃无法归属句」保住 100%，#15 却留了一道低置信度后门，两者需要一起看 |

**本轮 badcase 归因汇总**见 `data/eval/badcases.md`：11 条 badcase 中 **9 条属于「检索已对、要素已齐，却被后置校验误杀」**（锚点误拒 4 + 覆盖率误拒 2 + 图片上下文 3，另 1 条数值误判、1 条覆盖率放宽）。

### 2.3b 验证缺口填补结果（2026-09-18）

| # | 事项 | 结果 |
| --- | --- | --- |
| 11 | 黄金集 20 → 50 条 | ✅ **45 文字 + 5 图片 = 50**。构成：设备维护 20（40%）/ 工艺 13（26%）/ 标准 7（14%）/ 负样本 5（10%）/ 图片 5（10%）。新增 `scripts/check_golden.py` 做自检（章节真实性 + 要点可命中） |
| 12 | 正式评测报告 | ✅ `data/eval/report_20260918_191452.json`（45 条）+ `report_image_20260918_190823.json`（5 条） |
| 13 | badcase 归因 | ✅ `data/eval/badcases.md` 已填 11 行 + 归因汇总表 |
| 14 | NFR-02 并发 20 | ✅ `scripts/load_test.py`。**20/20 成功、0% 错误率，但 P95 = 189.9s**（单条 8.5s）。实测**完全串行**：n=1→8.46s、n=5→40.2s、n=20→190.1s，吞吐恒定约 0.11 req/s，**并发不提升吞吐**。结论：**目前不能支撑 20 并发**，需按 T11 优化 |
| 15 | G5 首 Token | ✅ 已按修订口径（≤8s）实测：全部场景首 Token ≈ 整条链路耗时（「先过 verify 再出字」），P95 4.3s，在口径内 |
| 16 | 三个演示场景走查 | ✅ `scripts/demo_walkthrough.py`，**5/5 通过**（A 抢修 / B 培训模式 / C1 参数 / C2 备件 / D 拒答）。记录见 `data/eval/demo_20260918_192408.json` |
| 17 | 图片类评测（G4） | ✅ 5 张 fixture 已生成（`scripts/make_image_fixtures.py`），图片类评测跑通：**2/5 通过**，3 条失败全部归因于缺陷 #11 |

### 2.4 环境前置（2026-09-18 复核）

| 项 | 现状 | 说明 |
| --- | --- | --- |
| `.env` | ✅ 密钥已直接写入 `.env`（`DEEPSEEK_API_KEY` / `DASHSCOPE_API_KEY`） | 非交互启动也能读到，不再需要 `bash -ic 'make dev'` |
| Ollama | ✅ 已有 `bge-m3:latest`（1024 维） | 现走本地 HF 模型（`local` 路线），Ollama 不再被依赖 |
| 本地 HF 模型 | ✅ 已就绪（`backend/data/models`，约 3.4GB） | 向量库实跑 `local/chroma`（5 文档 / 32 切片），精排 `CrossEncoderReranker` |
| 生产库 | ✅ 已切 MySQL（本机 `127.0.0.1:3307`，库 `smka`，utf8mb4） | 6 张业务表由 `db.py` 的 `init_db()` 启动时自动建好 |

---

## 3. 指标对照（G1 ~ G5）

> ⚠️ 本节表格是 **2026-09-17 的初版快照**（当时 20 条样本、未配密钥、精排走兜底），
> **仅为历史留档**。当前真值见 §0 结论表、§7 与 `README.md` 的实测指标表。

| 编号 | 目标 | 目标值 | 当时快照 | 状态（当时） |
| --- | --- | --- | --- | --- |
| G1 | Top-5 命中率 | ≥ 80% | 100%（仅 20 条样本 + 兜底精排） | ⚠️ 缩样达成 |
| G2 | 引用覆盖率 | **100%** | 100% | ✅ |
| G3 | 要点命中率 | ≥ 80% | **44.7%**（未配 LLM 的降级值） | ❌ 未达标 |
| G3 | 无依据拒答率 | ≥ 90% | 100% | ✅ |
| G4 | 图片提取准确率 | ≥ 85% | 未评测 | ❌ 待验 |
| G5 | 首 Token | ≤ 8s（已放宽） | 未测 | ⚠️ |
| G5 | 文字 P95 | ≤ 8s | 4.49~4.59s（兜底精排） | ✅ |

> **已解决**（2026-09-20 复测，真实 LLM + 45 条黄金集 + 5 条图片）：
> G1 100%、G2 覆盖率 100%（270 引用 / 0 伪造）、G3 严格 89.7% 且宽松 100%、
> G4 5/5、G5 空闲环境 3.9s / 4.3s。精排阈值与术语阈值仍建议在真实语料上重标（改 `setting.py`）。

---

## 4. 自测命令与实测结果

```bash
# 单测（84 passed）
.venv/bin/python -m pytest tests/ -q

# 起服务（密钥在 ~/.bashrc，需交互式 shell 才能读到）
bash -ic 'make dev'
# 本机 8000 端口被残留监听占用时：make dev PORT=8010

# 健康检查（7 组件）
curl -s localhost:8000/api/health

# 索引（本机无 HF 模型时用兜底；有 Ollama 时见缺陷 #8）
EMBEDDING_PROVIDER=hashing RERANK_PROVIDER=lexical make ingest
```

**最近一次实测**：单测 84 passed · 后端端到端 **58 项断言全过** · health `status=ok`。

---

## 5. 建议的收尾顺序

**第一步 — 解锁指标（只需配置）**

1. 配 `DEEPSEEK_API_KEY` → 重跑 `scripts/eval.py`，看 G3 是否回到 ≥80%
2. 配 `DASHSCOPE_API_KEY` → 打通 G4 图片提取评测
3. 决定向量路线：修缺陷 #8 用 Ollama bge-m3，或下载 HF 模型

**第二步 — 修两个阻塞缺陷**

4. 缺陷 #8：`OllamaEmbedder` 超时改从 setting 读（默认提到 60s）或分批
5. 缺陷 #9：让 `--rebuild` 绕过签名校验（或先 `reset_store()`）

**第三步 — D8 交付**

6. 黄金集扩到 50 条 → **重标阈值**
7. 跑正式评测 + 写 badcase 归因
8. 三个演示场景走查 + 20 并发压测

**第四步 — 增量功能（可砍，文档 §7.4 排 4~6 位）**

9. 图片知识侧入库（抽图 + VLM caption + 写 `kb_image`）
10. 前端：备件入口 + 消费 `done.qa_id`
11. 权限分级表（FR-11）→ 检索层过滤生效

---

## 6. 变更记录

| 日期 | 说明 |
| --- | --- |
| 2026-09-17 | 首版：对照开发文档 FR/NFR/G/D 四张表，逐项实测填写 |

---

## 7. RAG 侧回执（2026-09-18）

> 针对 §2.2 的三条「RAG 侧缺陷」，已全部修复并实测验证。改动都在 RAG 侧文件，未触碰平台侧实现。

| # | 缺陷 | 结论 | 修复与证据 |
| --- | --- | --- | --- |
| 8 | `OllamaEmbedder` 超时写死 10s | ✅ 成立，已修 | `timeout_s` 与批量改为读配置：新增 `setting.ollama_timeout_s`（默认 **60s**）与 `ollama_batch_size`（默认 **16**，分批发送）。`create_embedder` 的两条构造路径都传入配置 |
| 9 | 签名不匹配会挡住重建路径 | ✅ 成立，已修 | ① 报错信息改为指向**真实存在**的做法（`scripts/build_index.py` 默认即清空重建，追加用 `--keep`）；② 新增 `store.rebuild_store()`：以 `strict_signature=False` 先清空再建，`scripts/ingest.py --rebuild` 与 `build_index.py` 都走它。**实测**：把索引用 hashing 重建（制造签名不匹配）后，`ingest.py --rebuild` 恢复成功；查询路径仍会被 `EmbeddingMismatchError` 拦住（闸门未被削弱） |
| 10 | LLM 路径覆盖率不达标 → 整条拒答 | ✅ 成立，已修 | 采用「**程序化补引用 + 丢弃无法归属的句子**」，而不是放宽门槛：新增 `citation.enforce_citations()`，在 `generate` 之后执行 —— 已挂引用的句子不动；漏引用的句子按字符二元组重合度归属到最匹配的上下文块（阈值 `setting.citation_attribution_threshold`，默认 0.45）；归属不上的句子**不输出**，并在 `uncertain` 里说明「有 N 句结论因未找到依据被省略」。**这样 G2 的 100% 恢复成它本来的含义：发出去的每条结论都有依据**，而不是靠拒答维持 |

回归证据：`pytest tests/ -q` → **88 passed**（新增 4 个引用归属用例：自动归属 / 保留非结论行 / 全无依据仍拒答 / 部分缺引用被修复而非拒答）；
黄金集五项指标仍全达标（MRR 1.0000 / NDCG@5 0.9932 / 引用真实性 100%）。
> LLM 路径的真值对比仍需在有 `DEEPSEEK_API_KEY` 的环境重跑 `scripts/eval.py`（我这边无密钥，用等价的模拟答案做了回归）。

### 7.1 另外发现：评测口径没跟上新的跨源能力

你们接上备件台账/工单后，`scripts/eval.py` 出现「MRR 1.0 → 0.9444、P95 4.6s → 16.4s」的假性退化。
排查结论：**不是回归，是评测口径的问题** —— 备件类问题（q009/q011/q012）的 top-1 变成了
`备件库存台账`（第三章的正确来源），而评测只把 `must_cite` 里的手册章节算作相关。已修：

- `golden_qa.jsonl`：三个备件类条目标注 `allow_external_sources: true`（台账/工单也算有效证据）；
- `scripts/eval.py`：外部源计入相关性判定；延迟按路径拆分统计 ——
  **纯文档 P95（目标 ≤8s）** 与 **带外部源 P95（目标 ≤15s，与图片类同档）** 分别报出。

### 7.2 延迟成本已量化（供你决定要不要优化）

逐节点 `timings` 实测（稳态）：

| 问题类型 | 主要耗时 | 合计 |
| --- | --- | --- |
| 纯文档 | `rerank ≈ 4.9s`（CPU 交叉编码器，12 候选 × 256 token）＋ `retrieve ≈ 0.1s` | ≈ 5s |
| 备件类（命中外部源） | 上面 5s ＋ `augment ≈ 8.8s`（其中外部查询与二次精排 `augment.rerank ≈ 3.7s`） | ≈ 14s |

如需压到 8s 以内，两条低成本路径：① 外部查询与 CE 精排**并行**（`asyncio.gather`，能省 ~4s）；
② 有 GPU 时把精排从 4.9s 压到亚秒级（`rerank_max_candidates` 可同时调大换召回）。


---

## 8. 三块新业务回执（2026-09-20）

项目方追加需求：**维护计划生成 / 备件商城与采购结算 / 考核认证**，前端交第三人，本次只做后端。

### 8.1 交付内容

| 功能 | 端点数 | 服务层 | 路由 | 数据表 | 测试 |
| --- | --- | --- | --- | --- | --- |
| 维护计划生成 | 4 | `backend/services/plan_service.py` | `backend/routers/plans.py` | `maintenance_plan` | `tests/test_api/test_plan_service.py`（14 passed） |
| 备件商城与采购结算 | 11 | `backend/services/parts_service.py` | `backend/routers/parts.py` | `part_order`、`part_settlement` | `tests/test_api/test_parts_service.py`（10 passed） |
| 考核认证 | 6 | `backend/services/training_service.py` | `backend/routers/training.py` | `training_quiz`、`training_attempt`、`certification` | `tests/test_api/test_training_service.py`（10 passed） |

新增共享件：`backend/services/llm_client.py`（文本模型统一入口：`available/chat/chat_json/parse_json`，
未配密钥时返回 `LLMUnavailable`，调用方一律降级到确定性实现，**不静默失败**）。

`backend/routers/__init__.py` 的 `register_routers()` 已挂载三组新路由，接口总数 **11 → 32**；
契约层（`db.py` 6 张新表 → 共 12 张、`schemas.py` 约 20 个模型、`routers/__init__.py` 的
service getter + `_*_SERVICE_API` 协议）由我先行冻结，三人并行实现业务层。

### 8.2 零幻觉在这三块上的落法

| 风险 | 处置 | 实测 |
| --- | --- | --- |
| 编造维护周期 | 周期只能从**检索到的原文句子**里正则抽取（每 4000 小时 / 每 25 片 / 每 3 个月 / 每次开腔），每条计划项带 `evidence`（文档+版本+章节+页码+`chunk_id`） | 8 条计划项全部带依据；查不到周期的 3 项如实进 `uncovered`（含原因：「未收录」/「未给出可换算的周期数值」） |
| 把执行频次当周期（如「每班点检」） | 只折算成数值会失真 → 一律进 `uncovered`，不硬塞天数 | 手册里此类写法未生成任何计划项 |
| 推荐无依据的替代件 | `basis` 缺失即拒单（`400 SUBSTITUTE_BASIS_REQUIRED`），与问答链路同口径 | 无依据替代件 400；带依据替代件成功且明细里回显依据原文 |
| 出题无依据 | 先检索再出题；LLM 题目若 `evidence_index` 越界、或题干里的数字在证据文本中找不到（`_untraceable_numbers`）→ **丢弃该题**；一题不剩则 `422 QUIZ_GENERATION_FAILED` | 实测 4 道题（`generator=llm:deepseek-chat`）全部带依据；未配密钥时为 `extractive-fallback`（原句+同片段数值做干扰项），功能不中断 |
| 幻觉成「已下单 / 已获原厂认证」 | 采购只生成内部申请单（`approve` 即人工确认点）；发证须显式调用，`issuer` 默认「内部授权」且 note 固定标注不代表原厂 | 交卷响应 `certification_id` 恒为 `null`；发证接口在未通过考核时 400 / 记录不存在时 404 |

### 8.3 端到端实测（真实 app `/api`，`fastapi.testclient`）

```
① 维护计划（Etcher-A / 5200h / 46000 片）
   POST /api/plans/generate           200  计划项 8 条 / uncovered 3 条
     · 干泵排气过滤器更换  hours  周期 4000  剩余 -200   due     依据 刻蚀设备维护手册 3.5
     · 分子泵轴承检查      days   周期 360   剩余  360   planned 依据 刻蚀设备维护手册 3.5
   POST /api/plans/generate(persist)  200  落库 8 条
   GET  /api/plans                    200  total=8（按 remaining 升序）
   POST /api/plans/1/complete         200  status→done
   POST /api/plans/999999/complete    404
   GET  /api/plans?status=乱写         400  INVALID_ARGUMENT（列出允许值）

② 备件商城与采购
   GET  /api/parts/catalog            200  SP-ETA-0101 价格 185.0 / stock_status ok / 替代件 1 个
   GET  /api/parts/SP-ETA-0101        200  价格 185.0、供应商「示例供应商-密封件」、
                                            forbidden 返回成品句子「禁止替代：NBR…——…」
   POST /api/parts/orders             200  PO-20260920-0063 金额 2050.0（2×185 + 3×560）
   submit→approve→receive→settle      200  状态与 allowed_actions 逐步变化
   POST …/settle（重复）               409  ALREADY_SETTLED
   GET  /api/parts/settlements        200  台账 + total_amount 汇总

③ 考核认证
   POST /api/training/quizzes         200  quiz_id=20 generator=llm:deepseek-chat 4 道题（全部带依据）
   POST /api/training/quizzes 非法题型  400
   POST /api/training/quizzes/{id}/submit 200 score=100.0 passed=true pass_line=60.0
                                            certification_id=None（**未自动发证**）
   POST /api/training/certifications  200  cert L2、到期 2027-09-20、days_to_expiry=365、status=valid
   GET  /api/training/certifications/expiring 200
```

回归：`pytest tests/ -q` → **125 passed**（原 91 + 新业务 34）；健康检查仍 7 组件全 ok；
原问答链路（文本 / 培训 / 图片 / 拒答 / 跨源）未受影响。

### 8.4 明确不做 / 留给后续

- **不对供应商发起真实下单、不做财务过账**（开发文档 4.2 红线；只出申请单与结算台账）；
- **库存不扣减**：收货只登记事实，不动库存台账（真实入库需接 ERP）；
- **权责分离未做**：`approve` 没有身份校验（缺权限分级表，与 FR-11 同一根因），
  当前靠前端区分「申请人 / 审批人」视图；
- **计划项依赖知识库收录的项**：候选清单固定（手册里能识别到的 PM 项），
  语料扩充后可调 `plan_service` 的候选表；
- 三块功能的**前端页面**由第三人实现，接口清单与按钮状态机见 `接口文档.md` §10 与
  `前端对接说明（三块新业务）.md`。
