# 项目进度报告

> **项目**：半导体设备维护知识库智能问答系统
> **对照基准**：《半导体设备维护知识库智能问答系统_开发文档（初稿）(2).md》
> **当前状态**：**端到端已跑通并完成收尾评测**（50 条黄金集全量跑分、演示走查、并发压测均已执行）
> **报告日期**：2026-09-18（收尾评测后终稿）

---

## 一、结论速览

| 维度 | 文档要求 | 当前 | 完成度 |
| --- | --- | --- | --- |
| 业务功能 | 10 项（P0 六项 / P1 四项） | 8 项完整、2 项部分 | **约 90%** |
| 非功能需求 | 7 项 | 6 项达成、1 项未达标（并发） | **约 85%** |
| 验收指标 | G1–G5 | G1/G3/G5 达标，**G2 差 1.01%**，**G4 2/5** | **约 75%** |
| 里程碑 | D1–D8 | D1–D8 全部完成（D8 的部署与评测已补齐） | **100%** |
| 工程结构 | 目录树全部落位 | 11 个原空文件已全部实现 | **约 97%** |

**一句话**：主链路、部署产物、评测体系、演示走查全部就位，**50 条黄金集已跑出可复跑的正式报告**；
未达标项收敛为**两条**——**G2 引用覆盖率 98.99%**（缺陷 #15）与 **G4 图片类 2/5**（缺陷 #11），
两者**根因都在 RAG 侧的生成上下文与后置校验**，已逐条记录复现证据，**本侧未擅自改动 RAG 实现**。

---

## 二、业务功能对照（FR-01 ~ FR-10）

| 编号 | 功能 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- | --- |
| FR-01 | 知识问答（多轮 + SSE） | P0 | ✅ **完成** | 9 节点图 + SSE 六事件；多轮上下文已验证；演示走查场景 A/B/C 全过 |
| FR-02 | 多模态提问（图片 + 文本） | P0 | ⚠️ **部分** | 上传/`ingest_image`/`vision_extract`/提示词全部就位，`DASHSCOPE_API_KEY` 已配，**VLM 识别经实测 100% 正确（0.98 置信度）**；但**识别结果进不了生成上下文**（缺陷 #11），带图提问必拒答 → **实际不可用** |
| FR-03 | 跨源检索 | P0 | ✅ **完成** | 文档源（BM25 + 向量 + RRF + 精排 + 元数据过滤）+ **工单源 MCP 已接入**（`mcp_client.py` / `ticket_server.py`，`/api/health` 报 registered） |
| FR-04 | 答案溯源 | P0 | ✅ **完成** | 引用由代码分配 [n]，verify 独立校验真实性；**实测已发出 228 项引用，伪造 0 项** |
| FR-05 | 置信度与拒答 | P0 | ✅ **完成** | 三级标注 + 确定性拒答；负样本 5/5 正确拒答，域外问题实测正确拒答 |
| FR-06 | 会话与历史 | P1 | ✅ **完成** | 会话列表/历史消息/分页/搜索；落盘重启后可恢复 |
| FR-07 | 反馈记录 | P1 | ✅ **完成** | 四态反馈落库 ✅、驳回写 badcases.md ✅、**`done` 事件回填 `qa_id` 使流式回答可直接反馈**（收尾 T4）；badcases 归因汇总表已填（11 条） |
| FR-08 | 知识入库与索引重建 | P0 | ⚠️ **部分** | 上传/切片/向量化/一键重建 ✅（5 文档 32 切片）；**docx/pptx/xlsx 不支持**（明确返回 415）；**抽图未接 VLM caption**（图片知识侧未入库） |
| FR-09 | 培训与认证 | P1 | ✅ **完成** | 分层讲解 ✅（`mode=training` + 两套提示词 + 前端开关）；演示走查场景 B 已验证 |
| FR-10 | 备件查询与订购引导 | P1 | ✅ **完成** | parts_query ✅（替代件依据、禁止替代警告、询价单草稿）；**前端已加快捷提问入口**（收尾 T8），演示走查场景 C2 返回真实台账引用 |

### 逐项缺口详解（仅剩两处）

**FR-02 多模态：能力全在，只差「把识别结果喂给生成」**

- 收尾评测补齐了 5 张 fixture 并首次真实调用 VLM：`img001` 识别出 `E-2041`、`img004` 识别出 `E-3061`、
  `img005` 识别出 `SP-ETA-0101`，**置信度均为 0.98、Top-5 命中 1.0**——识别环节无问题。
- 断点：`backend/agents/nodes/generation.py` 里**没有任何 `image` 引用**，`image_result` 的唯一消费者是
  `nodes/retrieval.py`（仅把它当作一条检索 query）。于是 LLM 只看到字面问题「这个报警怎么处理？」，
  指代无法消解 → 走「未覆盖」分支 → `enforce_citations` 丢弃全部无归属句子 → 空答案 → 拒答。
- **结论：这是 RAG 侧一行代码的缺口，不是环境或数据问题。** 归 RAG 侧处理（缺陷 #11）。

**FR-08 的 Office 解析与图片入库**

- docx/pptx/xlsx 待接 MarkItDown；离线抽图的 VLM caption 未做。二者都在文档 §7.4 的可砍顺位内。

---

## 三、非功能需求对照（NFR-01 ~ NFR-07）

| 编号 | 要求 | 状态 | 说明 |
| --- | --- | --- | --- |
| NFR-01 | 首 Token ≤ 2s；文字 P95 ≤ 8s；图片 P95 ≤ 15s | ⚠️ **部分** | **文字 P95 实测 4.29s（达标）**，分路径 纯文档 3.86s / 带外部源 5.14s。**首 Token 未达标**：本实现「先过 verify 再出字」，首 Token ≈ 整条链路耗时 → 与 NFR-01 的 2s 口径冲突（设计取舍，已在 RAG 说明 §9.4 定案） |
| NFR-02 | 支持 20 并发问答 | ❌ **未达标** | 已实测：n=1 → 8.5s、n=5 → 40s、n=20 → **189.9s**，吞吐恒定 ≈0.11 req/s。**错误率 0%、语义无退化，纯粹是排队**——精排是 CPU 密集且进程内串行 |
| NFR-03 | 密钥只进 .env；日志脱敏；**检索层权限过滤** | ⚠️ **部分** | 前两项 ✅（`mask_secrets` + .env 不入库）；**权限过滤仍未生效**——`build_filters` 只接 device_model/category，`user_role` 仅透传记录 |
| NFR-04 | 提示词/阈值/权重全部配置化 | ✅ **基本达成** | 绝大部分在 `setting.py` + `.env`；少量策略常量仍在代码里（token 缓冲开关、多轮上下文轮数） |
| NFR-05 | 新增工具不改智能体代码；向量库访问收敛 | ✅ **达成** | 工具集中在 `tools/kb_tools.py`；向量库唯一出口 `rag/store.py` |
| NFR-06 | trace_id + 节点耗时 + 工具调用 + 引用 | ✅ **达成** | trace_id 贯穿 HTTP → 图 → 日志；`timings` 覆盖 8 个节点（含 augment 子耗时） |
| NFR-07 | Python 3.10~3.12；Chrome/Edge 最新两版 | ✅ **达成** | 3.10.12 实测通过 |

---

## 四、验收指标对照（G1 ~ G5）

**基准：50 条黄金集（45 文字 + 5 图片），生成走真 LLM 路径，报告 `data/eval/report_20260918_191452.json` / `report_image_20260918_190823.json`。**

| 编号 | 目标 | 目标值 | 实测 | 状态 |
| --- | --- | --- | --- | --- |
| G1 | 检索质量（Top-5 命中率） | ≥ 80% | **100%** | ✅ |
| G2 | 引用覆盖率 | **100%**（不可降级） | **98.99%** | ❌ **未达标**（差 1.01%） |
| G2 | 引用真实性 | ≥ 95% | **100%**（228 项，伪造 0） | ✅ |
| G3 | 要点命中率 | ≥ 80% | **100%**（185/185）；仅看答案正文 **73.51%** | ✅ |
| G3 | 无依据拒答率 | ≥ 90% | **100%**（5/5） | ✅ |
| G4 | 图片关键信息提取准确率 | ≥ 85% | **2/5 通过**（识别环节 3/3 全对，败在生成） | ❌ **未达标** |
| G5 | 首 Token ≤ 2s | — | 未达标（先过 verify 再出字） | ⚠️ |
| G5 | 文字 P95 ≤ 8s | — | **4.29s**（纯文档 3.86s / 带外部源 5.14s） | ✅ |
| G5 | 图片类 P95 ≤ 15s | — | 随生成链路，未单独出数 | ⚠️ |
| —— | MRR / NDCG@5 | —— | 0.9750 / 0.9785 | —— |

### 三个最关键的指标问题

**① G2 覆盖率 98.99%，差 1.01%（不可降级指标，最需要交代）。**

两条坏消息里有一条是好消息：真 LLM 路径下**原始**覆盖率只有 78%~94%（模型自己组织语言，句子未必能逐句溯源），
`enforce_citations()`（阈值 0.45）程序化补引用后拉回到 **98.99%**。**残留的 1.01% 全部来自 q009 一条**：
它走**低置信度路径**，该路径把覆盖率门槛从 100% 放宽到 **80%** 并放行（缺陷 #15）。
**这是唯一一条，修掉它 G2 就是 100%。**

**② G4 图片类 2/5，但失败原因与「识别准确率」无关。**

3 条失败（img001/004/005）的 VLM 识别**全部正确**（E-2041 / E-3061 / SP-ETA-0101，置信度 0.98），
败在**识别结果没有传进生成节点**（缺陷 #11）。也就是说 G4 当前测出来的不是识别能力，而是链路断点。
**修掉缺陷 #11，G4 预计直接到 4/5 或 5/5。**

**③ G3 要点命中率的两个口径必须一起报。**

`185/185 = 100%` 是宽松口径（答案正文 + 引用材料一起算）；**仅看答案正文是 73.51%**。
切到真 LLM 路径后，「答案原句摘自命中切片」这个抽句式红利消失了——严格口径从降级的 44.7%
提升到 **73.51%**，但离 100% 仍有距离。这是「模型自己组织语言」的固有代价，**两个数都要给，不能只报高的那个**。

### 阈值的重标结论：**不调阈值**

收尾 T5 要求按扩样后的数据重标 `anchor_kb_missing_ratio`(0.40) 与精排阈值。实测结论是**不该调**：

- 精排阈值 0.5 的绝对余量依然健康（正样本分 0.53~0.73）；
- 扩样后暴露的 11 条 badcase 里 **9 条是「检索已对、要素已齐，却被后置校验误杀」**，根因是
  **锚点校验把口语虚词当关键术语**（缺陷 #13：`我们/准备/换个/这块/讲究` 甚至连词 `或者` 都进了分母）
  与**型号里的数字被当工艺数值**（缺陷 #14：`CVD-200` 被切成 `200` + `片`）；
- 单纯把 0.40 放宽到 0.6 会同时放过真正的负样本（`XH-900` 这类型号缺失）——**得不偿失**。

**正确做法是改分词口径与数值抽取口径，不是改数字。** 详细证据见 `data/eval/badcases.md` 与 `功能完成度清单.md` §2.2b。

---

## 五、里程碑对照（D1 ~ D8）

| 天 | 主题 | 状态 | 缺口 |
| --- | --- | --- | --- |
| D1 | 环境与数据 | ✅ 完成 | 5 文档 / 32 切片，元数据完整率 100% |
| D2 | 索引与检索 | ✅ 完成 | 向量库 + BM25 + 一键重建 |
| D3 | 检索增强 | ✅ 完成 | 改写 / 术语归一 / 元数据过滤 / 精排 |
| D4 | 生成与引用（核心日） | ✅ 完成 | 引用校验 + 拒答 + 培训模板 + 真 LLM 路径；**图片识别节点已真实跑通**（残留缺陷 #11 在生成侧，非本日主题） |
| D5 | 服务化 | ✅ 完成 | FastAPI + SSE + 上传 + 会话落盘 + 预热 |
| D6 | 前端 | ✅ 完成 | 三个视图、引用卡片、置信度标签、SSE 解析；**收尾补：反馈按钮 qa_id 回填、快捷提问入口** |
| D7 | 工具与管理页 | ✅ 完成 | parts_query ✅、任务进度 ✅、**MCP 工单已接入** ✅ |
| D8 | **评测与交付** | ✅ 完成 | 见下 |

### D8 清单（收尾后全部闭合）

- [x] 黄金集从 20 条扩到 **50 条**（45 文字 + 5 图片；设备维护 40% / 工艺 26% / 标准 14% / 负样本 11% / 图片 10%）
- [x] 评测脚本跑分并产出正式报告（`report_20260918_191452.json`、`report_image_20260918_190823.json`）
- [x] badcase 归因（`badcases.md`：11 条明细 + 归因汇总表）
- [x] **Docker Compose 部署**（`docker-compose.yml`、`deploy/Dockerfile.*`、`deploy/nginx.conf` 均已实现，nginx 已关 SSE 缓冲）
- [x] README
- [x] 演示场景验收（`demo_walkthrough.py`：A 抢修 / B 培训 / C 参数+备件 / D 拒答 —— **5/5 通过**）
- [x] 并发压测（`load_test.py -n 20`，NFR-02 结论：**不能支撑**，如实记录）

---

## 六、工程结构

文档 §6.2 目录树中此前的 **12 个空文件已全部落位**（唯二空文件是 `backend/__init__.py` 与
`backend/mcp_servers/__init__.py`，均为合法的包标记）：

| 文件 | 现状 |
| --- | --- |
| `backend/mcp_client.py` / `mcp_servers/ticket_server.py` | ✅ 已实现，`/api/health` 报 `ticket_search registered` |
| `backend/tools/__init__.py` | ✅ 已实现（本地工具 + MCP 工具聚合，含降级） |
| `deploy/Dockerfile.backend` / `Dockerfile.frontend` / `nginx.conf` / `docker-compose.yml` | ✅ 已实现 |
| `tests/test_api/test_sse.py` / `test_mcp/test_ticket_server.py` / `test_multimodal/test_vision.py` | ✅ 已实现 |

> 全仓测试总数 **88 passed**（收尾前为 48）。

---

## 七、超出文档的增量（已实现但文档未要求）

| 项 | 说明 |
| --- | --- |
| `POST /api/admin/index/rebuild` + `GET /api/admin/jobs/{id}` | 文档 §5.4 接口表遗漏了索引重建入口，但 §6.2 又规定 admin.py 负责它——已补齐 |
| **安全前置提示** | `respond` 节点按回答内容自动追加 LOTO 能源隔离提示，走 `uncertain` 下发，不破坏已冻结的 SSE 契约 |
| `done` 事件追加 `qa_id` | 原本前端拿不到 qa_id，问答页反馈按钮只能置灰；纯追加字段、向后兼容。**收尾 T4 已让前端消费它** |
| 前端快捷提问入口 | 输入框下方三个常用提问（备件库存/替代件/参数查询），点选填入并聚焦，**不自动发送**（避免误触） |
| 备件示例数据源 | `backend/config/parts_inventory.yaml`，含替代件兼容性依据与禁止替代条目 |
| 契约缺口①②的落地 | `AgentState` 追加 `device_model`/`category`/`answer_mode`/`user_role`，并打通 HTTP → 图的全链路透传 |
| 降级不中断 | 模型/数据库/MCP 任一不可用都不阻止启动，由 `/api/health` 如实报 `degraded` 并给出原因 |
| **收尾新增的 5 个评测脚本** | `check_golden.py`（黄金集自检）、`make_image_fixtures.py`（图片 fixture 生成）、`eval_image.py`（图片评测）、`demo_walkthrough.py`（演示走查）、`load_test.py`（并发压测） |

---

## 八、环境前置条件（2026-09-18 复核：**已全部解决**）

| 项 | 现状 |
| --- | --- |
| 本地向量模型 `bge-large-zh-v1.5` | ✅ 已就绪（`backend/data/models`，约 3.4GB）；实跑 `local/chroma`（5 文档 / 32 切片） |
| 精排模型 `bge-reranker-v2-m3` | ✅ 已就绪；实跑 `CrossEncoderReranker`（报告内 `rerank_provider=cross-encoder`） |
| `DEEPSEEK_API_KEY` | ✅ 已配置，**已在 LLM 路径下全量重跑**（本文所有生成类指标均为 LLM 路径真值） |
| `DASHSCOPE_API_KEY` | ✅ 已配置，**已有真实 VLM 调用记录**（图片评测 5 条，识别 3/3 全对） |
| MySQL | ✅ 已接入（本机 `127.0.0.1:3307`，库 `smka`），6 张业务表启动时自动建好 |
| 图片 fixtures | ✅ **已补齐 5 张**（`data/eval/fixtures/*.png`，含 1 张故意模糊的负样本），图片类评测可执行 |
| `/api/health` | ✅ 7 个组件全 `ok`（graph 9 节点 / LLM / VLM / database / vector_store 32 切片 5 文档 / MCP ticket_search / 预热 29.0s） |

---

## 九、遗留问题与建议顺序

**第一优先（两条未达标指标，都在 RAG 侧，本侧不擅自改）**

1. **缺陷 #11 —— 把 `image_result` 喂进 `generation.py`**。修这一条**同时解决 G4（预计 4/5~5/5）与 FR-02 可用性**，
   是当前性价比最高的一处改动。
2. **缺陷 #15 —— 低置信度路径不应把覆盖率门槛放宽到 80%**。修掉即 **G2 回到 100%**（不可降级指标）。
3. **缺陷 #13 / #14 —— 锚点校验的分词口径**。二者合计造成 11 条 badcase 里的 7 条误拒；
   口语化提问（「我们准备开腔换个部件…」）因虚词缺失比 56% 被拒，**越像真人说话越容易被拒**。

**第二优先（容量与工程）**

4. **NFR-02 并发**：精排前 `asyncio.gather` 并行化外部查询（省 ~4s）；有 GPU 时精排从 4.9s 压到亚秒级。
   当前 20 并发 P95 189.9s，**不能上线**。
5. **NFR-03 权限过滤**：`user_role` 已透传但未参与 `build_filters`，等权限分级表（FR-11）落地。
6. **NFR-01 首 Token 2s 口径**：要么改文档口径（接受「先过 verify 再出字」），要么改成边生成边发、
   由 `done` 纠正——**需产品拍板**。

**第三优先（文档 §7.4 可砍顺位内）**

7. 图片知识侧入库（抽图 + VLM caption）、docx/pptx/xlsx 解析（MarkItDown）。
8. 前端补：知识入库页、切片查看页。

---

## 十、当前可运行状态（2026-09-18 实测）

```
前端  http://127.0.0.1:5173     后端  http://127.0.0.1:8000（/docs）
测试  88 passed
索引  5 文档 / 32 切片 / 元数据完整率 100% / 向量库 local+chroma / 精排 cross-encoder
健康  /api/health 7 组件全 ok
评测  50 条黄金集：Top-5 100% · 覆盖率 98.99% · 引用真实性 100%（228 项 0 伪造）
      · 要点命中 100%（正文口径 73.51%）· 负样本拒答 100%（5/5）
      · MRR 0.9750 · NDCG@5 0.9785 · 文字 P95 4.29s（纯文档 3.86s / 带外部源 5.14s）
图片  2/5 通过（识别 3/3 全对，败在生成上下文，缺陷 #11）
演示  5/5 场景通过（A 抢修 / B 培训 / C1 参数 / C2 备件 / D 拒答）
并发  n=1 8.5s · n=5 40.1s · n=20 189.9s（吞吐恒定 0.11 req/s，不能支撑 20 并发）
```

启动命令：`make dev`（后端）、`make dev-web`（前端）。全部指标可用 README「评测与验收」一节的命令复跑。
