# 项目进度报告

> **项目**：半导体设备维护知识库智能问答系统
> **对照基准**：《半导体设备维护知识库智能问答系统_开发文档（初稿）(2).md》
> **当前状态**：**端到端已跑通**，核心链路可演示；距离文档的验收标准还有明确缺口
> **报告日期**：2026-09

---

## 一、结论速览

| 维度 | 文档要求 | 当前 | 完成度 |
| --- | --- | --- | --- |
| 业务功能 | 10 项（P0 六项 / P1 四项） | 3 项完整、7 项部分 | **约 70%** |
| 非功能需求 | 7 项 | 4 项达成、2 项部分、1 项未测 | 约 70% |
| 验收指标 | G1–G5 | G2/G3(拒答) 达成，G1 缩样达成，**G3(要点)/G4 未达标或未测** | **约 50%** |
| 里程碑 | D1–D8 | D1–D6 完成，D7 部分，**D8 基本未做** | 约 75% |
| 工程结构 | 目录树全部落位 | **12 个文件仍是 0 字节** | 约 87% |

**一句话**：主链路（提问 → 检索 → 生成 → 引用校验 → 拒答 → SSE → 前端展示）已经打通并验证；
**未完成集中在三处：部署交付（D8）、工单跨源检索（MCP）、以及受环境制约的指标验收**。

---

## 二、业务功能对照（FR-01 ~ FR-10）

| 编号 | 功能 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- | --- |
| FR-01 | 知识问答（多轮 + SSE） | P0 | ✅ **完成** | 9 节点图 + SSE 六事件；多轮上下文已验证（同会话第 2 问累积到 4 条历史） |
| FR-02 | 多模态提问（图片 + 文本） | P0 | ⚠️ **部分** | 上传接口、ingest_image 节点、vision_extract 工具、vision.md 提示词均已实现；**但从未真实跑通**（缺 DASHSCOPE_API_KEY），且**图片知识侧入库未做** |
| FR-03 | 跨源检索 | P0 | ⚠️ **部分** | **文档源完成**（BM25 + 向量 + RRF + 精排 + 元数据过滤）；**工单源未做**——MCP 未接入 |
| FR-04 | 答案溯源 | P0 | ✅ **完成** | 引用由代码分配 [n]，verify 独立校验真实性；接口返回文档名/版本/页码/章节 |
| FR-05 | 置信度与拒答 | P0 | ✅ **完成** | 三级标注 + 确定性拒答；拒答时清空引用、改写答案、压低置信度 |
| FR-06 | 会话与历史 | P1 | ✅ **完成** | 会话列表/历史消息/分页/搜索；落盘重启后可恢复 |
| FR-07 | 反馈记录 | P1 | ⚠️ **部分** | 四态反馈落库 ✅、驳回写 badcases.md ✅；**「每周归因报告」机制未做**（badcases.md 仍是空表） |
| FR-08 | 知识入库与索引重建 | P0 | ⚠️ **部分** | 上传/切片/向量化/一键重建 ✅（5 文档 32 切片）；**docx/pptx/xlsx 不支持**（明确返回 415 并提示需 MarkItDown）；**抽图未接 VLM** |
| FR-09 | 培训与认证 | P1 | ⚠️ **部分** | 分层讲解 ✅（`mode=training` + 两套提示词 + 前端开关，前端标注「实验性」）；**高频问题记录未做**；考核与认证按文档已声明不在本期 |
| FR-10 | 备件查询与订购引导 | P1 | ⚠️ **部分** | parts_query 工具 ✅（含替代件依据、禁止替代警告、询价单草稿、示例数据源）；**前端无备件入口**，用户看不到 |

### 逐项缺口详解

**FR-02 多模态（P0，缺口最大）**

- 代码写完了，但**仍未真实调用过 VLM**：原因为 `DASHSCOPE_API_KEY` 未配置（**2026-09-18 已配置**，但 `/api/health` 的 `vlm.ok=true` 仅代表密钥存在，未做真实调用）
- **图片知识侧（离线抽图）完全未做**：`rag/ingest.py` 里已有 `ImageAsset` 数据结构，但没有接 VLM 生成 caption、也没有把图内文字写回检索
- 图片评测集 `golden_image.jsonl` 有 3 条，但**图片 fixture 文件不存在**，无法评测

**FR-03 跨源检索（P0）**

- 文档源完整；**历史工单源（MCP）未实现**：`backend/mcp_client.py`、`backend/mcp_servers/ticket_server.py` 都是 0 字节
- 影响：场景 A「检索手册 + 历史工单」实际只能检索到手册
- 文档 §7.4 的取舍顺序里，工单 MCP 是**可砍项（第 3 顺位）**，因此这属于"按计划取舍"而非失控

**FR-07 / FR-09 的 P1 增量**

- 归因报告：目前只有"驳回即追加到 badcases.md"的写入，没有汇总/统计入口
- 高频问题记录：无任何实现（无表、无接口、无页面）
- 前端没有培训专属页，只在输入框里加了个模式开关

**FR-10 前端可见性**

- 后端能力齐了，但三个视图（Chat / History / Admin）里没有备件查询入口
- 询价单草稿目前只能通过工具返回值拿到，没有对应的前端展示

---

## 三、非功能需求对照（NFR-01 ~ NFR-07）

| 编号 | 要求 | 状态 | 说明 |
| --- | --- | --- | --- |
| NFR-01 | 首 Token ≤ 2s；文字 P95 ≤ 8s；图片 P95 ≤ 15s | ⚠️ **部分** | P95 实测 4.49s（乙侧，兜底精排）；**首 Token 未实测**，且当前 token 采用「verify 定稿后一次性发」策略，首 token 时间 ≈ 整图耗时，需与文档口径对齐 |
| NFR-02 | 支持 20 并发问答 | ❌ **未测** | 无压测脚本、无并发用例 |
| NFR-03 | 密钥只进 .env；日志脱敏；**检索层权限过滤** | ⚠️ **部分** | 前两项 ✅（`mask_secrets` + .env 不入库）；**权限过滤未生效**——`build_filters` 只接了 device_model/category，`user_role` 仅透传记录，代码注释写明"等权限分级表 FR-11 落地后再扩展" |
| NFR-04 | 提示词/阈值/权重全部配置化 | ✅ **基本达成** | 绝大部分在 `setting.py` + `.env`；少量策略常量仍在代码里（token 缓冲开关、多轮上下文轮数） |
| NFR-05 | 新增工具不改智能体代码；向量库访问收敛 | ✅ **达成** | 工具集中在 `tools/kb_tools.py`；向量库唯一出口 `rag/store.py` |
| NFR-06 | trace_id + 节点耗时 + 工具调用 + 引用 | ✅ **达成** | trace_id 贯穿 HTTP → 图 → 日志；`timings` 覆盖 8 个节点（含 augment 子耗时） |
| NFR-07 | Python 3.10~3.12；Chrome/Edge 最新两版 | ✅ **达成** | 3.10.12 实测通过 |

---

## 四、验收指标对照（G1 ~ G5）

| 编号 | 目标 | 目标值 | 当前 | 状态 |
| --- | --- | --- | --- | --- |
| G1 | 检索质量（Top-5 命中率） | ≥ 80% | **100%**（仅 20 条黄金集，且为兜底精排） | ⚠️ 缩样达成 |
| G2 | 引用覆盖率 | **100%** | 100% | ✅ |
| G3 | 要点命中率 | ≥ 80% | **44.7%**（未配 LLM，抽句式降级） | ❌ **未达标** |
| G3 | 无依据拒答率 | ≥ 90% | 100%（乙侧）；域外问题实测正确拒答 | ✅ |
| G4 | 图片关键信息提取准确率 | ≥ 85% | **未评测** | ❌ **未测** |
| G5 | 首 Token ≤ 2s | — | 未测 | ⚠️ |
| G5 | 文字 P95 ≤ 8s | — | 4.49s（稳态） | ✅ |
| G5 | 图片类 P95 ≤ 15s | — | 未测 | ❌ |

### 三个最关键的指标问题

**① G3 要点命中率 44.7% —— 这是当前最大的功能性缺口。**
根因不是代码，而是 **未配置 `DEEPSEEK_API_KEY`**，生成节点降级成"抽句式答案"（从上下文里抽句子拼起来，不是模型生成）。
**2026-09-18：密钥已配置**，但该指标**尚未在 LLM 路径下重跑**，44.7% 仍是目前唯一有据可查的数值。

**② G4 完全未测。**
需要三件事同时具备：`DASHSCOPE_API_KEY` + 图片 fixture 文件 + 评测脚本支持图片用例。
**2026-09-18：密钥已配置、`golden_image.jsonl` 已就位，但 fixture 实物图片仍缺失**，故 G4 仍不可测。

**③ G1/G3 的样本量不足。**
黄金集只有 **20 条**，文档要求 D8 扩到 **50 条**（构成：设备维护 40% / 工艺 25% / 标准 15% / 负样本 10% / 图片 10%）。
更要注意：**精排阈值 0.5、术语覆盖率 0.40 这两个阈值只在 20 条上标定过**，扩样后必须重标。

---

## 五、里程碑对照（D1 ~ D8）

| 天 | 主题 | 状态 | 缺口 |
| --- | --- | --- | --- |
| D1 | 环境与数据 | ✅ 完成 | 5 文档 / 32 切片，元数据完整率 100% |
| D2 | 索引与检索 | ✅ 完成 | 向量库 + BM25 + 一键重建 |
| D3 | 检索增强 | ✅ 完成 | 改写 / 术语归一 / 元数据过滤 / 精排 |
| D4 | 生成与引用（核心日） | ⚠️ 部分 | 引用校验与拒答 ✅；**图片识别节点未真实跑通**；培训模板 ✅ |
| D5 | 服务化 | ✅ 完成 | FastAPI + SSE + 上传 + 会话落盘 + 预热 |
| D6 | 前端 | ✅ 完成 | 三个视图、引用卡片、置信度标签、SSE 解析 |
| D7 | 工具与管理页 | ⚠️ **部分** | parts_query ✅、任务进度 ✅、受限 agent ✅；**MCP 工单未做**；**前端无上传页/切片查看页** |
| D8 | **评测与交付** | ❌ **基本未做** | 见下 |

### D8 未完成清单（交付日的工作几乎整块没做）

- [ ] 黄金集从 20 条扩到 **50 条**（含负样本与图片类）
- [ ] 评测脚本跑分并产出正式报告（`scripts/eval.py` 可用，但未产出报告）
- [ ] badcase 归因（`badcases.md` 仍是空表）
- [ ] **Docker Compose 部署**（`docker-compose.yml`、`deploy/Dockerfile.*`、`deploy/nginx.conf` 全部 0 字节）
- [x] README（已完成）
- [ ] 三个演示场景验收（场景 A 抢修 / B 新人学习 / C 参数查询）

---

## 六、工程结构缺口

文档 §6.2 的目录树中，**12 个文件仍是 0 字节**：

| 文件 | 归属 | 影响 |
| --- | --- | --- |
| `backend/mcp_client.py` | 甲 | MCP 客户端封装与降级——**未实现** |
| `backend/mcp_servers/ticket_server.py` | 甲 | 工单检索 MCP server——**未实现** |
| `backend/mcp_servers/__init__.py` | 甲 | 包标记 |
| `backend/tools/__init__.py` | 甲 | 本地工具 + MCP 工具聚合（含降级）——**未实现**；当前 augment 直接 import kb_tools，功能不受影响，但缺了文档要求的聚合层 |
| `deploy/Dockerfile.backend` | 甲 | 部署——**未实现** |
| `deploy/Dockerfile.frontend` | 甲 | 部署——**未实现** |
| `deploy/nginx.conf` | 甲 | **未实现**；文档 §8 缺口⑧指出必须关 `proxy_buffering` 并设 `X-Accel-Buffering`，否则 SSE 首 Token 退化 |
| `docker-compose.yml` | 甲 | **未实现** |
| `tests/test_api/test_sse.py` | 甲 | API 测试——**空**（接口层目前只有临时验证脚本，未沉淀为用例） |
| `tests/test_mcp/test_ticket_server.py` | 甲 | MCP 测试——**空** |
| `tests/test_multimodal/test_vision.py` | 甲 | **空**；文档要求含"识别失败必须拒答"用例 |
| `backend/__init__.py` | — | 空包标记，无需内容 |

> 当前测试总数 **48 个**，全部集中在 RAG 与链路层（`tests/test_rag/*`、`tests/test_agents/*`）。

---

## 七、超出文档的增量（已实现但文档未要求）

这些是设计过程中发现并补齐的，属于"比文档多做"：

| 项 | 说明 |
| --- | --- |
| `POST /api/admin/index/rebuild` + `GET /api/admin/jobs/{id}` | 文档 §5.4 接口表遗漏了索引重建入口，但 §6.2 又规定 admin.py 负责它——已补齐 |
| **安全前置提示** | `respond` 节点按回答内容自动追加 LOTO 能源隔离提示，走 `uncertain` 下发，不破坏已冻结的 SSE 契约 |
| `done` 事件追加 `qa_id` | 原本前端拿不到 qa_id，问答页反馈按钮只能置灰；纯追加字段、向后兼容 |
| 备件示例数据源 | `backend/config/parts_inventory.yaml`，含替代件兼容性依据与禁止替代条目 |
| 契约缺口①②的落地 | `AgentState` 追加 `device_model`/`category`/`answer_mode`/`user_role`，并打通 HTTP → 图的全链路透传 |
| 降级不中断 | 模型/数据库/MCP 任一不可用都不阻止启动，由 `/api/health` 如实报 `degraded` 并给出原因 |

---

## 八、环境前置条件（2026-09-18 复核：除图片 fixture 外均已解决）

| 项 | 现状 | 说明 |
| --- | --- | --- |
| 本地向量模型 `bge-large-zh-v1.5` | ✅ 已就绪（`backend/data/models`，约 3.4GB） | 向量库实跑 `local/chroma`（5 文档 / 32 切片），不再是 `hashing` 兜底 |
| 精排模型 `bge-reranker-v2-m3` | ✅ 已就绪 | 精排实跑 `CrossEncoderReranker`，不再是 `lexical` 降级 |
| `DEEPSEEK_API_KEY` | ✅ 已配置（`.env`） | `/api/health` 报 `llm.ok=true`（该字段只检查密钥是否存在，**非真实调用**）；**G3 要点命中率尚未重测**——下表 44.7% 是未配 LLM 时的降级值 |
| `DASHSCOPE_API_KEY` | ✅ 已配置（`.env`） | `/api/health` 报 `vlm.ok=true`（同上，仅配置检查）；**尚无真实 VLM 调用记录**，且 G4 仍受阻于图片 fixture |
| MySQL | ✅ 已接入（本机 `127.0.0.1:3307`，库 `smka`） | 由 SQLite 切至 MySQL；6 张业务表由 `db.py` 的 `init_db()` 启动时自动建好 |
| 图片 fixtures | **仍缺失** | `data/eval/golden_image.jsonl` 已就位，但其引用的 `fixtures/*.png` 在库中不存在 → 图片类评测仍无法执行 |

---

## 九、建议的收尾顺序

按"先保底线、再补交付"排：

**第一优先（保住文档 §7.4 的"不砍"项）**

1. ✅ 密钥已配置 —— **待办：重跑评测**，把 G3 要点命中率拉到 ≥80%（表内 44.7% 已失效）
2. `DASHSCOPE_API_KEY` 已配置；**待办：补图片 fixture** → 打通并评测 G4

> **2026-09-18 新增阻塞项（优先级高于上面两条）**：配上 `DEEPSEEK_API_KEY` 后，
> `citation.py` 的「高置信度必须 100% 引用覆盖率」硬闸门会把 LLM 生成的答案判为不达标而拒答
> （实测覆盖率落在 78%~94%）。招牌示例问题「刻蚀机腔体真空度异常怎么排查？」当前答不出来，
> 而 G2 的「覆盖率 100%」是靠拒答不达标的回答维持的。详见 `功能完成度清单.md` 缺陷 #10。

**第二优先（D8 交付）**

3. `docker-compose.yml` + `deploy/Dockerfile.*` + `deploy/nginx.conf`（nginx 必须关 `proxy_buffering`）
4. 黄金集扩到 50 条，重标精排阈值与术语阈值，产出正式评测报告
5. 三个演示场景走查（抢修 / 新人学习 / 参数查询）

**第三优先（按 §7.4 取舍顺序可砍）**

6. MCP 工单检索（`mcp_client.py` + `ticket_server.py` + 工具聚合）
7. 前端补：备件查询入口、知识入库页、切片查看页
8. 图片知识侧入库（抽图 + VLM caption）
9. 测试补齐：`test_api/test_sse.py`、`test_mcp/`、`test_multimodal/`
10. 高频问题记录、badcase 归因汇总、20 并发压测

---

## 十、当前可运行状态（已验证）

```
前端  http://127.0.0.1:5174     后端  http://127.0.0.1:8000（/docs）
测试  48 passed
验证  接口层 41/41 · 前端代理全链路 11/11 · 多轮与错误路径 8/8
索引  5 文档 / 32 切片 / 元数据完整率 100%
实测  答案 572 字 / 引用 8 条 / 置信度 0.73 / 首条引用「刻蚀设备维护手册 V3.2 P.3 §3.4」
```

启动命令：`make dev`（后端）、`make dev-web`（前端）。

> **注意**：以上是在 `EMBEDDING_PROVIDER=hashing` + `RERANK_PROVIDER=lexical` 兜底配置下跑通的。
> 换上真实模型后**必须重建索引**（manifest 签名不匹配会报 `EmbeddingMismatchError`）并重跑评测。

> **2026-09-18 更新**：真实模型已就位，索引已在 `local/chroma` 下重建（5 文档 / 32 切片），
> 精排为 `CrossEncoderReranker`，业务库切至本机 MySQL（`smka`，6 张表），`/api/health` 7 组件全 `ok`。
> 但上面 `## 十` 的实测数据仍出自上述兜底配置，**指标须在真实模型下重跑后才能更新**。
