# 使用手册

> **写给谁**：需要**演示、试用或验收**这套系统的人。本文只讲「打开哪个页面、点什么、会看到什么」。
>
> **不写给谁**：想改代码或对接接口的开发者 —— 请看 `README.md`（环境与启动）、
> `接口文档.md`（接口契约）、`前端对接说明（三块新业务）.md`（请求/响应与按钮状态机）。
> 本文不重复接口细节。

---

## 0. 先看这张表

系统一共 **7 个页面**，顶栏分成**两组**，中间用一条竖线隔开：

| 组 | 页面 | 路由 | 一句话 |
| --- | --- | --- | --- |
| **问答组** | 问答 | `/chat` | 知识库问答 + 引用溯源（本系统的主线能力） |
| | 历史会话 | `/history` | 找回之前的问答记录 |
| | 管理 | `/admin` | 知识库索引状态与重建 |
| **业务组** | 维护计划 | `/plans` | 按设备运行量算「什么时候该保养」 |
| | 备件商城 | `/parts` | 查备件、加购物车、拟采购申请 |
| | 采购结算 | `/procurement` | 走完采购审批、登记结算台账 |
| | 考核认证 | `/training` | 出题考试、发内部认证、到期提醒 |

**两组是两条独立链路**：业务组的四个页面**不依赖**问答链路。就算模型、向量库或
MCP 任一环节挂了，业务页照样能用；反过来也一样。演示时这一点是加分项，可以主动说明。

---

## 1. 启动（最短路径）

```bash
make dev        # 后端 API，http://127.0.0.1:8000
make dev-web    # 前端页面，http://127.0.0.1:5173
```

浏览器打开 **http://127.0.0.1:5173**。

首次使用需要先建索引（否则问答页会提示检索不到内容）：

```bash
make ingest     # 解析 -> 切片 -> 向量化；加 --rebuild 可先清空重来
```

> ⚠️ **后端启动要等一会儿**。它要加载嵌入模型（bge-large-zh）和精排模型（bge-reranker），
> 期间端口还没开始监听，访问会连不上。看到日志出现
> `数据库已就绪：N 张表` 和 `LangGraph 图已就绪` 才算好。
>
> 问答功能需要 `DEEPSEEK_API_KEY`；识图需要 `DASHSCOPE_API_KEY`。**不配也能跑**：
> 系统会走本地降级路径而不是报错。业务组四个页面完全不依赖密钥。

---

## 2. 问答页（`/chat`）—— 主线能力

### 怎么问

输入框支持 **Enter 发送、Shift+Enter 换行**，也可以直接**粘贴截图**。工具栏有三个按钮：

- **「图片」** —— 上传设备照片/参数表截图，系统会先识别图片再据此作答；
- **「检索条件」** —— 展开后可限定**设备型号**（如 `Etcher-A`）和**分类**（设备维护/工艺/标准），
  用于缩小检索范围；
- **「问答」/「培训讲解」** —— 切换回答风格。注意「培训讲解」旁标着 **「实验性」**。

输入框上方还有三个**快捷提问**：**「备件库存」「替代件」「参数查询」**。点它们只是把
问题**填进输入框**，不会自动发送 —— 你可以先改再发。（若输入框里已有别的内容，
它**不会覆盖**，而是提示你先清空。）

### 回答怎么看（这是本系统的重点）

每条回答都会带一个**状态条**，含义务必分清：

| 你看到 | 含义 | 该怎么做 |
| --- | --- | --- |
| **「知识库未覆盖该问题」** | **拒答**。检索不到可靠依据，按零幻觉策略**不给操作步骤** | 换问法，或先补充知识库 |
| **「结论需人工确认」** | 中等置信度：证据相关但覆盖不全 | **对着下方引用原文复核后再执行** |
| **「低置信度：仅提供相关材料清单」** | 证据很弱，只列材料不结论 | 视为线索，不要当结论用 |
| **「以下内容存疑，请人工核实」** | 结论与依据存在出入 | 人工判断 |
| **「已停止生成…」** | 你点了「停止」 | 已收到的内容仍可看 |

**拒答是设计行为，不是故障。** 设备维护场景下给不出依据的操作步骤比不回答更危险 ——
演示时这一点值得主动强调。

### 引用溯源

回答下方是 **「引用依据」** 清单，每条显示来源文档、**版本**、章节、页码。
点引用角标会打开 **「引用原文」** 抽屉，能看到切片原文；图片类依据会显示原图。

> 引用编号由后端代码分配，**模型不能自己编造编号**。

页脚显示状态、置信度、耗时和 `trace_id`（排障时给后端看这个），旁边有 **「复制」** 按钮。

---

## 3. 维护计划（`/plans`）

**它解决什么**：设备手册里写着「每 4000 运行小时更换过滤器」这类周期条款，本页根据
设备的**累计运行量**对照这些条款，算出「哪些项目该做了」。

### 操作步骤

1. 在 **「生成计划」** 区填表。带默认值，直接点也行：

   | 字段 | 默认值 | 说明 |
   | --- | --- | --- |
   | 设备型号 | `Etcher-A` | **必填** —— 它决定命中哪些设备手册 |
   | 设备编号 | `ETCH-01` | 选填 |
   | 累计运行小时 | `5200` | |
   | 累计生产片数 | `46000` | |
   | 距上次 PM（小时 / 片数） | 留空 | **留空即用上面的累计值** |
   | 召回条数 top_k | `8` | 1–20 |

2. 二选一：
   - **「生成预览」** —— 只算不存，用来试算；
   - **「生成并保存」** —— 算完落库，之后能在「计划列表」里跟踪。

3. 结果分两块。**「生成结果」** 表格列出算出的项目；下方若出现告警
   **「以下项目手册未给出周期，需人工确认」**，那是**如实列出**手册里没写周期的项目 ——
   系统**不会**替它们编一个周期出来。演示时这是个好细节。

4. 在 **「计划列表」** 里跟踪。可用状态筛选（全部/未到期/已到期/已完成/已跳过），
   每行可点：

   - **「完成」** —— 完成后自动**滚动到下一周期**；
   - **「跳过」** —— 需填原因；
   - **「依据 (N)」** —— 打开抽屉看这条计划是**从手册哪一页哪一节**推出来的。

> **注意**：预览结果**没落库**，对它点「完成」会提示
> 「该条目尚未落库（预览结果），无法标记状态」。

### 几个状态的含义

| 状态 | 含义 |
| --- | --- |
| 未到期 | 还没到执行点，剩余量显示「还剩 X」 |
| 已到期 | 已超期，显示「已超期 X」 |
| 已完成 | 已执行，周期已滚动 |
| 已跳过 | 本次不做，记录了原因 |

到期日无值时显示 **「按需执行」**（比如「每次开腔」这类口径）。

---

## 4. 备件商城（`/parts`）

左栏是**备件目录**，右栏是**购物车**。

### 找件与看详情

1. 左栏填**设备型号**（默认 `Etcher-A`）和**关键词**（如 `O-ring`），点 **「搜索」**。
2. 表格列出：编码、名称、规格、库存、库位、到货天数、单价、供应商、替代件。
   - 库存列会区分 **「缺货（N 天到货）」/「库存偏低」/「N 单位」**；
   - 单价缺失时显示 **「台账未提供」**。
3. 点 **「详情」** 打开抽屉，能看到：
   - **「禁止替代清单（安全信息）」** —— 台账里登记的**禁止替代项**，这条必须看；
   - **可用替代件列表**，每个替代件都带**兼容性依据**。

### 加入购物车

点 **「加入购物车」**，或在详情抽屉里点 **「加入购物车（本件）」** /
**「加入购物车（替代件）」**。

> **购物车就是一张草稿采购申请单**，首次加入会自动建单，标题栏会显示单号
> （如 `PO-20260920-0012`）。
>
> **重要**：加购物车**不会向供应商下单**。它只是拟一张**内部**申请单，
> 真正的确认点是后面的「人工确认」。

购物车里可以改数量、**「移出」**，也可点 **「新建购物车」** 另起一单。

### 提交

填 **申请人**（提交前必填，不填会被拦），点 **「提交采购申请」**。
提交后明细**冻结** —— 数量框、移出按钮全部关闭，这是防篡改设计。

购物车为空时提交会被拦下（提示 `EMPTY_ORDER`），这是有意为之。

---

## 5. 采购结算（`/procurement`）

两个 Tab：**「申请单」** 和 **「结算台账」**。

### 采购单的状态流转

```
草稿（购物车） → 已提交（待确认） → 已确认（待收货） → 已收货（待结算）
      ↓驳回/撤销                ↓驳回
   已驳回 / 已撤销（终态）
```

进入某张单的 **「详情」** 抽屉，可执行的动作区会显示当前能做什么：

| 按钮 | 含义 |
| --- | --- |
| **「提交申请」** | 草稿 → 已提交 |
| **「人工确认」** | 已提交 → 已确认。**这是系统唯一的确认点** |
| **「驳回」** | 审批不通过，**必须填原因**（会写进订单审计行） |
| **「撤销申请」** | 申请人作废，**必须填原因** |
| **「确认收货」** | 已确认 → 已收货 |
| **「登记结算」** | 打开弹窗登记结算台账 |

> **按钮是后端给的**，前端不硬编码状态机 —— 换句话说，**页面上没显示的按钮就是当前
> 状态不能做的操作**。终态单据会提示「当前状态没有可执行动作，本单为终态，只读」。
>
> 如果两人同时操作同一张单，后手会看到 **「状态已变更，请重试」**，列表会自动刷新。

### 结算

点 **「登记结算」** 弹窗填：结算金额（**留空即用订单金额**）、结算方式（默认「月结」）、
发票号、经办人，点 **「确认登记」**。

切到 **「结算台账」** Tab 可看全部结算记录，支持按订单号模糊搜索。

> **结算只登记台账，不做财务过账** —— 财务仍以 ERP 为准。页面也会这样提示。
>
> 另外「结算」不是状态流转环节：登记后订单仍留在「已收货（待结算）」列表里。

---

## 6. 考核认证（`/training`）

页面从上到下是四块：**出题 → 答题 → 判分结果 → 认证记录**，顶部还有一条到期提醒。

### 完整流程

1. **出题**：填**设备型号**（默认 `Etcher-A`）、**主题**（如「真空系统」，留空则不限）、
   **难度**（基础/进阶）、**题目数量**（1–10）、**题型**（单选/多选/判断/简答，至少选一种），
   点 **「生成题目」**。

   > 每道题**都带依据**。如果某个主题实在抽不到有依据的题，系统会提示
   > 「该主题抽不到有依据的题目，换个主题或设备再试」——
   > **宁可不命题，也不出没有依据的题**。
   >
   > ⚠️ **实际题量可能少于你填的数量**。依据校验不通过的题会被**直接剔除**（不补题重试），
   > 例如模型给某个数值配了依据里查不到的内容。这时页面会提示
   > 「要求 N 题，M 题因依据不足已剔除」—— 这不是故障，是零幻觉的必然结果。

2. **答题**：逐题作答，每题可展开看依据。顶部填 **答题人**（必填，证书上署的就是这个名字）。
   计时自动开始。

3. **交卷**：点 **「交卷判分」**。若还有题没答，会弹确认框提醒（判断题漏选会按「错误」计）。

4. **判分结果**：显示**通过/未通过**、得分和及格线，并逐题列出你的作答、标准答案、解析与依据。

5. **申请认证**（**只有通过后才出现**）：点 **「申请认证」** 打开弹窗，可选**等级**
   （L1/L2/L3）和**有效期**（天数），签发方固定为「内部授权」。点 **「确认发证」** 完成。

### 认证记录与到期提醒

底部 **「认证记录」** 表列出：证书 ID、姓名、设备、等级、分数、签发方、签发时间、
到期时间、状态、备注。状态会自动区分 **已吊销 / 已过期 N 天 / N 天后到期 / 有效**。

页面顶部的告警条显示 **「30 天内到期或已过期共 N 条」**，可展开查看。

### 两条必须知道的红线

1. **不会自动发证**。交卷判分**不会**同时发证，发证必须显式点按钮，且**必须基于一次通过
   的考核**。没通过就点发证会被拒（`ATTEMPT_NOT_PASSED`）。
2. **签发方固定为「内部授权」**，不可改成别的；界面与证书备注都会注明
   「系统不代表设备原厂签发」。

---

## 7. 演示脚本（建议按这个顺序）

配合录屏，全程约 6–8 分钟。数据都是示例数据，可放心操作。

1. **问答（主线）** —— 问一个语料里有的问题（如 O-ring 更换周期），展示**引用溯源**：
   点开引用看原文、版本、页码。然后再问一个语料里**没有**的（如「明年的维保预算」），
   演示**拒答**：「知识库未覆盖该问题」。

2. **维护计划** —— 用默认值点 **「生成并保存」**。指出「未覆盖项」告警：
   手册没写周期的项目被如实列出，**没有编造**。展开某条的「依据」看它来自手册哪一节，
   点「完成」后刷新，看周期滚动。

3. **备件商城** —— 搜 `O-ring`，看库存、单价、替代件。点进详情展示
   **「禁止替代清单」**。加几件进购物车，改数量，看合计金额变化。
   指出提示语：**这只是在拟内部申请单，不会向供应商下单**。

4. **采购结算** —— 先演示**空车提交被拦**。填申请人后提交，再进详情依次
   「人工确认」→「确认收货」→「登记结算」。切到结算台账看汇总。
   回到订单详情，指出**明细已冻结**（改数量/移出按钮置灰）。

5. **考核认证** —— 按「Etcher-A / 真空系统」出 5 题，答完交卷。
   （实际题量可能少于 5 题并弹出「N 题因依据不足已剔除」提示 —— 顺势说明这就是
   「宁可不命题，也不出没有依据的题」，是加分项而非故障。）
   先故意答错几题演示**未通过时发不了证**，再「重新作答」通过，
   点「申请认证」发 L2 证，最后看认证记录与到期提醒。

6. **独立性（加分项）** —— 说明业务组四个页面与问答链路完全解耦，
   不共享会话、不走 SSE，问答链路降级不影响业务页。

---

## 8. 红线（系统层面的硬约束）

这些不是文档约定，是**代码强制**的：

| 红线 | 表现 |
| --- | --- |
| **不对供应商发起真实下单** | 只生成内部采购申请单；「人工确认」是唯一确认点 |
| **替代件必须有兼容性依据** | 台账里查不到依据会直接报 `SUBSTITUTE_BASIS_REQUIRED` |
| **不自动发证** | 判分接口返回的 `certification_id` 恒为 `null`，发证须显式调用 |
| **不编造依据** | 计划的周期值只能从检索到的原文里抽取；出题若引用越界则**丢弃该题** |
| **无依据不回答** | 问答检索不到可靠依据时**拒答**，不给操作步骤 |

---

## 9. 常见问题

**Q：页面能打开，但接口全报错？**
先确认后端真的起来了。后端加载模型期间端口还没监听 —— 看日志出现
`数据库已就绪` 才算就绪。

**Q：问答一直拒答？**
可能索引没建（跑 `make ingest`），或没配 `DEEPSEEK_API_KEY`。用「检索条件」限定
设备型号再试。也可能是问题确实超出知识库范围 —— 那拒答就是正确行为。

**Q：业务页提示功能不可用？**
业务组四个页面不依赖模型和密钥。若报 `DB_UNAVAILABLE`，检查 `DATABASE_URL` 指向的
数据库是否可连。

**Q：点「完成」提示「尚未落库」？**
你操作的是**预览**结果。改用「生成并保存」重新生成。

**Q：采购单的按钮和我预期的不一样？**
按钮由后端按当前状态下发。没显示的就是该状态不允许做的；单据到终态后整体只读。

**Q：数字对不上？**
页面上的金额一律取后端返回的 `total_amount`，前端不自行累加。若有出入请后端核对。

---

## 10. 相关文档

| 想了解 | 看哪份 |
| --- | --- |
| 环境准备、启动、常用命令 | `README.md` |
| 接口契约（请求/响应/错误码） | `接口文档.md` §10 |
| 三块新业务的对接细节与按钮状态机 | `前端对接说明（三块新业务）.md` |
| 交付范围与完成度 | `交付说明.md`、`功能完成度清单.md` |
| 检索与问答链路的实现 | `RAG实现说明.md` |
