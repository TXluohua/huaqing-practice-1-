# 路由层接口文档

> **项目**：半导体设备维护知识库智能问答系统
> **版本**：v0.1（接口先行 —— 契约变更必须先改本文档与测试，再改实现，见开发文档 §6.4）
> **设计依据**：开发文档 §5.4 接口设计、§5.6 数据库设计、§4.3 节点设计
> **范围**：`backend/routers/` 全部对外 HTTP 接口 + SSE 事件契约

---

## 1. 分层与设计约定

### 1.1 职责边界

```
routers/          ← 本文档描述的范围：只做 HTTP 翻译（async def + Depends）
  ↓
services/         ← 会话管理、调图、包 SSE 响应；不碰 HTTP 细节
  ↓
agents/ tools/ rag/   ← 业务实现
  ↓
setting.py / config/  ← 配置与提示词
```

依开发文档 §6.1，**依赖方向单向**，禁止反向 import。因此：

- `routers/` 内**不写业务逻辑**、不直接调用 `rag/` 或向量库；
- 所有参数校验在 `schemas.py` 完成，路由函数只做「取参 → 调 service → 返回」。

### 1.2 统一约定

| 项 | 约定 |
| --- | --- |
| 基础路径 | `/api`（前端 dev 通过 Vite 代理转发，见 `frontend/vite.config.ts`） |
| 版本策略 | 本期不引入 `/v1`；契约破坏性变更时再引入前缀 |
| 编码 | 一律 UTF-8；JSON 用 `application/json` |
| 成功响应 | **直接返回业务数据对象**，不套 `{code,data}` 信封 |
| 失败响应 | HTTP 状态码表达成败 + 统一错误体（见 §5） |
| 链路追踪 | 每个响应（含 SSE `meta` 事件）带 `trace_id`，对应 NFR-06 |
| 时间格式 | ISO 8601 带时区，如 `2026-09-15T12:30:00+08:00` |
| 命名 | 路径全小写 + 连字符；查询参数蛇形（`session_id`）|

> **为什么不用 `{code, data, message}` 信封**：SSE 无法使用信封，若 JSON 用、SSE 不用，
> 前端要维护两套解包逻辑。统一用 HTTP 状态码后，两套通道的错误语义一致；
> 且 nginx / 监控可直接按状态码统计。若后续前端希望统一拦截，可在 `api/http.ts` 内封装。

### 1.3 鉴权（本期最简实现）

开发文档 §1.3 明确「生产级权限体系」不在本期范围，但 §1.4 要求**检索即鉴权**——
权限过滤必须发生在检索层，而非「先检索再删答案」。因此接口层需把身份**透传**到检索层：

| 请求头 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `X-User-Id` | string | 否 | 缺省 `anonymous` |
| `X-User-Role` | enum | 否 | `engineer` / `admin`，缺省 `engineer` |

本期不校验签名，仅做透传与日志脱敏（NFR-03）。**检索层据此过滤**，接口层不裁剪答案。

---

## 2. 接口分档总览

按**优先级分档** + **模块分组**。优先级取自开发文档 §3.1 的 FR 优先级。

### P0 档（主链路，D5 / D7 必须交付）

| 模块 | 方法 | 路径 | 用途 | 对应 FR |
| --- | --- | --- | --- | --- |
| 健康 | GET | `/api/health` | 健康检查：向量库 / 模型 / VLM / MCP / 图 | NFR-06 |
| 问答 | POST | `/api/chat/stream` | **核心**：提问，SSE 流式返回答案 | FR-01/03/04/05 |
| 上传 | POST | `/api/upload/image` | 上传图片，返回 `image_id` | FR-02 |
| 管理 | POST | `/api/admin/documents` | 上传文档并触发入库 | FR-08 |
| 管理 | POST | `/api/admin/index/rebuild` | **一键重建索引**（⚠️ 文档 §5.4 遗漏，本文档补充） | FR-08 |

### P1 档（体验闭环）

| 模块 | 方法 | 路径 | 用途 | 对应 FR |
| --- | --- | --- | --- | --- |
| 会话 | GET | `/api/chat/sessions` | 会话列表 | FR-06 |
| 会话 | GET | `/api/chat/sessions/{id}/messages` | 历史消息（含引用） | FR-06 |
| 反馈 | POST | `/api/feedback` | 四态反馈落库 | FR-07 |
| 管理 | GET | `/api/admin/chunks` | 切片查询（调试用） | FR-08 |
| 管理 | GET | `/api/admin/jobs/{job_id}` | 异步任务进度查询（⚠️ 补充） | FR-08 |

### P2 档（可选扩展，本文档建议）

| 模块 | 方法 | 路径 | 用途 |
| --- | --- | --- | --- |
| 会话 | DELETE | `/api/chat/sessions/{id}` | 删除会话 |
| 会话 | PATCH | `/api/chat/sessions/{id}` | 重命名会话 |
| 备件 | POST | `/api/parts/query` | FR-10 备件查询与询价单草稿 |

### 当前实现状态

| 接口 | 可否现在开工 | 阻塞项 |
| --- | --- | --- |
| `/api/health`、`/api/chat/stream` | ✅ **可以** | 无。`agent_factory.astream()` 已可用，骨架期返回 `NOT_COVERED` 状态 |
| `/api/upload/image` | ✅ 可以 | 无（`utils/image.py` 待实现） |
| `/api/feedback`、会话类 | ⚠️ 需先建表 | `db.py` 与 MySQL |
| `/api/admin/*` | ⚠️ 需先有 RAG | `rag/ingest.py`、`rag/store.py` |

---

## 3. 通用数据结构

### 3.1 Citation（引用项）

与 `backend/agents/state.py` 的 `Citation` 模型、SSE `citations` 事件**三处字段完全一致**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int | 引用编号 `[n]`，**由代码分配，禁止模型编造** |
| `source_type` | enum | `kb_doc` / `ticket` / `image` |
| `doc` | string\|null | 文档名 |
| `version` | string\|null | 文档版本（P5 版本混乱痛点的关键字段） |
| `section` | string\|null | 章节 |
| `page` | int\|null | 页码 |
| `chunk_id` | string\|null | 切片 ID，用于「查看原文」 |
| `image_url` | string\|null | 图片类依据的原图地址 |
| `snippet` | string\|null | 原文片段 |

### 3.2 置信度三级

依 §3.3 FR-05：`confidence = 0.40×精排Top1归一化分 + 0.35×引用覆盖率 + 0.25×来源权威度`

| label | 区间 | 呈现 |
| --- | --- | --- |
| `high` | ≥ 0.80 | 直接给结论 |
| `medium` | 0.60–0.80 | 给结论 + 标注「需人工确认」 |
| `low` | < 0.60 | 只给相关材料清单 + 未覆盖说明，**不给操作步骤** |

### 3.3 status 取值

| 值 | 含义 |
| --- | --- |
| `OK` | 正常返回 |
| `NOT_COVERED` | 知识库未覆盖，拒答（**图内初始值即为此，fail-closed**） |
| `ERROR` | 链路异常 |

---

## 4. 接口详细设计

### 4.1 GET `/api/health`

**用途**：健康检查。供运维与前端启动自检，也是 R7「MCP 加载失败」降级状态的暴露点。

**请求参数**（query）

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `verbose` | bool | 否 | `false` | 是否返回各组件耗时明细 |

**响应 200**

```json
{
  "status": "degraded",
  "version": "0.1.0",
  "components": {
    "vector_store": { "ok": true,  "detail": "chroma: 1240 chunks" },
    "llm":          { "ok": true,  "detail": "deepseek-chat" },
    "vlm":          { "ok": false, "detail": "DASHSCOPE_API_KEY 未配置" },
    "mcp":          { "ok": true,  "tools": ["ticket_search"], "degraded": false },
    "graph":        { "name": "smka_qa_graph", "ready": true, "nodes": [],
                      "checkpointer": "sqlite-async", "persistent": true }
  },
  "trace_id": "9740d1e9db784095942dd80f87be6cc7"
}
```

- `status`：`ok` 全部正常 / `degraded` 部分降级但主链路可用 / `down` 主链路不可用
- `graph` 字段**直接来自已实现的** `agent_factory.graph_summary()`，无需额外开发

---

### 4.2 POST `/api/upload/image`

**用途**：上传图片，返回 `image_id`。**此接口只做预处理，不做识别**（识别在问答链路的 `ingest_image` 节点）。

**请求**：`multipart/form-data`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `file` | File | ✅ | 图片文件 |
| `session_id` | string | 否 | 关联会话，便于历史回看 |

**约束**（数值需写入 `setting.py`，NFR-04 配置化）

| 项 | 值 |
| --- | --- |
| 允许格式 | `jpg` / `jpeg` / `png` / `webp` |
| 单文件上限 | 5 MB |
| 最小边长 | 200 px（低于此值识别质量不可用） |
| 处理 | 压缩 + 格式统一 + 体积校验（`utils/image.py`） |
| 存储 | `backend/data/uploads/`（`UPLOAD_DIR`） |

**响应 200**

```json
{
  "image_id": "img_7f3a9c21",
  "filename": "alarm_E2041.png",
  "mime": "image/png",
  "size_bytes": 184320,
  "width": 1280,
  "height": 720,
  "url": "/static/uploads/img_7f3a9c21.png",
  "trace_id": "..."
}
```

**错误**：`413 IMAGE_TOO_LARGE` / `415 UNSUPPORTED_IMAGE_TYPE` / `400 EMPTY_FILE` / `400 IMAGE_TOO_SMALL`

> **使用方式**：先上传拿 `image_id` → 再在 `/api/chat/stream` 的 `image_ids` 中引用。
> SSE 端点**不处理文件上传**（§5.4）。

---

### 4.3 POST `/api/chat/stream`　★核心接口

**用途**：提问并流式返回答案。承载 FR-01（多轮问答）、FR-02（多模态）、FR-03（跨源检索）、
FR-04（答案溯源）、FR-05（置信度与拒答）、FR-09（培训分层讲解）。

**请求**：`application/json`

| 字段 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `question` | string | ✅ | — | 用户问题，长度 1–2000 |
| `session_id` | string\|null | 否 | `null` | 缺省则**新建会话**并在 `meta` 事件中回传 |
| `image_ids` | string[] | 否 | `[]` | 图片 ID，最多 3 个（先经上传接口获得） |
| `mode` | enum | 否 | `qa` | `qa` 问答 / `training` 培训分层讲解（FR-09） |
| `device_model` | string\|null | 否 | `null` | 设备型号，用于**元数据过滤**（FR-03） |
| `category` | string\|null | 否 | `null` | 分类过滤：设备维护 / 工艺 / 标准 |

**请求示例**

```json
{
  "question": "刻蚀机腔体真空度异常波动，应该按什么顺序排查？",
  "session_id": "9f2c1a7b",
  "image_ids": ["img_7f3a9c21"],
  "mode": "qa",
  "device_model": "Etcher-A"
}
```

**响应 200**：`Content-Type: text/event-stream`

事件契约见 §6。**注意**：前端必须用 `fetch + response.body.getReader()` 解析，
**不使用 `EventSource`**（只支持 GET、无法带自定义请求头，见 §5.5）。

**⚠️ 契约依赖**：`mode` 字段目前**在 `state.py` 中不存在**，见 §8 缺口 ①。

---

### 4.4 GET `/api/chat/sessions`

**用途**：会话列表（FR-06）。数据源为 `session_map` 表。

**请求参数**

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `limit` | int | 否 | 20 | 1–100 |
| `offset` | int | 否 | 0 | 分页偏移 |
| `keyword` | string | 否 | — | 按标题模糊搜索 |

**响应 200**

```json
{
  "items": [
    {
      "session_id": "9f2c1a7b",
      "thread_id": "thread-9f2c1a7b",
      "title": "真空度异常排查",
      "message_count": 6,
      "last_question": "为什么先查 O-ring？",
      "updated_at": "2026-09-15T12:30:00+08:00"
    }
  ],
  "total": 1, "limit": 20, "offset": 0, "trace_id": "..."
}
```

---

### 4.5 GET `/api/chat/sessions/{session_id}/messages`

**用途**：历史消息回看，含引用（FR-04/FR-06）。数据源为 `qa_record` 表。

**路径参数**：`session_id`（必填）

**请求参数**

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `limit` | int | 否 | 50 | 1–200 |
| `offset` | int | 否 | 0 | 分页偏移 |
| `with_citations` | bool | 否 | `true` | 是否返回引用清单 |

**响应 200**

```json
{
  "session_id": "9f2c1a7b",
  "thread_id": "thread-9f2c1a7b",
  "items": [
    {
      "qa_id": 101,
      "role": "user",
      "content": "刻蚀机腔体真空度异常波动，应该按什么顺序排查？",
      "image_ids": ["img_7f3a9c21"],
      "created_at": "2026-09-15T12:30:00+08:00"
    },
    {
      "qa_id": 101,
      "role": "assistant",
      "content": "1. 检查腔体密封 O-ring [1]…",
      "citations": [
        { "id": 1, "source_type": "kb_doc", "doc": "刻蚀设备维护手册",
          "version": "V3.2", "section": "3.4", "page": 118,
          "chunk_id": "c_00871", "image_url": null, "snippet": "…" }
      ],
      "confidence": 0.86,
      "confidence_label": "high",
      "status": "OK",
      "latency_ms": 3120,
      "created_at": "2026-09-15T12:30:03+08:00"
    }
  ],
  "total": 2, "limit": 50, "offset": 0, "trace_id": "..."
}
```

**错误**：`404 SESSION_NOT_FOUND`

---

### 4.6 POST `/api/feedback`

**用途**：四态反馈落库（FR-07），驳回案例计入 `badcases.md`。数据源为 `qa_feedback` 表。

**请求**：`application/json`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `qa_id` | int | ✅ | 被反馈的问答记录 ID |
| `type` | enum | ✅ | `adopt` 采纳 / `partial` 部分采纳 / `reject` 驳回 / `correct` 修正 |
| `comment` | string\|null | 否 | 备注，≤500 字 |
| `corrected_answer` | string\|null | 条件必填 | `type=correct` 时**必填** |

**响应 200**

```json
{ "feedback_id": 55, "recorded": true, "created_at": "2026-09-15T12:31:00+08:00", "trace_id": "..." }
```

**错误**：`404 QA_NOT_FOUND` / `400 CORRECTED_ANSWER_REQUIRED`

---

### 4.7 POST `/api/admin/documents`

**用途**：上传文档并触发入库流水线（FR-08）：解析 → 抽图 → 清洗 → 切片 → 向量化 → 入库。
**长耗时任务，异步执行**，立即返回 `job_id`。

**请求**：`multipart/form-data`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `file` | File | ✅ | 支持 PDF / MD / TXT / DOCX / PPTX / XLSX |
| `doc_title` | string | ✅ | 文档标题 |
| `version` | string | ✅ | **必填**，元数据完整率 100% 要求 `page` + `version` 齐备 |
| `category` | string | ✅ | 设备维护 / 工艺 / 标准 |
| `uploader` | string | 否 | 上传人 |

**响应 202**

```json
{ "doc_id": 12, "job_id": "job_a1b2c3", "status": "processing", "trace_id": "..." }
```

**错误**：`415 UNSUPPORTED_DOC_TYPE` / `409 DOC_VERSION_EXISTS`

---

### 4.8 POST `/api/admin/index/rebuild`　⚠️ 文档补充

**用途**：一键重建索引（FR-08，验收标准 ≤ 5 分钟）。
**文档 §5.4 的接口表遗漏了此接口**，但 §6.2 明确 `admin.py` 负责「文档入库 / 索引重建 / 切片查询」。

**请求**：`application/json`

| 字段 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `scope` | enum | 否 | `all` | `all` 全量 / `incremental` 增量 |
| `force` | bool | 否 | `false` | 是否强制重建（忽略切片版本） |

**响应 202**：`{ "job_id": "job_x9y8z7", "status": "running", "trace_id": "..." }`

---

### 4.9 GET `/api/admin/jobs/{job_id}`　⚠️ 文档补充

**用途**：查询异步任务（入库 / 重建索引）进度。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `job_id` | string | 路径参数，必填 |
| `status` | enum | `pending` / `running` / `succeeded` / `failed` |
| `progress` | float | 0–1 |
| `message` | string | 当前阶段描述，如「切片 128/540」 |
| `result` | object\|null | 完成后结果，如 `{"chunks": 540, "images": 32}` |

---

### 4.10 GET `/api/admin/chunks`

**用途**：切片查询，**调分块参数时用**（§6.2）。

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `doc_id` | int | 否 | — | 按文档过滤 |
| `q` | string | 否 | — | 按内容模糊搜索 |
| `page` | int | 否 | — | 按页码过滤 |
| `limit` / `offset` | int | 否 | 20 / 0 | 分页 |

**响应**：`{ "items": [{ "chunk_id", "doc", "version", "section", "page", "token_len", "text", "image_url" }], "total", ... }`

---

## 5. 统一错误体

```json
{
  "code": "IMAGE_TOO_LARGE",
  "message": "图片超过 5MB 限制",
  "detail": { "max_mb": 5, "actual_mb": 7.2 },
  "trace_id": "9740d1e9db784095942dd80f87be6cc7"
}
```

| HTTP | code | 触发场景 |
| --- | --- | --- |
| 400 | `INVALID_ARGUMENT` | 参数校验失败（Pydantic） |
| 400 | `EMPTY_FILE` / `IMAGE_TOO_SMALL` | 文件为空 / 图片过小 |
| 400 | `CORRECTED_ANSWER_REQUIRED` | 反馈类型为修正但未给修正内容 |
| 404 | `SESSION_NOT_FOUND` / `QA_NOT_FOUND` / `JOB_NOT_FOUND` | 资源不存在 |
| 409 | `DOC_VERSION_EXISTS` | 同文档同版本已存在 |
| 413 | `IMAGE_TOO_LARGE` | 图片超限 |
| 415 | `UNSUPPORTED_IMAGE_TYPE` / `UNSUPPORTED_DOC_TYPE` | 格式不支持 |
| 422 | `QUESTION_TOO_LONG` | 问题超过 2000 字 |
| 429 | `RATE_LIMITED` | 超过 20 并发（NFR-02） |
| 500 | `INTERNAL_ERROR` | 未预期异常 |
| 503 | `VECTOR_STORE_UNAVAILABLE` / `LLM_UNAVAILABLE` | 依赖不可用 |

> **降级优先于报错**：依开发文档 R7 与已实现的 `memory.py` 降级策略，
> MCP / 检查点等**非关键依赖**失败时不得中断主链路，应降级并在 `errors` 中记录，
> 仅在主链路（向量库 / LLM）不可用时才返回 503。

---

## 6. SSE 事件契约（前后端唯一契约）

响应头：`Content-Type: text/event-stream`、`Cache-Control: no-cache`、`X-Accel-Buffering: no`（关闭 nginx 缓冲）
事件之间以**空行**分隔。

| 顺序 | event | data | 对应 `AgentState` 字段 |
| --- | --- | --- | --- |
| 1 | `meta` | `{session_id, trace_id, ts}` | `session_id` / `trace_id` |
| 2 | `image` | `{image_id, image_type, extracted{}, confidence}` | `image_result` |
| 3 | `token` | `{delta}` | `answer` 的增量 |
| 4 | `citations` | `{items: Citation[]}` | `citations` |
| 5 | `done` | `{status, confidence, label, uncertain[]}` | `status` / `confidence` / `confidence_label` / `uncertain` |
| — | `error` | `{code, message}` | `errors` |

> 上表右列说明：**本契约与已实现的 `state.py` 逐字段对齐**，
> 因此 `agent_factory.astream()` 的产出可直接映射为 SSE 事件，无需中间转换层。

**完整示例**

```
event: meta
data: {"session_id":"9f2c1a7b","trace_id":"9740d1e9","ts":"2026-09-15T12:30:00+08:00"}

event: image
data: {"image_id":"img_7f3a9c21","image_type":"alarm_screen","extracted":{"alarm_code":"E-2041"},"confidence":0.92}

event: token
data: {"delta":"1. 检查腔体密封 "}

event: token
data: {"delta":"O-ring [1]…"}

event: citations
data: {"items":[{"id":1,"source_type":"kb_doc","doc":"刻蚀设备维护手册","version":"V3.2","section":"3.4","page":118,"chunk_id":"c_00871","image_url":null,"snippet":"…"}]}

event: done
data: {"status":"OK","confidence":0.86,"label":"high","uncertain":[]}
```

**拒答示例**（`status=NOT_COVERED`，不给具体操作步骤）

```
event: meta
data: {"session_id":"9f2c1a7b","trace_id":"…","ts":"…"}

event: token
data: {"delta":"知识库未覆盖该问题。建议：…"}

event: done
data: {"status":"NOT_COVERED","confidence":0.12,"label":"low","uncertain":["未检索到该型号腔体清洗周期"]}
```

**图片识别失败**（§4.1 FR-02 硬性要求：不允许猜测）

```
event: image
data: {"image_id":"img_xxx","image_type":"unknown","extracted":{},"confidence":0.31}

event: token
data: {"delta":"图片未能识别，请用文字描述现象。"}
```

---

## 7. 前后端联调约定

| 项 | 约定 |
| --- | --- |
| 流式接收 | 前端 `utils/sse.ts` 用 `fetch + response.body.getReader()`，**禁用 `EventSource`** |
| 跨域 | 开发期由 Vite 代理 `/api` → `http://localhost:8000`；生产由 nginx 反代 |
| 超时 | 前端读流超时 60s；后端单次问答预算见 `setting.py` |
| 缓冲 | nginx 必须对 SSE 关闭 proxy_buffering，否则首 Token 会退化（NFR-01 ≤ 2s） |
| 图片 | 先上传拿 `image_id` 再提问，**两段式** |
| 错误 | 流**已经开始**后出错 → 发 `error` 事件；流未开始 → 返回 HTTP 错误体 |

---

## 8. 设计缺口与待确认项

以下是设计本文档时发现的**契约不一致或文档遗漏**，需在实现前拍板：

| # | 缺口 | 影响 | 建议 |
| --- | --- | --- | --- |
| ① | FR-09 培训模式需要 `mode=training`，但 `state.py` 的 `AgentState` **没有该字段** | 请求参数无法进入图 | 在 `state.py` 追加班次字段（如 `answer_mode`），并同步 `prompts/` 模板选择逻辑。**属契约级变更，需双方同步** |
| ② | 「检索即鉴权」需把角色传到检索层，但 §5.4 接口表未定义传递方式 | 权限过滤无处落地 | 本文档 §1.3 采用请求头 `X-User-Role` 透传 |
| ③ | 文档未定义**分页**规范 | 会话列表/历史/切片查询无统一约定 | 本文档统一用 `limit/offset` |
| ④ | §5.4 遗漏**索引重建**接口，与 §6.2 的 `admin.py` 职责矛盾 | FR-08「一键重建 ≤5min」无入口 | 本文档补充 §4.8 + §4.9 |
| ⑤ | FR-10 备件查询的输出「**询价单草稿**」落点未定：随 chat 返回还是独立接口？ | 前端无对应组件（§5.5 组件表里没有） | 暂定走 chat 工具调用返回；如需独立页再开 §2 P2 档接口 |
| ⑥ | 图片大小/格式/数量阈值文档未给数值 | 无法写校验 | 本文档暂定 5MB / jpg,png,webp / ≤3 张，写入 `setting.py` |
| ⑦ | 会话删除、重命名缺失 | 历史页无法管理 | 见 §2 P2 档 |
| ⑧ | `X-Accel-Buffering` 等 SSE 头易被忽略 | 首 Token 超 2s，NFR-01 不达标 | 部署时在 `deploy/nginx.conf` 显式配置 |

---

## 9. 实现顺序建议（对接两人分工）

| 步骤 | 接口 | 依赖 | 说明 |
| --- | --- | --- | --- |
| 1 | `/api/health` | 无 | 可直接用 `graph_summary()`，最快见效 |
| 2 | `/api/chat/stream` | `agent_factory.astream()` ✅ | **现在就能开工**，骨架期返回 `NOT_COVERED`；节点接入后自动变真答案 |
| 3 | `/api/upload/image` | `utils/image.py` | 纯文件处理，不阻塞 |
| 4 | 会话 / 反馈类 | `db.py` + MySQL | 建表后一次性做完 |
| 5 | `/api/admin/*` | `rag/ingest.py` `rag/store.py` | 依赖 RAG 链路 |

> **关键**：第 1、2、3 步**不依赖 RAG 节点**，可由平台侧立即并行开工；
> RAG 侧继续调节点，双方通过 `state.py` 契约交互，互不阻塞。

---

**变更记录**

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.1 | — | 首版：8 个文档既有接口 + 2 个补充接口 + SSE 契约 + 8 项设计缺口 |
