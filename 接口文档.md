# 路由层接口文档

> **项目**：半导体设备维护知识库智能问答系统
> **版本**：v0.1（接口先行 —— 契约变更必须先改本文档与测试，再改实现，见开发文档 §6.4）
> **设计依据**：开发文档 §5.4 接口设计、§5.6 数据库设计、§4.3 节点设计
> **范围**：`backend/routers/` 全部对外 HTTP 接口 + SSE 事件契约

---

## 1. 分层与设计约定

### 1.1 职责边界

```
routers/          ← 本文档描述的范围：只做 HTTP 翻译（async def + Depends）
  ↓
services/         ← 会话管理、调图、包 SSE 响应；不碰 HTTP 细节
  ↓
agents/ tools/ rag/   ← 业务实现
  ↓
setting.py / config/  ← 配置与提示词
```

依开发文档 §6.1，**依赖方向单向**，禁止反向 import。因此：

- `routers/` 内**不写业务逻辑**、不直接调用 `rag/` 或向量库；
- 所有参数校验在 `schemas.py` 完成，路由函数只做「取参 → 调 service → 返回」。

### 1.2 统一约定

| 项 | 约定 |
| --- | --- |
| 基础路径 | `/api`（前端 dev 通过 Vite 代理转发，见 `frontend/vite.config.ts`） |
| 版本策略 | 本期不引入 `/v1`；契约破坏性变更时再引入前缀 |
| 编码 | 一律 UTF-8；JSON 用 `application/json` |
| 成功响应 | **直接返回业务数据对象**，不套 `{code,data}` 信封 |
| 失败响应 | HTTP 状态码表达成败 + 统一错误体（见 §5） |
| 链路追踪 | 每个响应（含 SSE `meta` 事件）带 `trace_id`，对应 NFR-06 |
| 时间格式 | ISO 8601 带时区，如 `2026-09-15T12:30:00+08:00` |
| 命名 | 路径全小写 + 连字符；查询参数蛇形（`session_id`）|

> **为什么不用 `{code, data, message}` 信封**：SSE 无法使用信封，若 JSON 用、SSE 不用，
> 前端要维护两套解包逻辑。统一用 HTTP 状态码后，两套通道的错误语义一致；
> 且 nginx / 监控可直接按状态码统计。若后续前端希望统一拦截，可在 `api/http.ts` 内封装。

### 1.3 鉴权（本期最简实现）

开发文档 §1.3 明确「生产级权限体系」不在本期范围，但 §1.4 要求**检索即鉴权**——
权限过滤必须发生在检索层，而非「先检索再删答案」。因此接口层需把身份**透传**到检索层：

| 请求头 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `X-User-Id` | string | 否 | 缺省 `anonymous` |
| `X-User-Role` | enum | 否 | `engineer` / `admin`，缺省 `engineer` |

本期不校验签名，仅做透传与日志脱敏（NFR-03）。**检索层据此过滤**，接口层不裁剪答案。

---

## 2. 接口分档总览

按**优先级分档** + **模块分组**。优先级取自开发文档 §3.1 的 FR 优先级。

### P0 档（主链路，D5 / D7 必须交付）

| 模块 | 方法 | 路径 | 用途 | 对应 FR |
| --- | --- | --- | --- | --- |
| 健康 | GET | `/api/health` | 健康检查：向量库 / 模型 / VLM / MCP / 图 | NFR-06 |
| 问答 | POST | `/api/chat/stream` | **核心**：提问，SSE 流式返回答案 | FR-01/03/04/05 |
| 上传 | POST | `/api/upload/image` | 上传图片，返回 `image_id` | FR-02 |
| 管理 | POST | `/api/admin/documents` | 上传文档并触发入库 | FR-08 |
| 管理 | POST | `/api/admin/index/rebuild` | **一键重建索引**（⚠️ 文档 §5.4 遗漏，本文档补充） | FR-08 |

### P1 档（体验闭环）

| 模块 | 方法 | 路径 | 用途 | 对应 FR |
| --- | --- | --- | --- | --- |
| 会话 | GET | `/api/chat/sessions` | 会话列表 | FR-06 |
| 会话 | GET | `/api/chat/sessions/{id}/messages` | 历史消息（含引用） | FR-06 |
| 反馈 | POST | `/api/feedback` | 四态反馈落库 | FR-07 |
| 管理 | GET | `/api/admin/chunks` | 切片查询（调试用） | FR-08 |
| 管理 | GET | `/api/admin/jobs/{job_id}` | 异步任务进度查询（⚠️ 补充） | FR-08 |
| 管理 | GET | `/api/admin/questions/top` | 高频问题榜（⚠️ 补充，FR-09 本期交付） | FR-09 |

### P2 档（可选扩展，本文档建议）

| 模块 | 方法 | 路径 | 用途 |
| --- | --- | --- | --- |
| 会话 | DELETE | `/api/chat/sessions/{id}` | 删除会话 |
| 会话 | PATCH | `/api/chat/sessions/{id}` | 重命名会话 |
| 备件 | POST | `/api/parts/query` | FR-10 备件查询与询价单草稿 |

### 当前实现状态（2026-09-17 更新）

**11 个接口全部实现**（单测 84 passed；后端端到端 58 项断言全过）。

| 接口 | 状态 | 说明 |
| --- | --- | --- |
| `GET /api/health` | ✅ | 7 个组件真实探测：图 / 文本模型 / 图片模型 / 数据库 / 向量库 / MCP / 启动预热 |
| `POST /api/upload/image` | ✅ | 分片限量读取、PIL 校验、等比压缩（>1600px）、静态可访问；413/415/400 齐全 |
| `POST /api/chat/stream` | ✅ | 事件顺序 `meta → image? → token* → citations → done`；`done` 带 `qa_id` |
| `GET /api/chat/sessions` | ✅ | 分页 / 搜索 / message_count / last_question |
| `GET /api/chat/sessions/{id}/messages` | ✅ | 含引用与置信度；未知会话 404 |
| `POST /api/feedback` | ✅ | 四态；`correct` 校验；驳回写 `badcases.md` |
| `POST /api/admin/documents` | ✅ | 异步入库 + job 进度；同版本 409；docx 等 415（提示 MarkItDown） |
| `POST /api/admin/index/rebuild` | ✅ | 异步重建；实测 5 篇 / 32 切片 |
| `GET /api/admin/jobs/{job_id}` | ✅ | 进度 0~1 + message + result |
| `GET /api/admin/chunks` | ✅ | 支持 doc_id / 关键词 / 页码过滤 |
| `GET /api/admin/questions/top` | ✅ | 归一化问题聚合（同问题不同标点会归并） |

**仍需外部条件才能验证的**：G3 要点命中率（需 `DEEPSEEK_API_KEY`）、
G4 图片提取（需 `DASHSCOPE_API_KEY` + 图片 fixture）——都是配置动作，不是开发任务。

---

## 3. 通用数据结构

### 3.1 Citation（引用项）

与 `backend/agents/state.py` 的 `Citation` 模型、SSE `citations` 事件**三处字段完全一致**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int | 引用编号 `[n]`，**由代码分配，禁止模型编造** |
| `source_type` | enum | `kb_doc` / `ticket` / `image` |
| `doc` | string\|null | 文档名 |
| `version` | string\|null | 文档版本（P5 版本混乱痛点的关键字段） |
| `section` | string\|null | 章节 |
| `page` | int\|null | 页码 |
| `chunk_id` | string\|null | 切片 ID，用于「查看原文」 |
| `image_url` | string\|null | 图片类依据的原图地址 |
| `snippet` | string\|null | 原文片段 |

### 3.2 置信度三级

依 §3.3 FR-05：`confidence = 0.40×精排Top1归一化分 + 0.35×引用覆盖率 + 0.25×来源权威度`

| label | 区间 | 呈现 |
| --- | --- | --- |
| `high` | ≥ 0.80 | 直接给结论 |
| `medium` | 0.60–0.80 | 给结论 + 标注「需人工确认」 |
| `low` | < 0.60 | 只给相关材料清单 + 未覆盖说明，**不给操作步骤** |

### 3.3 status 取值

| 值 | 含义 |
| --- | --- |
| `OK` | 正常返回 |
| `NOT_COVERED` | 知识库未覆盖，拒答（**图内初始值即为此，fail-closed**） |
| `ERROR` | 链路异常 |

---

## 4. 接口详细设计

### 4.1 GET `/api/health`

**用途**：健康检查。供运维与前端启动自检，也是 R7「MCP 加载失败」降级状态的暴露点。

**请求参数**（query）

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `verbose` | bool | 否 | `false` | 是否返回各组件耗时明细 |

**响应 200**

```json
{
  "status": "degraded",
  "version": "0.1.0",
  "components": {
    "vector_store": { "ok": true,  "detail": "chroma: 1240 chunks" },
    "llm":          { "ok": true,  "detail": "deepseek-chat" },
    "vlm":          { "ok": false, "detail": "未配置 API Key" },
    "mcp":          { "ok": true,  "tools": ["ticket_search"], "degraded": false },
    "graph":        { "name": "smka_qa_graph", "ready": true, "nodes": [],
                      "checkpointer": "sqlite-async", "persistent": true }
  },
  "trace_id": "9740d1e9db784095942dd80f87be6cc7"
}
```

- `status`：`ok` 全部正常 / `degraded` 部分降级但主链路可用 / `down` 主链路不可用
- `graph` 字段**直接来自已实现的** `agent_factory.graph_summary()`，无需额外开发

---

### 4.2 POST `/api/upload/image`

**用途**：上传图片，返回 `image_id`。**此接口只做预处理，不做识别**（识别在问答链路的 `ingest_image` 节点）。

**请求**：`multipart/form-data`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `file` | File | ✅ | 图片文件 |
| `session_id` | string | 否 | 关联会话，便于历史回看 |

**约束**（数值需写入 `setting.py`，NFR-04 配置化）

| 项 | 值 |
| --- | --- |
| 允许格式 | `jpg` / `jpeg` / `png` / `webp` |
| 单文件上限 | 5 MB |
| 最小边长 | 200 px（低于此值识别质量不可用） |
| 处理 | 压缩 + 格式统一 + 体积校验（`utils/image.py`） |
| 存储 | `backend/data/uploads/`（`UPLOAD_DIR`） |

**响应 200**

```json
{
  "image_id": "img_7f3a9c21",
  "filename": "alarm_E2041.png",
  "mime": "image/png",
  "size_bytes": 184320,
  "width": 1280,
  "height": 720,
  "url": "/static/uploads/img_7f3a9c21.png",
  "trace_id": "..."
}
```

**错误**：`413 IMAGE_TOO_LARGE` / `415 UNSUPPORTED_IMAGE_TYPE` / `400 EMPTY_FILE` / `400 IMAGE_TOO_SMALL`

> **使用方式**：先上传拿 `image_id` → 再在 `/api/chat/stream` 的 `image_ids` 中引用。
> SSE 端点**不处理文件上传**（§5.4）。

---

### 4.3 POST `/api/chat/stream`　★核心接口

**用途**：提问并流式返回答案。承载 FR-01（多轮问答）、FR-02（多模态）、FR-03（跨源检索）、
FR-04（答案溯源）、FR-05（置信度与拒答）、FR-09（培训分层讲解）。

**请求**：`application/json`

| 字段 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `question` | string | ✅ | — | 用户问题，长度 1–2000 |
| `session_id` | string\|null | 否 | `null` | 缺省则**新建会话**并在 `meta` 事件中回传 |
| `image_ids` | string[] | 否 | `[]` | 图片 ID，最多 3 个（先经上传接口获得） |
| `mode` | enum | 否 | `qa` | `qa` 问答 / `training` 培训分层讲解（FR-09） |
| `device_model` | string\|null | 否 | `null` | 设备型号，用于**元数据过滤**（FR-03） |
| `category` | string\|null | 否 | `null` | 分类过滤：设备维护 / 工艺 / 标准 |

**请求示例**

```json
{
  "question": "刻蚀机腔体真空度异常波动，应该按什么顺序排查？",
  "session_id": "9f2c1a7b",
  "image_ids": ["img_7f3a9c21"],
  "mode": "qa",
  "device_model": "Etcher-A"
}
```

**响应 200**：`Content-Type: text/event-stream`

事件契约见 §6。**注意**：前端必须用 `fetch + response.body.getReader()` 解析，
**不使用 `EventSource`**（只支持 GET、无法带自定义请求头，见 §5.5）。

**⚠️ 契约依赖**：`mode` 字段目前**在 `state.py` 中不存在**，见 §8 缺口 ①。

---

### 4.4 GET `/api/chat/sessions`

**用途**：会话列表（FR-06）。数据源为 `session_map` 表。

**请求参数**

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `limit` | int | 否 | 20 | 1–100 |
| `offset` | int | 否 | 0 | 分页偏移 |
| `keyword` | string | 否 | — | 按标题模糊搜索 |

**响应 200**

```json
{
  "items": [
    {
      "session_id": "9f2c1a7b",
      "thread_id": "thread-9f2c1a7b",
      "title": "真空度异常排查",
      "message_count": 6,
      "last_question": "为什么先查 O-ring？",
      "updated_at": "2026-09-15T12:30:00+08:00"
    }
  ],
  "total": 1, "limit": 20, "offset": 0, "trace_id": "..."
}
```

---

### 4.5 GET `/api/chat/sessions/{session_id}/messages`

**用途**：历史消息回看，含引用（FR-04/FR-06）。数据源为 `qa_record` 表。

**路径参数**：`session_id`（必填）

**请求参数**

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `limit` | int | 否 | 50 | 1–200 |
| `offset` | int | 否 | 0 | 分页偏移 |
| `with_citations` | bool | 否 | `true` | 是否返回引用清单 |

**响应 200**

```json
{
  "session_id": "9f2c1a7b",
  "thread_id": "thread-9f2c1a7b",
  "items": [
    {
      "qa_id": 101,
      "role": "user",
      "content": "刻蚀机腔体真空度异常波动，应该按什么顺序排查？",
      "image_ids": ["img_7f3a9c21"],
      "created_at": "2026-09-15T12:30:00+08:00"
    },
    {
      "qa_id": 101,
      "role": "assistant",
      "content": "1. 检查腔体密封 O-ring [1]…",
      "citations": [
        { "id": 1, "source_type": "kb_doc", "doc": "刻蚀设备维护手册",
          "version": "V3.2", "section": "3.4", "page": 118,
          "chunk_id": "c_00871", "image_url": null, "snippet": "…" }
      ],
      "confidence": 0.86,
      "confidence_label": "high",
      "status": "OK",
      "latency_ms": 3120,
      "created_at": "2026-09-15T12:30:03+08:00"
    }
  ],
  "total": 2, "limit": 50, "offset": 0, "trace_id": "..."
}
```

**错误**：`404 SESSION_NOT_FOUND`

---

### 4.6 POST `/api/feedback`

**用途**：四态反馈落库（FR-07），驳回案例计入 `badcases.md`。数据源为 `qa_feedback` 表。

**请求**：`application/json`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `qa_id` | int | ✅ | 被反馈的问答记录 ID |
| `type` | enum | ✅ | `adopt` 采纳 / `partial` 部分采纳 / `reject` 驳回 / `correct` 修正 |
| `comment` | string\|null | 否 | 备注，≤500 字 |
| `corrected_answer` | string\|null | 条件必填 | `type=correct` 时**必填** |

**响应 200**

```json
{ "feedback_id": 55, "recorded": true, "created_at": "2026-09-15T12:31:00+08:00", "trace_id": "..." }
```

**错误**：`404 QA_NOT_FOUND` / `400 CORRECTED_ANSWER_REQUIRED`

---

### 4.7 POST `/api/admin/documents`

**用途**：上传文档并触发入库流水线（FR-08）：解析 → 抽图 → 清洗 → 切片 → 向量化 → 入库。
**长耗时任务，异步执行**，立即返回 `job_id`。

**请求**：`multipart/form-data`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `file` | File | ✅ | 支持 PDF / MD / TXT / DOCX / PPTX / XLSX |
| `doc_title` | string | ✅ | 文档标题 |
| `version` | string | ✅ | **必填**，元数据完整率 100% 要求 `page` + `version` 齐备 |
| `category` | string | ✅ | 设备维护 / 工艺 / 标准 |
| `uploader` | string | 否 | 上传人 |

**响应 202**

```json
{ "doc_id": 12, "job_id": "job_a1b2c3", "status": "processing", "trace_id": "..." }
```

**错误**：`415 UNSUPPORTED_DOC_TYPE` / `409 DOC_VERSION_EXISTS`

---

### 4.8 POST `/api/admin/index/rebuild`　⚠️ 文档补充

**用途**：一键重建索引（FR-08，验收标准 ≤ 5 分钟）。
**文档 §5.4 的接口表遗漏了此接口**，但 §6.2 明确 `admin.py` 负责「文档入库 / 索引重建 / 切片查询」。

**请求**：`application/json`

| 字段 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `scope` | enum | 否 | `all` | `all` 全量 / `incremental` 增量 |
| `force` | bool | 否 | `false` | 是否强制重建（忽略切片版本） |

**响应 202**：`{ "job_id": "job_x9y8z7", "status": "running", "trace_id": "..." }`

---

### 4.9 GET `/api/admin/jobs/{job_id}`　⚠️ 文档补充

**用途**：查询异步任务（入库 / 重建索引）进度。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `job_id` | string | 路径参数，必填 |
| `status` | enum | `pending` / `running` / `succeeded` / `failed` |
| `progress` | float | 0–1 |
| `message` | string | 当前阶段描述，如「切片 128/540」 |
| `result` | object\|null | 完成后结果，如 `{"chunks": 540, "images": 32}` |

---

### 4.10 GET `/api/admin/chunks`

**用途**：切片查询，**调分块参数时用**（§6.2）。

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `doc_id` | int | 否 | — | 按文档过滤 |
| `q` | string | 否 | — | 按内容模糊搜索 |
| `page` | int | 否 | — | 按页码过滤 |
| `limit` / `offset` | int | 否 | 20 / 0 | 分页 |

**响应**：`{ "items": [{ "chunk_id", "doc", "version", "section", "page", "token_len", "text", "image_url" }], "total", ... }`

---

### 4.11 GET `/api/admin/questions/top`　⚠️ 文档补充

**用途**：高频问题榜（FR-09 本期交付的「高频问题记录」）。
用于反推培训内容与知识补充方向 —— 被问得最多的问题，往往就是最该补文档的地方。

**请求参数**

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `limit` | int | 否 | 20 | 1–200 |
| `min_count` | int | 否 | 1 | 被问次数下限，用于过滤长尾 |

**响应 200**

```json
{
  "items": [
    {
      "question": "腔体温度异常怎么处理？",
      "question_key": "腔体温度异常怎么处理",
      "ask_count": 4,
      "first_asked_at": "2026-09-17T23:29:00+08:00",
      "last_asked_at": "2026-09-17T23:35:00+08:00"
    }
  ],
  "total": 1,
  "limit": 20,
  "trace_id": "..."
}
```

> **归并规则**：`question_key` = 问题去掉空白与中英文标点、转小写、截断 120 字
> （`utils/text.normalize_question`）。因此「腔体温度异常怎么处理？」与
> 「腔体温度异常怎么处理?」会归并成同一条计数。
>
> 数据落在 `db.QuestionStat` 表（`question_key` 为主键），每次问答落库时自增。

---

## 5. 统一错误体

```json
{
  "code": "IMAGE_TOO_LARGE",
  "message": "图片超过 5MB 限制",
  "detail": { "max_mb": 5, "actual_mb": 7.2 },
  "trace_id": "9740d1e9db784095942dd80f87be6cc7"
}
```

| HTTP | code | 触发场景 |
| --- | --- | --- |
| 400 | `INVALID_ARGUMENT` | 参数校验失败（Pydantic） |
| 400 | `EMPTY_FILE` / `IMAGE_TOO_SMALL` | 文件为空 / 图片过小 |
| 400 | `CORRECTED_ANSWER_REQUIRED` | 反馈类型为修正但未给修正内容 |
| 404 | `SESSION_NOT_FOUND` / `QA_NOT_FOUND` / `JOB_NOT_FOUND` | 资源不存在 |
| 409 | `DOC_VERSION_EXISTS` | 同文档同版本已存在 |
| 413 | `IMAGE_TOO_LARGE` | 图片超限 |
| 415 | `UNSUPPORTED_IMAGE_TYPE` / `UNSUPPORTED_DOC_TYPE` | 格式不支持 |
| 422 | `QUESTION_TOO_LONG` | 问题超过 2000 字 |
| 429 | `RATE_LIMITED` | 超过 20 并发（NFR-02） |
| 500 | `INTERNAL_ERROR` | 未预期异常 |
| 503 | `VECTOR_STORE_UNAVAILABLE` / `LLM_UNAVAILABLE` | 依赖不可用 |

> **降级优先于报错**：依开发文档 R7 与已实现的 `memory.py` 降级策略，
> MCP / 检查点等**非关键依赖**失败时不得中断主链路，应降级并在 `errors` 中记录，
> 仅在主链路（向量库 / LLM）不可用时才返回 503。

---

## 6. SSE 事件契约（前后端唯一契约）

响应头：`Content-Type: text/event-stream`、`Cache-Control: no-cache`、`X-Accel-Buffering: no`（关闭 nginx 缓冲）
事件之间以**空行**分隔。

| 顺序 | event | data | 对应 `AgentState` 字段 |
| --- | --- | --- | --- |
| 1 | `meta` | `{session_id, trace_id, ts}` | `session_id` / `trace_id` |
| 2 | `image` | `{image_id, image_type, extracted{}, confidence}` | `image_result` |
| 3 | `token` | `{delta}` | `answer` 的增量 |
| 4 | `citations` | `{items: Citation[]}` | `citations` |
| 5 | `done` | `{status, confidence, label, uncertain[]}` | `status` / `confidence` / `confidence_label` / `uncertain` |
| — | `error` | `{code, message}` | `errors` |

> 上表右列说明：**本契约与已实现的 `state.py` 逐字段对齐**，
> 因此 `agent_factory.astream()` 的产出可直接映射为 SSE 事件，无需中间转换层。

**完整示例**

```
event: meta
data: {"session_id":"9f2c1a7b","trace_id":"9740d1e9","ts":"2026-09-15T12:30:00+08:00"}

event: image
data: {"image_id":"img_7f3a9c21","image_type":"alarm_screen","extracted":{"alarm_code":"E-2041"},"confidence":0.92}

event: token
data: {"delta":"1. 检查腔体密封 "}

event: token
data: {"delta":"O-ring [1]…"}

event: citations
data: {"items":[{"id":1,"source_type":"kb_doc","doc":"刻蚀设备维护手册","version":"V3.2","section":"3.4","page":118,"chunk_id":"c_00871","image_url":null,"snippet":"…"}]}

event: done
data: {"status":"OK","confidence":0.86,"label":"high","uncertain":[]}
```

**拒答示例**（`status=NOT_COVERED`，不给具体操作步骤）

```
event: meta
data: {"session_id":"9f2c1a7b","trace_id":"…","ts":"…"}

event: token
data: {"delta":"知识库未覆盖该问题。建议：…"}

event: done
data: {"status":"NOT_COVERED","confidence":0.12,"label":"low","uncertain":["未检索到该型号腔体清洗周期"]}
```

**图片识别失败**（§4.1 FR-02 硬性要求：不允许猜测）

```
event: image
data: {"image_id":"img_xxx","image_type":"unknown","extracted":{},"confidence":0.31}

event: token
data: {"delta":"图片未能识别，请用文字描述现象。"}
```

---

## 7. 前后端联调约定

| 项 | 约定 |
| --- | --- |
| 流式接收 | 前端 `utils/sse.ts` 用 `fetch + response.body.getReader()`，**禁用 `EventSource`** |
| 跨域 | 开发期由 Vite 代理 `/api` → `http://localhost:8000`；生产由 nginx 反代 |
| 超时 | 前端读流超时 60s；后端单次问答预算见 `setting.py` |
| 缓冲 | nginx 必须对 SSE 关闭 proxy_buffering，否则首 Token 会退化（NFR-01 ≤ 2s） |
| 图片 | 先上传拿 `image_id` 再提问，**两段式** |
| 错误 | 流**已经开始**后出错 → 发 `error` 事件；流未开始 → 返回 HTTP 错误体 |

---

## 8. 设计缺口与待确认项

以下是设计本文档时发现的**契约不一致或文档遗漏**，及其**当前处置状态**（2026-09-17）：

| # | 缺口 | 处置状态 |
| --- | --- | --- |
| ① | FR-09 培训模式需要 `mode=training`，但 `AgentState` 没有对应字段 | ✅ **已解决**：`state.py` 追加 `answer_mode`（及 `device_model` / `category` / `user_role`），`agent_factory` 全链路透传，`prompts/generate.md` 分 qa / training 两套模板 |
| ② | 「检索即鉴权」缺传递方式 | ⚠️ **部分**：请求头 `X-User-Role` 已透传到 `retrieve` 节点与 `build_filters(user_role=...)`，但**过滤规则未定义**（需一份权限分级表），因此当前只记录不生效 |
| ③ | 未定义分页规范 | ✅ 统一用 `limit/offset`，各接口已实现 |
| ④ | §5.4 遗漏索引重建接口 | ✅ 已补 §4.8 + §4.9 并实现 |
| ⑤ | FR-10「询价单草稿」落点未定 | ✅ **已解决（走 chat，未开独立接口）**：`parts_query` 已接进 `augment`，备件类提问会查台账并把结果作为证据注入答案（含库存 / 库位 / 到货 / 替代件依据）；询价单草稿随回答给出 |
| ⑥ | 图片大小/格式/数量阈值未给数值 | ✅ 已写入 `setting.py`：5MB / jpg,png,webp / ≤3 张 / 最短边 ≥200px / 最长边 >1600px 自动压缩 |
| ⑦ | 会话删除、重命名缺失 | ⏳ **未实现**（P2 档，前端也无入口） |
| ⑧ | SSE 头易被忽略 | ✅ `deploy/nginx.conf` 已设 `proxy_buffering off` + `proxy_cache off`；后端发 `X-Accel-Buffering: no` |
| ⑨ | **（新增）** `done` 事件不带 `qa_id`，问答页反馈按钮只能置灰 | ✅ **已解决（前后端闭环）**：后端 `schemas.py:158` 的 `DoneEvent.qa_id` 已声明；前端 `api/chat.ts` 的 `DoneEvent` 补 `qa_id` 字段、`stores/chat.ts` 的 `onDone` 写入 `message.qaId`。**问答页现在不刷新即可提交反馈**（`FeedbackBar.vue` 的置灰条件 `qaId === null` 自动解锁）；历史页走接口返回的 `qa_id`，行为不变 |
| ⑩ | **（新增）** `train` 类图片经 `/api/upload/image` 上传后，SSE 里图片识别结果只进检索式、不进生成上下文 | ⏳ **未解决**：`nodes/generation.py` 不读 `image_result`，带图提问会因「这个/这张」无法消解而拒答（详见《功能完成度清单》缺陷 #11）。**属实现缺陷，非契约缺陷** —— SSE 的 `image` 事件契约本身没问题 |

---

## 9. 实现顺序建议（对接两人分工）

> **本节的「建议顺序」已全部执行完毕**（2026-09-17），保留供追溯。

| 步骤 | 接口 | 当时依赖 | 结果 |
| --- | --- | --- | --- |
| 1 | `/api/health` | 无 | ✅ 已实现，7 组件真实探测 |
| 2 | `/api/chat/stream` | `agent_factory.astream()` | ✅ 已实现，9 节点图 + 条件边 |
| 3 | `/api/upload/image` | `utils/image.py` | ✅ 已实现（含压缩与校验） |
| 4 | 会话 / 反馈类 | `db.py` | ✅ 已实现（2026-09-18 起运行于 MySQL，库 `smka`） |
| 5 | `/api/admin/*` | `rag/ingest.py` `rag/store.py` | ✅ 已实现（异步入库 + job） |

### 仍待推进的（不属于接口层）

| 项 | 说明 |
| --- | --- |
| ~~前端消费 `done.qa_id`~~ | ✅ **已完成**（2026-09-18）：`stores/chat.ts` 的 `onDone` 写入 `message.qaId`，反馈按钮即时解锁 |
| ~~前端备件查询入口~~ | ✅ **已完成**（2026-09-18）：`ChatInput.vue` 增加「快捷提问」组（备件库存 / 替代件 / 参数查询），点击填入输入框并聚焦（**不自动发送** —— 模板含占位符，须由用户替换成真实件号） |
| 检索层权限过滤规则 | 角色已透传，缺一份权限分级表（FR-11）才能落地 |
| `done.qa_id` 之外的 SSE 字段 | 本轮未新增；`done` 现有字段 `status / confidence / label / uncertain / qa_id` 与前端类型已逐一对齐 |

---

## 10. 三块新业务接口（2026-09-20 追加）

> 范围说明：以下三块功能在原开发文档 §1.3 中列为「不做 / 后续批次」，
> 现按项目方要求**补齐后端**（前端由第三人负责）。
> 三块功能同样受**零幻觉**约束：计划项与考题都必须带 `evidence`（引用手册章节/切片），
> 抽不到依据的条目一律不产出，绝不编造周期、依据或题目。
>
> 错误约定与既有接口一致：请求体**字段级**校验失败统一返回 `400 INVALID_ARGUMENT`
> （不复用 FastAPI 默认的 422），业务冲突用 404 / 409，数据库不可用用 503 `DB_UNAVAILABLE`。

### 10.1 维护计划生成

| 模块 | 方法 | 路径 | 用途 | 请求 / 响应 |
| --- | --- | --- | --- | --- |
| 计划 | POST | `/api/plans/generate` | 按设备 + 运行数据生成维护计划 | `PlanGenerateRequest` → `PlanGenerateResponse` |
| 计划 | GET | `/api/plans` | 计划列表（按紧急度排序） | query `device_model/status/limit/offset` → `PlanListResponse` |
| 计划 | POST | `/api/plans/{plan_id}/complete` | 标记完成并滚到下一周期 | `PlanUpdateRequest` → `PlanItem` |
| 计划 | POST | `/api/plans/{plan_id}/skip` | 跳过（`note` 建议填原因，不填记录为「未填写原因」） | `PlanUpdateRequest` → `PlanItem` |

要点：

- **周期值只能来自知识库原文**（从检索片段里正则抽取「每 4000 运行小时 / 每 25 片 / 每 3 个月 / 每 90 天」这类表述），
  每条计划项都带 `evidence`（文档名 / 版本 / 章节 / 页码 / 切片 ID）与 `chunk_id`；
- 知识库查不到周期的项目进 `uncovered` 列表，**不生成计划项、不推测周期**；
- `remaining` 为距离到期还差的计量单位，`remaining <= 0` 即 `status="due"`（已到期/超期）；
  `status` 取值共四个：`planned` / `due` / `done` / `skipped`；
- `due_at` 口径：`hours` 按「24 h 连续运行」折算（`now + max(0,remaining)/24 天`，超期即当天）；
  `days` 为 `now + cycle_value 天`；`wafers`（生产片数）**一律留空**（手册不给产线速率，
  不猜速率），`remaining` 仍给出还差多少片；只有执行频次（每班次 / 每次开腔）的条款
  不进计划项，归入 `uncovered`；
- `uncovered` 元素为 `名称：原因` 的成品字符串（未收录 / 未给出可换算周期 / 只有执行频次），
  这类项目**不进 `items`**，由人工确认；
- `PlanItem` 另带 `created_at` / `updated_at` / `completed_at`（UTC ISO 字符串，
  `completed_at` 仅完成或跳过后有值）——契约只追加，不影响既有调用方；
- `persist=false` 只预览不落库；`persist=true` 为**追加写入**（不覆盖旧计划，历史留档）。

### 10.2 备件商城与采购

| 模块 | 方法 | 路径 | 用途 | 请求 / 响应 |
| --- | --- | --- | --- | --- |
| 商城 | GET | `/api/parts/catalog` | 商城目录（库存/价格/替代件） | query `device_model/keyword/limit/offset` → `PartCatalogResponse` |
| 商城 | GET | `/api/parts/{code}` | 单个备件详情 | → `PartItem` |
| 采购 | POST | `/api/parts/orders` | 创建采购申请单（草稿） | `OrderCreateRequest` → `PartOrderResponse` |
| 购物车 | POST | `/api/parts/orders/{order_id}/items` | 加入购物车（同编码累加数量） | `OrderItemRequest` → `PartOrderResponse` |
| 购物车 | PATCH | `/api/parts/orders/{order_id}/items/{code}` | 改数量（1~999） | `OrderItemUpdateRequest` → `PartOrderResponse` |
| 购物车 | DELETE | `/api/parts/orders/{order_id}/items/{code}` | 移出购物车（可清空） | → `PartOrderResponse` |
| 采购 | GET | `/api/parts/orders` | 订单列表 | query `status/applicant/limit/offset` → `OrderListResponse` |
| 采购 | GET | `/api/parts/orders/{order_id}` | 订单详情 | → `PartOrderResponse` |
| 采购 | POST | `/api/parts/orders/{order_id}/submit` | 提交（草稿→已提交） | `OrderActionRequest` → `PartOrderResponse` |
| 采购 | POST | `/api/parts/orders/{order_id}/approve` | **人工确认**（已提交→已批准） | `OrderActionRequest` → `PartOrderResponse` |
| 采购 | POST | `/api/parts/orders/{order_id}/reject` | 驳回（须填 `reason`） | `OrderActionRequest` → `PartOrderResponse` |
| 采购 | POST | `/api/parts/orders/{order_id}/receive` | 收货（已批准→已收货） | `OrderActionRequest` → `PartOrderResponse` |
| 结算 | POST | `/api/parts/orders/{order_id}/settle` | 登记结算（写台账，幂等） | `SettlementRequest` → `SettlementResponse` |
| 结算 | GET | `/api/parts/settlements` | 结算台账与汇总金额 | query `order_no/limit/offset` → `SettlementListResponse` |

要点与边界：

- **购物车就是 `draft` 状态的申请单**，不额外建 cart 表：
  「加入购物车」= `POST …/items`（同编码**累加数量**，上限 999，不产生重复行）、
  「改数量」= `PATCH …/items/{code}`、「移出」= `DELETE …/items/{code}`（允许清空）、
  「我的购物车」= `GET /parts/orders?status=draft`；
  * 明细的**单价与库存一律由后端回台账取**（不信任前端传的价格），合计 `total_amount` 由后端重算；
  * 只有 `draft` 可改 —— 提交后明细即冻结（审批审的就是这份清单），非草稿一律 `409 INVALID_STATE`；
  * 明细不存在 -> `404 ITEM_NOT_FOUND`；**空草稿单不能提交** -> `400 EMPTY_ORDER`；
  * 每次加/改/删都在订单 `note` 追加一行审计（可带 `operator`："购物车改量：SP-ETA-0101 × 6（操作人：张三）"）；
- **系统不对供应商发起任何真实下单**（原开发文档红线）：只生成内部采购申请单，
  `approve` 即人工确认点，之后才允许收货与结算；
- **替代件必须有兼容性依据**：`is_substitute=true` 但台账里查不到 `basis` → 直接拒单
  （`400 SUBSTITUTE_BASIS_REQUIRED`），与问答链路的备件约束一致；
- 状态机只允许 `draft→submitted→approved→received`，非法迁移 `409 INVALID_STATE`；
  终态为 `rejected` / `cancelled`；`approve` 即**人工确认点**，之后才允许收货与结算；
- **驳回与取消复用 `POST …/reject` 一个端点**（`OrderActionRequest` 不新增字段）：
  `reason` 以 `cancel` / `取消` 开头（后跟 `:` 或 `：`，或整串恰好等于标记）→ 视为取消
  （`draft|submitted → cancelled`）；其他 `reason` → 视为驳回（`submitted|approved → rejected`）。
  两种情况都必须带 `reason`，否则 `400 REASON_REQUIRED`；
- **结算幂等**：同一订单重复结算 `409 ALREADY_SETTLED`；仅 `approved` / `received` 可结算；
  结算只记账（写 `part_settlement`），不做财务过账；
- 价格与供应商由备件台账（`config/parts_inventory.yaml`）**原样透传**（`parts_query` 返回
  `price_cny` / `supplier`），台账没写即 `0.0` / `""`，**不估算价格**；
- 响应带 `allowed_actions`，前端据此渲染可点击的下一步动作
  （`draft→[submit,cancel]`、`submitted→[approve,reject,cancel]`、`approved→[receive]`、
  `received→[settle]`、终态→`[]`）。

### 10.3 考核认证

| 模块 | 方法 | 路径 | 用途 | 请求 / 响应 |
| --- | --- | --- | --- | --- |
| 考核 | POST | `/api/training/quizzes` | 按设备/主题出题 | `QuizGenerateRequest` → `QuizResponse` |
| 考核 | GET | `/api/training/quizzes/{quiz_id}` | 取题（含答案与依据） | → `QuizResponse` |
| 考核 | POST | `/api/training/quizzes/{quiz_id}/submit` | 交卷判分 | `QuizSubmitRequest` → `QuizSubmitResponse` |
| 认证 | GET | `/api/training/certifications` | 认证记录 | query `trainee/device_model/status/limit/offset` → `CertificationListResponse` |
| 认证 | GET | `/api/training/certifications/expiring` | 到期提醒（含已过期） | query `days=30` → `CertificationListResponse` |
| 认证 | POST | `/api/training/certifications` | 发证（须基于一次通过的考核） | `CertificationRequest` → `CertificationResponse` |

要点与边界：

- **每题必须带 `evidence`**：出题前先检索知识库，题目只能基于检索到的片段；
  LLM 出题时若 `evidence_index` 越界或缺失，该题**丢弃**；一道题都生成不出来时返回
  `422 QUIZ_GENERATION_FAILED`（宁可不命题，也不出无依据的题）；
- 未配置文本模型时走**确定性抽句出题**（`generator="extractive-fallback"`），功能不中断；
- 判分：单选=下标、多选=完全一致、判断=布尔、简答=要点覆盖（字符二元组重合 ≥0.5 记命中，
  命中率 ≥0.6 算对）；`score = 正确题数/总题数×100`，`pass_line=60`；
- **不自动发证**：`submit_attempt` 的 `certification_id` 恒为 `null`，
  发证必须显式调用并指定等级（L1/L2/L3）与有效期；系统**不代表原厂签发认证**，
  `issuer` 默认「内部授权」。

---

**变更记录**

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.5 | 2026-09-20 | §10.2 补购物车三端点（`POST/PATCH/DELETE …/orders/{order_id}/items`），明确「购物车 = 草稿单」口径、明细冻结与空单不可提交；接口总数 32 -> 35 |
| v0.4 | 2026-09-20 | 新增 §10 三块新业务接口（维护计划生成 / 备件商城与采购 / 考核认证），共 21 个端点；明确零幻觉与「不真实对外下单」「不自动发证」两条边界 |
| v0.3 | 2026-09-18 | 缺口⑨（`done.qa_id`）前后端闭环，标 ✅；新增缺口⑩（图片识别结果不进生成上下文，属实现缺陷非契约缺陷）；§9「仍待推进」中前端两项标记完成。**本轮未改动任何 SSE / 接口契约**，仅更新处置状态 |
| v0.2 | 2026-09-17 | 补 `GET /api/admin/questions/top`（§4.11）；「当前实现状态」更新为 11 个接口全部实现；§8 缺口补处置状态（含新增缺口⑨ `done.qa_id`）；§9 改为结果追溯 |
| v0.1 | — | 首版：8 个文档既有接口 + 2 个补充接口 + SSE 契约 + 8 项设计缺口 |
