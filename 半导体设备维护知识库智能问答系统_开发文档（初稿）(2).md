基于多智能体协同的在线教育平台自动批改系统的设计与实现# 半导体设备维护知识库智能问答系统
# 开发文档（初稿）

| 项目名称 | 半导体设备维护知识库智能问答系统 |
| --- | --- |
| 文档版本 | **V0.1 初稿**（供评审与修改） |
| 文档密级 | 内部 |
| 编制日期 | 2026-09-11 |
| 项目性质 | 模拟项目（验证方案与核心功能可行性） |
| 开发周期 | **8 个工作日** |
| 前端技术 | Vue3 + Vite + Element Plus + Pinia |
| 图片识别 | 阿里云 DashScope `qwen-vl-max` |
| 核心命题 | 把 RAG 做扎实：**查得准、引得实、不敢答的敢说不知道** |

> 本文档为初稿，仅覆盖 8 天开发周期内的核心功能与工程规范，不含后续版本的规划内容。

---

<!-- TOC -->

## 1. 项目概述

### 1.1 项目背景

半导体设备维护是典型的高价值、高专业度、强时效场景：单台设备成本数百万美元，产线 24 小时连续运行，一次非计划停机可能连锁影响多道工序在制品。而设备维护所需的知识高度分散：

- 设备手册、工艺规范沉淀在文档系统或 PDF 中；
- 历史处置经验散落在工单系统里，多数只记录结论、未记录判断依据；
- 图纸、参数表、报警画面等关键信息**以图片形态存在**，关键词检索完全搜不到；
- 资深工程师的判断经验只存在于个人大脑，新人上手周期长。

同时，通用大模型在该领域存在不可接受的幻觉风险——编造一条"看起来合理"的维修步骤，可能导致设备二次损坏甚至人身伤害。因此本项目的重点不是"接入一个大模型"，而是**构建一条可信的知识供应链**：检索有据、回答可查、质量可测。

### 1.2 建设目标

| 编号 | 目标 | 目标值 | 验收方式 |
| --- | --- | --- | --- |
| G1 | 检索质量 | 黄金集 Top-5 命中率 ≥ 80% | 评测脚本自动跑分 |
| G2 | 答案可溯源 | 引用覆盖率 **100%**（结论句均有 `[n]` 且 `[n]` 真实存在） | 自动校验 + 人工抽检 20 条 |
| G3 | 不编造 | 要点命中率 ≥ 80%；无依据问题正确拒答率 ≥ 90% | 黄金集（含负样本） |
| G4 | 多模态可用 | 图片关键信息提取准确率 ≥ 85% | 图片评测集 |
| G5 | 响应性能 | 首 Token ≤ 2s；文字问答 P95 ≤ 8s；图片类 P95 ≤ 15s | 评测脚本记录耗时 |

### 1.3 范围界定

| 做 | 不做 |
| --- | --- |
| 设备维护文档的知识问答（带来源引用） | 多智能体领域分工 |
| 图片 + 文本两种模态提问 | 音频、视频、图纸精细推理 |
| 跨源检索（文档 + 历史工单 + 备件数据） | 维护计划生成 |
| 答案溯源、置信度三级标注与无依据拒答 | 备件自动下单、采购结算、备件商城 |
| 会话历史、反馈记录 | 审批流、知识图谱、模型微调 |
| 培训与认证（本期：分层讲解 + 高频问题记录） | 正式认证考核与发证、培训效果评估 |
| 备件查询与订购引导（生成询价单草稿） | 生产级权限体系、多厂部署 |
| 最简知识入库与索引重建 | 自动增量同步 |

### 1.4 设计原则

1. **零幻觉优先**：任何结论必须挂在证据上，证据不足即输出"知识库未覆盖"。
2. **生成与校验分离**：生成答案的节点不为自己打分，引用校验与置信度由独立节点以**确定性代码**完成。
3. **检索即鉴权**：权限过滤发生在检索层，而非"先检索再删答案"。
4. **受限自主性**：流程固定的环节由代码控制，仅"路径不唯一"的环节交给模型，并限定轮数与工具白名单。
5. **契约先行、可评测可回滚**：接口与事件格式先定义后开发；提示词/参数变更必须跑评测。

### 1.5 术语与缩略语

| 术语 | 说明 |
| --- | --- |
| RAG | 检索增强生成 |
| LangGraph | 基于有向图状态机的编排框架，支持检查点与人工中断 |
| MCP | Model Context Protocol，标准化工具接入协议 |
| 智能体（Agent） | 具备独立职责与工具权限的自治推理单元 |
| VLM | 视觉语言模型（本项目使用 `qwen-vl-max`） |
| 引用（Citation） | 回答中每条结论对应的原始依据（文档页 / 工单号 / 原图） |
| 置信度 | 系统对回答可靠性的自评等级，决定呈现方式与是否转人工 |
| 黄金集 | 人工编写、含标准答案与必备引用的评测题库 |
| LOTO | 上锁挂牌（能源隔离安全规程） |
| MTTD | 平均故障定位时间 |

---

## 2. 业务需求与痛点分析

### 2.1 目标用户与典型场景

| 角色 | 核心诉求 | 主要使用功能 |
| --- | --- | --- |
| 设备工程师 | 快速定位故障、拿到可执行步骤 | FR-01、FR-02、FR-03 |
| 资深设备工程师 | 复核结论、修正知识 | FR-01、FR-04、FR-07 |
| 新人工程师 | 学习 SOP、案例、安全规范 | FR-01、FR-02、FR-06 |
| 知识管理员 | 文档入库、切片质量、版本 | FR-08 |

**典型场景**

- **场景 A（现场抢修）**：刻蚀机报真空度异常波动，工程师描述现象并**拍到报警画面截图**上传 → 系统检索手册排查流程与历史工单，同时从截图提取报警码 → 输出按可能性排序的故障假设与验证步骤，每条带引用。
- **场景 B（新人学习）**：新人问"为什么真空度异常时先查 O-ring 而不是先查泵？"→ 系统给出原理、判断依据、操作步骤与常见误区，并关联真实案例与手册章节。
- **场景 C（参数查询）**：工程师上传手册中参数表截图，问"这个参数的下限是多少、在哪规定的？"→ 系统转出表格文本、检索生效版本、给出数值与出处。

### 2.2 痛点清单

| 编号 | 痛点 | 现状表现 | 业务影响 | 对应功能 |
| --- | --- | --- | --- | --- |
| P1 | 知识碎片化、检索慢 | 手册、规范、工单分处不同系统，工程师跨多个系统拼信息 | 定位根因平均 2–4 小时；同一故障处置口径不一 | FR-01、FR-03 |
| P2 | 回答缺乏依据、易编造 | 通用大模型编造维修步骤；人工经验回答无引用、无法复核 | 设备二次损伤、安全事故、无法追溯 | FR-04、FR-05 |
| P3 | 图片形态的知识无法利用 | 图纸、参数表、报警画面、铭牌信息只在图里，关键词检索搜不到 | 关键参数与图形化知识无法复用，现场只能靠问人 | FR-02 |
| P4 | 经验传承断层 | 关键判断依赖少数资深工程师，离职即流失；第三方服务商工程师缺原厂知识渠道 | 新人独立上手周期长；服务质量参差 | FR-01、FR-06、FR-09 |
| P5 | 知识版本与时效混乱 | 手册多版本并存，无法确认引用的是哪一版 | 旧版本指导作业，存在合规风险 | FR-04、FR-08 |
| P6 | 反馈无法回流 | 工程师发现答案有误只能口头反馈，知识库不更新 | 同类问题反复出错 | FR-07 |
| P7 | 备件信息不透明、等件停机 | 诊断出需换件时，库存、替代件、到货时间散在原厂系统与线下沟通中 | 等件造成的额外停机；替代件用错损坏设备 | FR-10 |

### 2.3 痛点—功能—智能体对应关系

| 痛点 | 承接功能 | 负责智能体 |
| --- | --- | --- |
| P1 检索慢 | FR-01、FR-03 | A1 问答智能体（检索链路） |
| P2 缺乏依据 | FR-04、FR-05 | A1（校验节点） |
| P3 图片知识 | FR-02 | A1（图片识别节点） |
| P4 经验断层 | FR-01、FR-06、FR-09 | A1（含培训提示词） |
| P5 版本混乱 | FR-04、FR-08 | A1（元数据与引用） |
| P6 反馈不回流 | FR-07 | A1（反馈采集） |
| P7 备件不透明 | FR-10 | A1（备件查询工具） |

---

## 3. 业务功能

### 3.1 功能清单

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-01 | **知识问答（多轮）** | 自然语言提问，系统基于知识库作答，支持追问；SSE 流式输出 | P0 |
| FR-02 | **多模态提问（图片 + 文本）** | 上传/粘贴图片 + 文字问题，识别图内信息后参与检索 | P0 |
| FR-03 | **跨源检索** | 统一检索手册文档与历史工单，术语归一、混合检索、精排 | P0 |
| FR-04 | **答案溯源** | 每条结论带引用标记，展示文档名/版本/章节/页码/原图，可点击查看 | P0 |
| FR-05 | **置信度标注与拒答** | 高/中/低三级标注；无依据时明确输出"知识库未覆盖" | P0 |
| FR-06 | **会话与历史** | 会话列表、历史消息回看、多轮上下文保持 | P1 |
| FR-07 | **反馈记录** | 采纳/部分采纳/驳回/修正 + 备注，落库并输出 badcase 清单 | P1 |
| FR-08 | **知识入库与索引重建** | 文档上传、解析、抽图、切片、向量化；一键重建索引 | P0 |
| FR-09 | **培训与认证** | 分层讲解答疑（原理 → 判断依据 → 操作 → 误区）+ 高频问题记录；考核题生成与认证记录为后续批次 | P1 |
| FR-10 | **备件查询与订购引导** | 按部件查询库存/替代件/到货时间；生成询价单草稿（不自动下单） | P1 |

### 3.2 核心功能详述

#### FR-01 知识问答（多轮）

- **输入**：自然语言问题（可含设备型号、报警码、时间窗）+ 可选图片。
- **处理流程**：（图片识别）→ 查询改写 → 混合检索 → 精排 → 上下文构建 → 生成 → 校验 → 回答。
- **输出（固定三段式）**：
  - **结论**：要点或按可能性排序的排查步骤，每句带 `[n]` 引用；
  - **依据**：`[1] 刻蚀设备维护手册 v1.2 · 3.4 真空系统排查 · P.118`；
  - **说明**：置信度等级、未覆盖项、安全前置提示（涉及拆机/带电/真空/特气/联锁时必须给出）。
- **验收标准**：5 个典型场景下推荐步骤与手册一致率 ≥ 85%；引用覆盖率 100%；P95 ≤ 8s。
- **边界**：不生成未经手册或工单支持的"经验式"步骤；不下发任何机台指令。

#### FR-02 多模态提问（图片 + 文本）

- **业务问题**：现场大量信息以图片形态存在，而工程师最自然的表达方式是"拍一张照片 + 一句话"。
- **处理流程**：上传/粘贴图片 → 预处理（压缩、格式统一）→ 阿里云 `qwen-vl-max` 结构化识别 → 识别结果并入检索 query → 走原 RAG 链路。
- **识别输出**：

```json
{
  "image_type": "alarm_screen | nameplate | table | photo | diagram | unknown",
  "extracted_text": "图内所有文字",
  "alarm_codes": ["E-2041"],
  "device_model": "Etcher-A",
  "components": ["O-ring", "压力传感器"],
  "table_markdown": "| 参数 | 值 |",
  "confidence": 0.82,
  "uncertain": ["型号第二位数字模糊"]
}
```

- **四类场景的用法**：

| 图片类型 | 提取什么 | 怎么用 |
| --- | --- | --- |
| 报警画面截图 | 报警码 | 强标识符 → BM25 权重提高，精确检索 |
| 设备铭牌/型号照片 | 设备型号 | 元数据过滤 `device_model` |
| 参数表截图 | Markdown 表格 | 转文本参与检索 |
| 部件照片 | 部件名称 | 术语归一后检索 |

- **设计红线**：**VLM 只做"转写与命名"，不做判断、不做回答。** 识别结果必须经过检索与引用校验，禁止 VLM 直接给出维修结论（否则绕过零幻觉机制）。
- **失败处理**：`confidence < 0.5` 或 `image_type = unknown` 时，明确提示"图片未能识别，请用文字描述现象"，**不允许猜测**。
- **验收标准**：图片关键信息提取准确率 ≥ 85%（报警码、型号等强标识符 ≥ 90%）；图片类问题引用覆盖率 100%。

#### FR-03 跨源检索

- **能力**：混合检索（BM25 + 向量）、查询改写与术语归一、Cross-Encoder 精排、元数据过滤、多轮上下文保持。
- **术语归一示例**：

| 口语/简称 | 标准检索词 | 同义词 |
| --- | --- | --- |
| 刻蚀机 / 蚀刻 | Etcher / Etch | 干法刻蚀、等离子刻蚀、RIE、ICP |
| 镀膜 / 薄膜 | Deposition / Thin Film | CVD、PVD、ALD |
| 光刻 | Photolithography | 曝光、涂胶显影、Track |
| 抛光 | CMP | 化学机械研磨、Slurry |
| 真空度跳变 | chamber pressure 波动 | 压力异常、真空泄漏、O-ring |
| 设备报警 | Alarm Code | 报警码、FDC 报警 |

- **验收标准**：10 个典型查询 Top-5 命中率 ≥ 80%。
- **权限约束**：**检索即鉴权**——按用户角色在检索层过滤（向量查询带过滤条件），而非在答案层删除。

#### FR-04 答案溯源

- **能力**：结论句挂引用标记；引用卡片展示文档名、版本、章节、页码；图片类依据展示缩略图并可查看原图。
- **硬性要求**：缺少引用清单的回答视为产品缺陷（测试用例覆盖）。
- **验收标准**：引用覆盖率 **100%**；引用真实性 ≥ 95%（引用块确实支持该结论）。

#### FR-05 置信度标注与拒答

**计算公式**

```
confidence = 0.40 × 精排 Top-1 归一化分
           + 0.35 × 引用覆盖率
           + 0.25 × 来源权威度（厂商手册 1.0 / 内部 SOP 0.8 / 工单案例 0.6）
```

| 等级 | 区间 | 呈现与动作 |
| --- | --- | --- |
| 高 | ≥ 0.80 | 直接给结论 |
| 中 | 0.60–0.80 | 给结论 + 标注"需人工确认" |
| 低 | < 0.60 | 只给相关材料清单 + 未覆盖说明，**不给操作步骤** |

- **拒答条件**：无候选块 / 精排最高分低于阈值 / 生成结果为 `NOT_COVERED`。
- **验收标准**：负样本正确拒答率 ≥ 90%。

#### FR-06 会话与历史

- 会话列表、历史消息回看、多轮追问；会话状态落盘（服务重启后仍在）。
- **验收标准**：刷新或重启后历史可恢复；追问能正确引用上文。

#### FR-07 反馈记录

- 四态反馈（采纳 / 部分采纳 / 驳回 / 修正）+ 备注，落库；驳回案例记入 `badcases.md` 并输出每周归因。
- **验收标准**：反馈 100% 落库；每周产出 badcase 归因报告。

#### FR-08 知识入库与索引重建

- 文档上传 → 解析（保留页码）→ 抽图（生成 caption）→ 清洗 → 结构感知切片（写入元数据）→ 向量化 → 入库。
- **验收标准**：一键重建索引 ≤ 5 分钟；切片元数据完整率 100%（`page`、`version` 必备）。

#### FR-09 培训与认证

- **业务问题**：设备原厂把售后服务交由第三方服务商承担后，服务商工程师缺少原厂知识渠道；新人培养周期长（12–18 个月），且培训内容常与最新设备变更脱节。同时，第三方工程师需经原厂认证才能执行部分设备的维护，而认证依赖原厂派人，成本高、覆盖慢。
- **本期交付（分层讲解 + 高频问题记录）**：复用问答主链路，切换**讲解模板**与讲解深度——按"原理 → 判断依据 → 操作步骤 → 常见误区"四段展开，并关联真实案例与手册章节；记录高频问题，用于反推培训内容与知识补充。
- **后续批次（考核与认证）**：基于知识库生成练习与考核题、认证记录与到期提醒、按设备类型分级的授权认证。**本期不实现**。
- **输入**：学员提问；可选的学习任务（如"讲解 3.4 节"）。
- **输出**：分层讲解 + 引用 + 关联案例；练习建议。
- **验收标准**：培训类问题可答且引用覆盖率同为 100%；高频问题记录可用。
- **边界**：**不替代正式认证考核、不签发认证结论**；不承诺培训效果；涉及安全风险的高级操作内容默认不展开。

#### FR-10 备件查询与订购引导

- **业务问题**：诊断指向换件时，服务商工程师需要立刻确认"有没有货、能不能替代、多久到"。这些信息散在原厂 ERP 与线下沟通中，导致等件停机；替代件兼容性靠经验判断，用错会损坏设备。
- **做法**：按部件名称/型号（可来自诊断结论）查询库存与在途；给出替代件及其**兼容性依据**；返回预计到货时间；生成**询价单草稿**，供人工确认后走原有采购流程。
- **输入**：部件名称或型号、设备型号与版本、数量。
- **输出**：库存状态、可用替代件与兼容性依据、预计可用时间、询价单草稿。
- **验收标准**：抽查 10 次查询与数据源一致率 100%；替代件建议必须附依据，无权威依据时明确输出"需原厂确认"。
- **边界**：**不自动下单**；不做价格谈判、备件商城、采购结算；兼容性无依据时**不允许推断**。

> FR-10 同样受零幻觉约束：没有权威依据就明说，不做"应该能替代"的推断——备件用错会损坏设备。

### 3.3 非功能需求

| 编号 | 类别 | 要求 |
| --- | --- | --- |
| NFR-01 | 性能 | 首 Token ≤ 2s；文字 P95 ≤ 8s；图片类 P95 ≤ 15s（识别与检索并行） |
| NFR-02 | 并发 | 支持 20 并发问答 |
| NFR-03 | 安全 | 密钥只进 `.env` 且不入版本库；日志脱敏（含图片路径与识别结果）；检索层权限过滤 |
| NFR-04 | 可维护 | 提示词、切片参数、检索权重、阈值全部配置化（YAML），改配置不改代码 |
| NFR-05 | 可扩展 | 新增工具不改智能体代码；向量库访问收敛在单一模块，便于替换 |
| NFR-06 | 可观测 | 每次问答生成 `trace_id`，记录节点耗时、工具调用、检索命中、置信度与最终引用 |
| NFR-07 | 兼容 | Python 3.10~3.12；Chrome / Edge 最新两个版本 |

---

## 4. 智能体设计

### 4.1 智能体数量：1 个

**本系统设计 1 个智能体：`A1 设备维护问答智能体`。**

**为什么是 1 个：**

1. 本项目的核心是 RAG 质量，而质量由切片策略、元数据、混合检索权重、精排、上下文组织、引用约束决定——这些都在**一条图的节点里**，不在智能体数量上；
2. 多智能体需要额外的状态契约、任务路由、结果汇总与冲突消解，在 8 天周期内会挤占调优时间，收益为负；
3. 引用校验与置信度评分逻辑简单（引用是否存在、结论句是否都有引用），用**规则 + 代码**实现比做成独立智能体更可靠、更易测试；
4. `generate` 与 `verify` 在代码上保持模块与提示词分离，未来若需拆分只是"节点 → 子图"，不需要重构。

### 4.2 智能体职责与解决的业务问题

| 项 | 内容 |
| --- | --- |
| 编号 / 名称 | A1 设备维护问答智能体 |
| **解决的业务问题** | ① 工程师跨多个系统翻查资料耗时长（平均 2–4 小时定位根因）；② 通用大模型会编造维修步骤，回答无依据不可复核；③ 图纸、参数表、报警截图里的知识无法被检索利用；④ 新人缺少随时可问的答疑渠道 |
| 输入 | 用户问题（文本）+ 可选图片 + 会话上下文 |
| 输出 | 结论（逐句带引用）+ 依据清单（文档/版本/页码/原图）+ 置信度 + 未覆盖说明 + 安全前置提示 |
| 工具权限 | 只读：知识库检索、工单检索、图片识别、术语查询；**无任何写权限** |
| 降级策略 | 证据不足 → 拒答并建议转人工；检索超时 → 返回已获得材料并标注；图片识别失败 → 提示改用文字 |
| 关键指标 | Top-5 命中率 ≥ 80%；引用覆盖率 100%；要点命中率 ≥ 80%；拒答率 ≥ 90% |
| **不做什么** | 不生成未经证据支持的步骤；不给出无引用的参数数值；不下发机台指令；不做安全判定之外的专业结论 |

### 4.3 内部节点设计

| 节点 | 输入 → 输出 | 关键技术点 |
| --- | --- | --- |
| `ingest_image`（可选） | 图片 → 结构化识别结果 | 阿里云 `qwen-vl-max`，`temperature=0`；只转写不判断；与检索并行执行 |
| `rewrite` | 问题（+识别结果）→ 2–3 路检索式 | 术语归一（刻蚀机/Etcher、真空度/chamber pressure） |
| `retrieve` | 检索式 → 候选切片 | BM25(jieba) + 向量 + RRF 融合；元数据过滤（设备型号/类别） |
| `rerank` | Top 30~50 → Top 5~8 | Cross-Encoder 精排（输出 logits，直接比大小排序） |
| `build_context` | 候选 → 带编号上下文 | 去重、版本择优（新版优先）、`[n]` 编号、token 预算裁剪（约 6000） |
| `augment`（受限 agent） | 证据不足时补检索 | **硬边界**：≤2 轮、≤3 次工具调用、15s 超时、3 个工具白名单 |
| `generate` | 上下文 + 问题 → 结构化答案 | `with_structured_output`，`temperature=0`；只用上下文、逐句挂引用 |
| `verify` | 答案 → 置信度 + 拒答判定 | 引用真实性、引用覆盖率、置信度三级、拒答（**确定性代码**） |

**图结构**

```
ingest_image(可选) → rewrite → retrieve → rerank → build_context
                                        │
                          [证据是否足够?]──是────────────────┐
                                        │否                    │
                                        ▼                      │
                        augment 节点（受限 agent ≤2 轮）        │
                                        └──────────┬───────────┘
                                                   ▼
                                   generate → verify → respond
```

### 4.4 工具调用规划

#### 4.4.1 工具清单（控制在 5–7 个）

| 工具 | 用途 | 调用方式 | 实现 |
| --- | --- | --- | --- |
| `kb_search(query, filters, k)` | 主检索（混合检索 + 精排） | 主链路 `retrieve` 节点**固定调用** | 本地函数 |
| `kb_search_expand(query, relax)` | 证据不足时放宽条件重检索 | 受限 agent（≤2 轮） | 本地 `@tool` |
| `kb_get_chunk(id, neighbors)` | 取原文/邻接块、原图 | 上下文补全与溯源 | 本地 `@tool` |
| `ticket_search(symptom)` | 历史工单案例检索 | 诊断类问题 | **MCP（stdio）** |
| `glossary_lookup(term)` | 术语归一 | 改写节点辅助 | 本地函数 |
| `parts_query(part, device_model)` | 备件库存 / 替代件 / 到货时间查询 | 备件类提问（FR-10） | 本地 `@tool`（接口按 MCP schema 设计） |
| `kb_stats()` | 知识库覆盖情况 | 拒答时给建议 | 本地 `@tool` |
| `vision_extract(image_id)` | 图片结构化识别 | `ingest_image` 节点 | 本地函数（调阿里云） |

> **docstring 是工具的唯一说明书**：何时使用、参数含义、返回结构必须写清；工具总数控制在 10 个以内，否则模型选择准确率下降。

#### 4.4.2 决策权分配（核心设计）

| 环节 | 谁决定 | 理由 |
| --- | --- | --- |
| 要不要检索 | **代码**（固定流程） | 该检索不检索 = 幻觉；不该检索时检索 = 浪费 |
| 检索几次、什么条件 | 代码固定 1 次 + 受限 agent 兜底 ≤2 轮 | 可控、可评测 |
| 选哪个工具 | 模型（**白名单内**） | 参数不确定、路径不唯一，才值得交给模型 |
| 引用编号 `[n]` | **代码** | 绝不能让模型自己编 |
| 是否拒答 | **代码**（阈值判定） | 确定性逻辑 |
| 术语归一 | 代码（词典）+ 模型兜底 | 词典覆盖率高，够用 |
| 结果能否进答案 | **`verify` 节点** | 与谁调用无关，一律过校验 |

> 原则：**能用固定流程表达的，绝不交给模型自主决定。**

#### 4.4.3 是否使用 MCP

**判断标准**：满足任一条才使用 MCP——① 跨进程/跨系统边界；② 需独立部署、扩容或独立权限；③ 已提供现成 MCP server。否则一律本地 `@tool`。

| 候选 | 判断 | 理由 |
| --- | --- | --- |
| `ticket_search`（历史工单） | ✅ **使用 MCP** | 属系统边界，接口按 MCP 定义，未来替换实现不影响上层 |
| 知识库检索 `kb_search` | ❌ **不使用** | 与后端同进程同语言，包成 MCP 只会增加进程、异步复杂度与延迟 |
| 术语归一、引用校验、上下文构建 | ❌ **不使用** | 纯本地确定性逻辑，无跨系统需求 |

> 接入方式：服务端用 `fastmcp` 定义 `@mcp.tool`，以 **stdio** 传输；客户端建 `MultiServerMCPClient` 对象后 `await client.get_tools()`；加载失败须 `try/except` 降级为空工具列表，不得中断主链路。

---

## 5. 系统架构

### 5.1 总体架构

采用**三层架构**，全部组件单机部署。

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 接入层  Vue3 + Vite + Element Plus + Pinia                               │
│  问答页(SSE 流式) │ 图片上传/粘贴 │ 引用卡片(文字+原图) │ 置信度标签 │ 历史页 │
└──────────────────────────────┬───────────────────────────────────────────┘
                               │ REST + SSE
┌──────────────────────────────▼───────────────────────────────────────────┐
│ 编排层  FastAPI(async) + LangGraph 1.2 单图                              │
│  ingest_image → rewrite → retrieve → rerank → build_context →            │
│  [augment] → generate → verify → respond                                 │
│  SqliteSaver 会话状态 │ session_map 映射 │ 超时与预算控制                 │
├──────────────────────────────────────────────────────────────────────────┤
│ 工具与数据层                                                             │
│  本地工具：kb_search / kb_get_chunk / glossary / vision_extract          │
│  MCP(stdio)：ticket_search                                               │
│  RAG：Chroma + BM25(jieba) + EnsembleRetriever + Cross-Encoder 精排       │
│  存储：Chroma(向量) │ MySQL(业务) │ SQLite(会话) │ 文档与图片目录          │
│  模型：本地 embedding(bge-m3) │ 文本模型 │ 阿里云 qwen-vl-max(图片)        │
└──────────────────────────────────────────────────────────────────────────┘
```

### 5.2 RAG 核心链路

#### 5.2.1 离线链路

| 环节 | 做法 | 关键点 |
| --- | --- | --- |
| 加载 | PDF 用 `PyMuPDFLoader`（保留页码）；MD/TXT 用 `TextLoader`；DOCX/PPTX/XLSX 用 `MarkItDown` | 页码必须可得 |
| 抽图 | 抽取页内图片 → VLM 生成 caption、图内文字、表格转 Markdown | 过滤 logo 与装饰图 |
| 清洗 | 正则去除页眉页脚/水印，修复断行，统一全角半角，恢复章节结构 | 保留条款编号 |
| 切片 | 结构感知切片（按标题层级）+ 递归字符切片兜底 | `chunk_size 512~1024`、`overlap 10%~20%`；**步骤列表与参数表不切断** |
| 元数据 | 切片时写入元数据 | 元数据过滤与引用落地的前提 |
| 向量化 | bge-m3（1024 维），归一化 | **建库与查询必须使用同一模型与同一实例** |
| 入库 | Chroma 持久化目录 | 向量库访问收敛在单一模块 |

**元数据字段（必填）**

| 字段 | 示例 | 用途 |
| --- | --- | --- |
| `doc_id` / `source` | `etch_manual_v1.2.pdf` | 引用显示 / 去重 |
| `doc_title` | 刻蚀设备维护手册 | 引用卡片标题 |
| `version` | v1.2 | 版本择优、旧版降权 |
| `section` | 3.4 真空系统排查 | 引用定位 |
| `page` | 118 | 引用页码（无此字段引用不可核对） |
| `device_model` | Etcher-A | 元数据过滤 |
| `category` | 设备维护 / 工艺 / 标准 | 分类检索与权限 |
| `source_type` | text / image / ticket | 证据类型（前端渲染分支） |
| `image_path` | `data/images/etch_p118_2.png` | 图片证据展示 |

#### 5.2.2 在线链路

| 步骤 | 做法 | 参数起点 |
| --- | --- | --- |
| 1 图片识别（可选） | 阿里云 `qwen-vl-max`，与检索并行 | `temperature=0`，超时 5s 降级为纯文本 |
| 2 查询改写 | 2–3 路检索式 + 术语归一 | `temperature=0` |
| 3 向量检索 | 相似度检索 + 元数据过滤 | `k=15` |
| 4 关键词检索 | BM25 + jieba 分词 | `k=15` |
| 5 融合 | RRF 融合 | 权重 `[0.5, 0.5]`；报警码/型号类改 `[0.3, 0.7]` |
| 6 精排 | Cross-Encoder | 粗排 30~50 → 精排 top 5~8 |
| 7 上下文构建 | 去重 / 版本择优 / `[n]` 编号 / token 预算 | 上限约 6000 tokens |
| 8 生成 | 结构化输出 | `temperature=0` |
| 9 校验 | 引用真实性 / 覆盖率 / 置信度 / 拒答 | 确定性代码 |

#### 5.2.3 零幻觉四道关

| 关卡 | 位置 | 规则 | 不通过的动作 |
| --- | --- | --- | --- |
| ① 生成约束 | 提示词 + 结构化输出 | 只能使用给定上下文；数值必须能在上下文原文找到 | 重试 1 次 → 仍不合规则拒答 |
| ② 引用真实性 | `verify` 节点 | `[n]` 必须在候选编号内，且引用块支持该结论 | 删除该引用并降置信度；结论失去全部引用 → 拒答 |
| ③ 引用覆盖率 | `verify` 节点 | 每个结论句至少 1 个引用 | 覆盖率 < 100% → 标记"未完全溯源"，置信度降一级 |
| ④ 拒答判定 | `verify` 节点 | 无候选块 / 精排最高分低于阈值 / 输出 `NOT_COVERED` | 输出"知识库未覆盖 + 建议"，不给具体操作步骤 |

### 5.3 技术选型

| 组件 | 选型 | 说明 |
| --- | --- | --- |
| Agent 编排 | langgraph 1.2.x | 有向图状态机，支持检查点与中断 |
| LLM 框架 | langchain / langchain-core 1.3.x | `init_chat_model` 统一模型入口 |
| 会话记忆 | langgraph-checkpoint-sqlite | `SqliteSaver` 落盘 |
| 文本模型 | DeepSeek（`deepseek-chat`） | 以账号可用模型名为准 |
| 图片模型 | 阿里云 DashScope `qwen-vl-max` | 项目方自行开通与管理 |
| Embedding | bge-m3（Ollama，1024 维） | 建库与查询共用同一实例 |
| 向量库 | Chroma（持久化目录） | 单机轻量，支持元数据过滤 |
| 词法检索 | BM25 + jieba 分词 | 中文必须分词，否则关键词检索失效 |
| 融合 | EnsembleRetriever（RRF） | 权重可调 |
| 精排 | Cross-Encoder（bge-reranker-v2-m3） | 粗排 → 精排 |
| 文档解析 | PyMuPDFLoader / TextLoader / MarkItDown | 保留页码 |
| MCP | fastmcp（服务端）+ langchain-mcp-adapters（客户端） | stdio 传输 |
| 后端 | FastAPI + uvicorn（全异步） | SSE 流式输出 |
| 业务库 | SQLAlchemy + MySQL | 用户、文档、问答、反馈 |
| 前端 | Vue3 + Vite + Element Plus + Pinia | — |
| 部署 | Docker Compose | 单机容器化 |
| Python | 3.10 ~ 3.12 | — |

### 5.4 接口设计

| 方法 | 路径 | 说明 | 返回 |
| --- | --- | --- | --- |
| POST | `/api/upload/image` | 上传图片，返回 `image_id` | JSON |
| POST | `/api/chat/stream` | 提问（可带 `image_ids`） | **SSE 流** |
| GET | `/api/chat/sessions` | 会话列表 | JSON |
| GET | `/api/chat/sessions/{id}/messages` | 历史消息（含引用） | JSON |
| POST | `/api/feedback` | 提交反馈 | JSON |
| POST | `/api/admin/documents` | 上传文档并触发入库 | JSON |
| GET | `/api/admin/chunks` | 切片查询（调试用） | JSON |
| GET | `/api/health` | 健康检查（向量库 / 模型 / VLM / MCP 状态） | JSON |

**SSE 事件契约（前后端唯一契约）**

| event | data | 说明 |
| --- | --- | --- |
| `meta` | `{session_id, trace_id, ts}` | 流开始 |
| `image` | `{image_id, image_type, extracted{...}, confidence}` | 图片识别结果（先于正文） |
| `token` | `{delta}` | 正文增量 |
| `citations` | `{items:[{id, source_type, doc, version, section, page, chunk_id, image_url}]}` | 引用列表 |
| `done` | `{status, confidence, label, uncertain[]}` | 结束；`status` ∈ `OK` / `NOT_COVERED` |
| `error` | `{code, message}` | 错误 |

> 事件之间以空行分隔。图片先上传拿 `image_id`，再在 chat 请求中引用；SSE 端点不处理文件上传。

### 5.5 前端设计（Vue3）

| 项 | 做法 |
| --- | --- |
| 框架 / UI | Vue3 + Vite；Element Plus |
| 状态管理 | Pinia：`useChatStore`、`useSessionStore` |
| 路由 | Vue Router：`/chat`、`/history`、`/admin` |
| Markdown 渲染 | `markdown-it` + `highlight.js` |
| 流式接收 | `utils/sse.ts`：`fetch` + `response.body.getReader()` 解析 `text/event-stream` |
| 图片交互 | 粘贴截图（`paste` 事件）+ `el-upload`；引用图片用 `el-image` 预览 |

| 组件 | 职责 |
| --- | --- |
| `ChatInput.vue` | 输入、发送、图片上传/粘贴 |
| `MessageList.vue` / `MessageItem.vue` | 消息渲染、流式追加、图片缩略图 |
| `CitationCard.vue` | 引用列表（文字 / 图片两种分支） |
| `SourceDrawer.vue` | 原文片段与原图查看 |
| `ConfidenceTag.vue` / `StatusBadge.vue` | 置信度与状态（正常 / 未覆盖 / 低置信度） |
| `FeedbackBar.vue` | 采纳 / 驳回 / 修正 + 备注 |

> **前端不使用 `EventSource`**：它只支持 GET 且无法携带自定义请求头，无法满足 POST 提问与鉴权需求。

### 5.6 数据库设计

| 表 | 关键字段 |
| --- | --- |
| `kb_document` | id, doc_title, source_path, category, version, uploader, status, created_at |
| `kb_image` | id, doc_id, page, section, image_path, caption, extracted_text |
| `qa_record` | id, session_id, question, answer, citations(JSON), confidence, status, latency_ms, image_ids, created_at |
| `qa_feedback` | id, qa_id, type, comment, created_at |
| `session_map` | session_id, thread_id, title, updated_at |

> 会话状态由 `SqliteSaver` 管理（其官方支持 SQLite / PostgreSQL），业务数据使用 MySQL。

---

## 6. 工程目录结构

### 6.1 组织原则

1. **按职责分包**：编排、节点、检索、工具、接口各归其位，边界清晰可独立测试；
2. **横切能力独立**：引用校验、评测脚本不散落在业务代码中；
3. **配置与代码分离**：提示词、切片参数、检索权重、阈值全部配置化；
4. **依赖单向**：`routers → services → agents/tools/rag → config`，禁止反向 import；
5. **向量库访问收敛**：仅 `rag/store.py` 可直接调用向量库，便于后续替换。

### 6.2 目录树

```
smka/
├── README.md                        # 快速开始：装依赖 → 建索引 → 跑评测 → 起服务
├── requirements.txt                 # 依赖版本固定
├── .env.example                     # 环境变量占位（真实 .env 不入库）
├── .gitignore                       # 含 .env / data/
├── docker-compose.yml               # backend + frontend + mysql
├── Makefile                         # make dev / test / eval / up
│
├── backend/
│   ├── main.py                      # FastAPI 入口 + lifespan 预热（agent / MCP / 模型）
│   ├── setting.py                   # 全部配置：.env + 切片/检索/精排/生成/多模态参数
│   ├── dependency.py                # 单例依赖（settings / agent / service）
│   ├── schemas.py                   # 全部请求响应模型（chat / upload / feedback / admin）
│   ├── db.py                        # 数据表模型 + 数据库会话
│   ├── routers/                     # 只做 HTTP 翻译（async def + Depends）
│   │   ├── chat.py                  # SSE 问答 + 会话 + 图片上传 + 反馈 + 健康检查
│   │   └── admin.py                 # 文档入库 / 索引重建 / 切片查询
│   ├── services/
│   │   ├── chat_service.py          # 会话管理 + 调图 + 包 SSE 响应
│   │   └── kb_service.py            # 文档与图片入库、索引重建
│   ├── agents/
│   │   ├── graph.py                 # LangGraph 主图（契约）
│   │   ├── state.py                 # AgentState / Evidence / Citation（契约）
│   │   ├── agent_factory.py         # 异步单例
│   │   ├── nodes/
│   │   │   ├── retrieval.py         # rewrite / retrieve / rerank / build_context
│   │   │   ├── generation.py        # generate + verify（引用校验、置信度、拒答）
│   │   │   └── multimodal.py        # ingest_image + augment（受限 agent）
│   │   └── prompts/
│   │       ├── system.md            # 零幻觉总则
│   │       ├── generate.md          # 生成 + 输出格式
│   │       ├── rewrite.md
│   │       └── vision.md            # 图片识别（只转写不判断）
│   ├── tools/
│   │   ├── __init__.py              # 本地工具 + MCP 工具聚合（含降级）
│   │   └── kb_tools.py              # kb_search / kb_search_expand / kb_get_chunk / kb_stats
│   │                                #   vision_extract / glossary_lookup / parts_query
│   ├── mcp_servers/
│   │   └── ticket_server.py         # 历史工单检索（stdio）
│   ├── mcp_client.py                # MCP 客户端封装 + 异常降级
│   ├── memory.py                    # SqliteSaver + session_id ↔ thread_id 映射
│   ├── rag/
│   │   ├── ingest.py                # 解析 + 抽图 + 清洗 + 切片（写入元数据）
│   │   ├── store.py                 # embedding + 向量库（唯一出口）
│   │   ├── retriever.py             # BM25 + 向量 + RRF + 精排 + 上下文构建
│   │   └── citation.py              # 引用生成与校验
│   ├── config/
│   │   └── glossary.yaml            # 术语归一表
│   ├── utils/
│   │   ├── image.py                 # 压缩 / 格式统一 / 体积校验
│   │   └── text.py                  # 分句 / 脱敏 / 日志
│   └── data/                        # 运行时数据（不入 git）
│       ├── chroma/  ├── sqlite/  ├── uploads/  └── images/
│
├── frontend/                        # Vue3 + Vite
│   ├── index.html
│   ├── package.json
│   ├── vite.config.ts               # /api 代理
│   └── src/
│       ├── main.ts
│       ├── App.vue
│       ├── router/index.ts
│       ├── stores/{chat.ts,session.ts}
│       ├── api/{http.ts,chat.ts,upload.ts,feedback.ts,admin.ts}
│       ├── utils/sse.ts             # fetch + ReadableStream 解析
│       ├── views/{ChatView.vue,HistoryView.vue,AdminView.vue}
│       ├── components/
│       │   ├── chat/{ChatInput.vue,MessageList.vue,MessageItem.vue,ImageThumb.vue}
│       │   ├── citation/{CitationCard.vue,SourceDrawer.vue}
│       │   └── common/{ConfidenceTag.vue,StatusBadge.vue,FeedbackBar.vue}
│       └── styles/
│
├── data/
│   ├── raw/                         # 原始文档
│   └── eval/
│       ├── golden_qa.jsonl          # 黄金集（含负样本）
│       ├── golden_image.jsonl       # 图片类样例
│       └── badcases.md              # badcase 记录
│
├── scripts/
│   ├── ingest.py                    # 一键入库（解析 → 抽图 → 切片 → 向量化）
│   ├── build_index.py               # 重建索引
│   ├── inspect_chunks.py            # 查看切片（调分块用）
│   ├── eval.py                      # 跑黄金集出指标
│   └── ask.py                       # 命令行单问（调试）
│
├── tests/
│   ├── test_rag/{test_ingest.py,test_retriever.py,test_citation.py}
│   ├── test_agents/{test_graph.py,test_verify.py}
│   ├── test_multimodal/test_vision.py   # 含"识别失败必须拒答"用例
│   ├── test_mcp/test_ticket_server.py
│   ├── test_api/test_sse.py
│   └── fixtures/
│
└── deploy/
    ├── Dockerfile.backend
    ├── Dockerfile.frontend
    └── nginx.conf
```

### 6.3 关键文件职责

| 文件 | 职责 | 变更影响 |
| --- | --- | --- |
| `agents/graph.py` | 主流程拓扑 | 影响全局，需评审 |
| `agents/state.py` | 状态与证据结构 | 契约级变更，所有节点需同步 |
| `agents/nodes/generation.py` | 生成与 `verify`（引用校验、置信度、拒答） | **质量底线，禁止绕过** |
| `agents/prompts/generate.md` | 零幻觉提示词 | 每次变更必须跑评测 |
| `rag/store.py` | 向量库唯一出口 | 替换向量库只改此文件 |
| `rag/ingest.py` | 切片与元数据 | 直接影响检索质量 |
| `setting.py` | 全部可调参数与密钥 | 调参不改代码 |
| `mcp_client.py` | MCP 接入与降级 | 工具可用性依赖 |

### 6.4 开发规范

| 项 | 规范 |
| --- | --- |
| 命名 | Python 用 `snake_case`，类用 `PascalCase`；前端组件用 `PascalCase.vue`，工具函数用 `camelCase` |
| 依赖方向 | `routers → services → agents/tools/rag → config`，禁止反向 import |
| 异步 | 全链路异步（工具为异步接口）；异步初始化不使用缓存装饰器，避免阻塞事件循环 |
| 配置 | 提示词、阈值、权重一律写入 `setting.py` 与 `.env`，禁止硬编码在业务逻辑中 |
| 接口先行 | 状态结构、工具接口、SSE 事件变更必须先改契约与测试，再改实现 |
| 分支与提交 | `main` ← `develop` ← `feature/*`；提交信息用 `feat/fix/docs/test/chore` 前缀 |
| 测试要求 | 新增能力必须附黄金集用例；多模态相关变更必须附"识别失败"用例 |
| 密钥 | 只进 `.env`，不入版本库；日志与前端不得出现密钥 |
| 禁止事项 | 不擅自升级依赖版本、不引入技术栈外组件、不绕过 `verify` 节点、不写死返回值让测试变绿 |

---

## 7. 开发里程碑（8 个工作日）

### 7.1 总览

| 天 | 主题 | 交付物 | 验收标准 |
| --- | --- | --- | --- |
| D1 | 环境与数据 | 开发环境与依赖；文档加载/清洗/切片/抽图；黄金集 v0（20 条，含 3 条图片与 2 条负样本） | 切片元数据完整率 100%（`page`、`version` 齐全）；抽检 20 块合格 ≥ 18 |
| D2 | 索引与检索 | 向量化、向量库、BM25、混合检索、一键重建索引 | 10 个查询 Top-5 命中 ≥ 70%；建索引 ≤ 5 分钟 |
| D3 | 检索增强 | 查询改写、术语归一、元数据过滤、Cross-Encoder 精排 | Top-5 命中 ≥ 80%；精排后 Top-8 含正确块 ≥ 90% |
| D4 | 生成与引用（核心日） | 生成提示词、结构化输出、引用校验、置信度与拒答；图片识别节点；培训分层讲解模板（FR-09） | 引用覆盖率 **100%**；抽检 20 条无编造；负样本拒答 ≥ 80%；图片提取 ≥ 85% |
| D5 | 服务化 | FastAPI、SSE 流式、图片上传接口、会话与落盘、问答落库 | 接口可调通；首 Token ≤ 2s；多轮追问生效；重启后历史仍在 |
| D6 | 前端（Vue3） | 问答页、图片上传/粘贴、引用卡片（文字/图片）、置信度标签、历史页 | 浏览器端到端可演示；引用可点击查看原文与原图 |
| D7 | 工具与管理页 | 工单检索 MCP 工具（含降级）、受限 agent 补检索节点；**备件查询工具与示例数据（FR-10）**；上传页、切片查看 | MCP 工具可独立调用；故意让工具报错主链路仍可用；备件查询与数据源一致 |
| D8 | 评测与交付 | 黄金集扩到 50 条；评测脚本跑分；badcase 归因；Docker Compose；README；演示 | G1–G5 达标或说明差距；一键建库 + 一键评测；3 个演示场景通过 |

### 7.2 每日要点

| 天 | 关键技术点 | 当日风险 |
| --- | --- | --- |
| D1 | 页码与章节必须抽对——它决定引用能否落到"P.118"；步骤列表与参数表不切断 | 文档为扫描件无文本层 → 换文档或改用章节号作引用 |
| D2 | 中文 BM25 必须分词（`jieba`）；建库与查询共用同一 embedding 实例 | 向量化模型下载/依赖问题 → 优先用本地小模型或云端接口 |
| D3 | 改写保留原始查询作为一路，避免改写引入噪声；精排分数为原始分（可负），直接比大小 | 精排模型跑不动 → 降级为模型打分 |
| D4 | 提示词 + 结构化输出 + 规则校验三管齐下；**每次改提示词都要跑评测，禁止凭感觉改** | 模型不守规矩 → 校验不过重试 1 次后拒答 |
| D5 | 全链路异步；异步初始化放在应用启动钩子中，不使用缓存装饰器 | 同步调用阻塞事件循环 → 放线程池 |
| D6 | 流式用 `fetch` + 流读取，不用 `EventSource`；跨端口配置开发代理 | 图片上传与消息体混用 → 先上传拿 `image_id` 再提问 |
| D7 | MCP 服务端脚本与解释器均用绝对路径；加载失败必须降级 | 工具报错中断主链路 → `try/except` + 空工具兜底 |
| D8 | 评测集覆盖正常问答、多轮、图片、无依据四类 | 指标不达标 → 按 7.4 顺序取舍，**先保引用与拒答** |

### 7.3 每日最小闭环

> 每天收工前必须跑通：**提问 → 得到带引用的答案 → 评测脚本记录指标**。
> 即使 D2 只有"向量检索 + 拼接上下文 + 直接生成"的粗糙链路，也要保持端到端可运行；之后每天只替换链路中的某一环（检索 → 重排 → 引用 → 校验）。

### 7.4 时间紧张时的取舍顺序

| 顺序 | 砍掉 | 影响 | 替代方案 |
| --- | --- | --- | --- |
| 1 | 界面样式打磨、移动端适配 | 观感下降 | 桌面端可用即可 |
| 2 | 反馈落库与统计 | 无反馈数据 | 界面保留按钮 |
| 3 | 工单 MCP 工具 | 少一个数据源 | 只做文档源，接口保持可扩展 |
| 4 | 图片入库（知识侧） | 图片知识不可检索 | 保留图片提问（输入侧） |
| 5 | 查询改写 | 命中率降 3–5 个点 | 原问题 + 术语同义词扩展 |
| 6 | Cross-Encoder 精排 | 命中率降 5–8 个点 | RRF 排序 + 增大 Top-K |
| **不砍** | **强制引用 + 引用校验 + 无依据拒答** | —— | **项目底线，任何情况下保留** |

### 7.5 工时分配

| 工作项 | 人天 | 占比 |
| --- | --- | --- |
| **RAG 链路（解析/切片/索引/检索/精排/上下文/生成/引用/校验）** | **4.5** | **约 56%** |
| 多模态（图片识别 + 前端交互 + 抽图） | 1.0 | 13% |
| 服务化与前端 | 1.5 | 19% |
| 评测与交付 | 1.0 | 12% |

---

## 8. 测试与验收

### 8.1 评测集设计

```json
{"id":"q001","question":"刻蚀机腔体真空度异常波动，应该按什么顺序排查？",
 "category":"设备维护","device_model":"Etcher-A",
 "answer_points":["检查腔体密封 O-ring","确认真空泵抽速","查看压力传感器零点漂移","检查气体管路泄漏"],
 "must_cite":["刻蚀设备维护手册#3.4"],"negative":false}
{"id":"q002","question":"XX 新型号腔体的清洗周期是多少？",
 "category":"设备维护","negative":true,"expect":"NOT_COVERED",
 "note":"知识库无此文档，给出具体周期数值即判不通过"}
{"id":"img001","image":"fixtures/alarm_E2041.png","question":"这个报警怎么处理？",
 "expect_extract":{"alarm_codes":["E-2041"]},
 "must_cite":["刻蚀设备维护手册#5.2"],"negative":false}
```

- **规模**：D1 建 20 条 → D8 扩到 50 条；
- **构成**：设备维护 40%、工艺制程 25%、标准规范 15%、负样本 10%、图片类 10%；
- **用途**：每日跑一次，作为"今天有没有变好"的唯一客观依据。

### 8.2 测试类型

| 类型 | 内容 | 方式 |
| --- | --- | --- |
| 单元测试 | 切片元数据完整性、清洗规则、引用校验（伪造引用必须被识别）、图片预处理 | pytest |
| 检索评测 | 命中率 / Recall / MRR / NDCG | 评测脚本 |
| 生成评测 | 要点命中率、引用覆盖率、拒答率 | 评测脚本 |
| 多模态评测 | 关键信息提取准确率、识别失败拒答正确率 | 评测脚本 |
| 接口测试 | SSE 事件完整性（meta/image/token/citations/done）、错误码 | pytest |
| 功能测试 | 多轮追问、历史回看、反馈落库、上传入库 | 手工 |

### 8.3 验收标准

| 指标 | 目标 | 是否可降级 |
| --- | --- | --- |
| Top-5 检索命中率 | ≥ 80% | 可降至 70% |
| **引用覆盖率** | **100%** | **不可降级** |
| 引用真实性 | ≥ 95% | 可降至 90% |
| 答案要点命中率 | ≥ 80% | 可降至 70% |
| 负样本正确拒答率 | ≥ 90% | 可降至 80% |
| 图片关键信息提取准确率 | ≥ 85% | 可降至 75% |
| 备件查询一致性 | 抽查 10 次与数据源一致 100% | 可降至 90% |
| 首 Token / 文字 P95 | ≤ 2s / ≤ 8s | 可放宽至 3s / 12s |
| 图片类 P95 | ≤ 15s | 可放宽至 20s |

---

## 9. 风险与应对

| 编号 | 风险 | 概率 | 影响 | 应对 |
| --- | --- | --- | --- | --- |
| R1 | 文档为扫描件或无页码，引用无法落地 | 中 | 高 | D1 确认文本层与页码；否则改用语料或退化到章节号引用 |
| R2 | 模型仍编造内容 | 高 | 高 | 四道关（生成约束/引用真实性/覆盖率/拒答）；校验不过一律拒答 |
| R3 | 图片识别不准或失败 | 中 | 中 | 识别失败明确拒答不猜测；强标识符阈值更高；保留文字输入兜底 |
| R4 | 多模态拉高响应时间 | 高 | 中 | 识别与检索并行；图片类单独定 P95；超时降级为纯文本 |
| R5 | 中文分词不当导致关键词检索失效 | 中 | 中 | BM25 传入分词函数；补充领域术语词典 |
| R6 | 同步调用阻塞事件循环，流式卡顿 | 中 | 中 | 全链路异步；必要时放线程池 |
| R7 | MCP 工具加载失败影响主链路 | 中 | 中 | 异常捕获后降级为空工具列表；健康检查暴露状态 |
| R8 | 8 天工期超期 | 高 | 高 | 按 7.4 顺序取舍；**保引用与拒答**；每日跑评测看进度 |
| R9 | 密钥泄漏（含云服务凭证） | 低 | 高 | 密钥只进 `.env`；不入版本库；日志脱敏 |

---

## 附录

### 附录 A：生成提示词骨架（零幻觉核心）

```text
你是半导体设备维护知识助手。你只能依据【上下文】回答问题。

硬性规则：
1. 每个结论句末尾必须标注来源编号（如 [1]、[2][3]）；没有来源的句子不要写。
2. 只能使用【上下文】中的信息，不得用你自己的知识补充维修步骤、参数、数值、标准条款。
3. 上下文不足以回答时，只输出 {"status":"NOT_COVERED","reason":"缺少哪类信息","suggest":"建议动作"}。
4. 涉及拆机、带电、真空/特气、联锁的操作，必须在结论前加"安全前置："说明。
5. 数值（力矩、压力、温度阈值、浓度）必须能在【上下文】中原文找到；找不到就写"以手册原文为准"。
6. 术语统一用行业标准表述（如 Etch/刻蚀、CMP、ALD）。

输出 JSON（不要输出其他内容）：
{"status":"OK|NOT_COVERED",
 "conclusions":[{"text":"结论","citations":[1,3]}],
 "steps":[{"order":1,"action":"步骤","citations":[2]}],
 "safety":"安全前置说明或空字符串",
 "uncertain":["不确定/未覆盖的点"]}

【上下文】
[1] {doc_title} {version} · {section} · P.{page}
{chunk_text}
[2] ...
```

### 附录 B：图片识别提示词骨架（只转写不判断）

```text
你是半导体设备维护资料的图片信息提取器。你只做"转写与命名"，不做任何判断或建议。

要求：
1. 提取图中所有可见文字，保持原文；表格转为 Markdown 表格。
2. 识别图片类型：alarm_screen（报警画面）/ nameplate（铭牌）/ table（参数表）/
   photo（现场照片）/ diagram（图纸）/ unknown。
3. 报警画面：提取报警代码（如 E-2041）与报警文本。
4. 铭牌：提取设备型号、序列号、厂商。
5. 不确定或模糊的内容写入 uncertain 数组，不得猜测。
6. 严禁给出故障原因、维修建议或处置步骤。
7. 输出 JSON：{image_type, extracted_text, alarm_codes[], device_model, components[],
   table_markdown, confidence, uncertain[]}
```

### 附录 C：环境变量清单

```env
# 文本模型
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=
DEEPSEEK_CHAT_MODEL=deepseek-chat

# 图片识别（阿里云，由项目方自行开通与管理）
DASHSCOPE_API_KEY=
DASHSCOPE_BASE_URL=
VISION_MODEL=qwen-vl-max
VISION_TIMEOUT_MS=5000

# 本地向量化模型
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=bge-m3

# 存储
MYSQL_HOST= / MYSQL_PORT= / MYSQL_DB= / MYSQL_USER= / MYSQL_PASSWORD=
SQLITE_DB_PATH=./backend/data/sqlite/app.db
CHROMA_DIR=./backend/data/chroma
UPLOAD_DIR=./backend/data/uploads
IMAGE_DIR=./backend/data/images

# RAG 参数
CHUNK_SIZE=800
CHUNK_OVERLAP=120
RETRIEVAL_TOP_K=15
RERANK_TOP_N=6
CONTEXT_TOKEN_BUDGET=6000
CONFIDENCE_HIGH=0.80
CONFIDENCE_LOW=0.60
```

### 附录 D：速查表

| 维度 | 结论 |
| --- | --- |
| 开发周期 | 8 个工作日 |
| 智能体数量 | **1 个**（内含 8 个图节点，含 1 个受限 agent 节点） |
| 业务功能 | 10 项（P0 六项、P1 四项） |
| 架构 | 三层：接入层 / 编排层 / 工具与数据层 |
| 核心技术 | LangGraph 编排 + LangChain + Chroma + BM25(jieba) + Cross-Encoder 精排 + FastAPI + SSE |
| 前端 | Vue3 + Vite + Element Plus + Pinia |
| 多模态 | 图片 + 文本；阿里云 `qwen-vl-max`；**VLM 只转写不判断** |
| MCP | 1 个（历史工单检索）；知识库检索留在进程内 |
| 关键指标 | Top-5 命中 ≥ 80%、引用覆盖 100%、要点命中 ≥ 80%、拒答 ≥ 90%、图片提取 ≥ 85% |
| 底线 | 强制引用 + 引用真实性校验 + 无依据拒答，任何情况下不砍 |

---

**初稿结束**
